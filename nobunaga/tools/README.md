---
status: active
created: 2026-05-29
---
# tools/ — Registry
> The catalog of every script here, so sessions invoke instead of rebuild. **Status legend:** ✅ canonical (use this) · 🔁 has duplicate/variant siblings (consolidation pending — see [ROADMAP](../ROADMAP.md) Phase 3) · 🧪 one-shot/discovery (did its job; keep for provenance, don't extend) · ⚠️ gap/broken. Initial pass 2026-05-29 from a full inventory; entries marked 🔎 need a purpose re-confirm before anyone relies on them.

## Core VM / emulator infrastructure
| Script | Status | Purpose |
|---|---|---|
| `nobunaga_vm.py` | ✅ | VM + 6502 runner; loads SRAM, switches banks, symbolic trace via `mesen-labels.toml`. The "ask the ROM" tool. |
| `cpu6502.py` | ✅ | MOS 6502 interpreter backing the VM runner. |
| `vm_decompile.py` | ✅ | Bytecode → readable C. Default path for new routine investigations. **Spec-agnostic since 2026-05-30:** dispatch normalizes the mnemonic from the **opcode byte** (`OPCODE_TO_MNEMONIC`), so it consumes disasm from EITHER the old hand-annotated files OR fresh `vm-disasm.py` (v2) output — same C from both. Validated: 58/131 bank-1 subs byte-identical old-vs-new; the rest differ only because the new tool has correct lengths + live labels (grow formula reproduces identically). **Opcode coverage near-complete since 2026-05-31:** decoded the general FRAME-LOCAL ops ($81-$88/$A0-$A3/$90/$DE/$DF, all `vm_fp + signed offset` — rendered via the `_WORD_SLOT`/`frame_word/byte/addr`/`_signed_operand` helpers as `localN` or `*(word*)(fp - N)`), the missing comparison/arith mirror cases (`cmp_sge/ugt/ult`, `is_zero`, `div/mod_unsigned`, `bitxor`, `lshr/ashr_by_regB`), byte-memory mirrors, `op_CB` (negate), the indirect host calls `$EA`/`$DD` (`(*(callee))(args)`), and the ext_op `$18`/`$19` 4-byte imm32 loads. **`// TODO:` lines across all 4 banks 698 -> 0** — full opcode coverage. **M3 (2026-06-03):** call args print in callee-param order (reverse-push fix); signatures declare the REAL arity (`_body_arg_count`, not hardcoded `arg1..arg4`); `$B2` = NOP (`nop_B2`, was a bogus PUSHL); dead `recent_pos_writes` removed; `annotate_syscall` reads the ID from the leading arg. A default-off `SELF_CHECK` trace + `check_stack_trace()` back the drift-guard (run via `decompile-all.py --self-check`). **M4 var-walk (2026-06-03):** `load_var_names()` reads `mesen-labels.toml` `[vars.bankN."0xADDR"]` sections and `apply_var_names()` (LAST emit pass) rewrites the positional `arg1..arg4`/`local0..local11` to semantic names — `arg1->output` → `fief->output`. Word-bounded single-pass alternation (no cascade). Pilot: `effect_grow` `$87F0`. Deterministic. |
| `econ_sim.py` | ✅ | Verified economy simulator (Grow/Build/Give/Dam/harvest). |
| `analyze-vm-opcodes.py` | ✅ | Opcode dispatch-table analysis, 3 modes: `survey` (handler clustering), `dispatch` (handler byte dumps), `classify` (operand-fetch patterns). **Merged the opcode trio (dump-dispatch/opcode-classify/opcode-survey), 2026-05-29** — output verified byte-identical. |
| `decompile-all.py` | ✅ | **Decompile-all driver — the "see the whole picture" payoff (2026-05-30).** `py -3 tools/decompile-all.py [--banks 1] [--out-dir decompiled]`. Runs `vm-disasm.py` (v2) → `vm_decompile.py` over the 4 CODE banks (0/1/2/15; rest are data) → persists `decompiled/bank_NN.c`. **495 subs, ~11.8k lines of C, byte-deterministic** (fixed ROM → identical C). Run once, read the committed `.c` like a codebase; regenerate only when a tool improves. ~22% of lines are `// TODO:` (decompiler's semantically-unhandled opcodes — shrinks as opcodes get decoded). **Also auto-emits the merged `decompiled/all_banks.c`** (via `decompile-merged.py`) on a full default run, so the per-bank + merged views regen together and never drift; `--no-merged` skips it. Shared core extracted as `bank_subs()` (used by both writers → byte-identical C). **M4 (2026-06-03):** loads `VAR_NAMES` (the `[vars.*]` table) once and threads each sub's dict into `vm_decompile.decompile`, so per-bank + merged views get identical var renames. **`--self-check` (M3 drift-guard, 2026-06-03):** decompiles all 4 banks with `vm_decompile.SELF_CHECK` on, then asserts each gated opcode's OBSERVED data-stack Δ (from the real handlers) matches the audited `vm_stack_effect.STACK_EFFECT` — CLEAN at 179 gated opcodes / 0 exclusions; the standing guard against the next $30-$3F/$B2-class stack-model bug (negative-tested). Exit 1 on drift; no regen. |
| `decompile-merged.py` | ✅ | **Merged PRG-keyed view — all 4 code banks in ONE file (2026-06-01).** Auto-run by `decompile-all.py` (default), or standalone: `py -3 tools/decompile-merged.py [--out decompiled/all_banks.c]`. Reuses `decompile-all.py`'s `bank_subs` (same C, byte-for-byte), then makes it navigable as a flat address space: (1) **PRG rewrite** — every CPU addr → its bank-unambiguous PRG offset (`bank*0x4000+(cpu&0x3FFF)`), goto labels `L_pPPPPP`, comments `// PRG $P`; the same `$8003` in two banks gets distinct PRG offsets so ctrl-F a label finds ONE def. (2) **Call-target annotation** — each named call gets `// -> bankT $CPU` (follow cross-bank), and the entry trampolines resolve to the REAL bytecode entry they JMP into (`call_bank_wrap(2)` → `bank2 entry battle_init_driver $AFE1`, read from ROM; `call_bank10_entry(c)` → bank-10 audio `$8003`). (3) PRG-sorted TOC index at top. **495 subs, ~13.7k lines, byte-deterministic.** DERIVED view — `decompiled/bank_NN.c` stay the canonical regen-guarded artifacts; this is for searching/following. No name collisions (def names unique across banks, verified). |
| `mesen-labels.py` | ✅ | **Label projector — the single-source-of-truth applier (2026-05-31).** `mesen-labels.toml` is authoritative; this projects it onto its consumers (it never writes back). `py -3 tools/mesen-labels.py --mlb [PATH]` → Mesen 2 `.mlb` (default: beside the ROM, ROM basename, so Mesen **auto-loads** it on launch; types `NesPrgRom`/`NesInternalRam`/`NesSaveRam`). `--asm` → `disasm/bank_NN_named.asm` for the 4 CODE banks (0/1/2/15) with toml names applied to code labels + RAM operands — **bank 15 is the rich target** (188 code call-sites named; its raw disasm uses symbolic `b15_` operands), banks 0/1/2 mainly gain RAM-operand labels (read their code via `decompiled/*.c`). `--check` = stats only. **Normalizes `$C000-$FFFF` to the fixed bank 15** regardless of toml section (≈60 kernel labels are mis-filed under `[prg.bank0]`; the address is authoritative, warns so the toml can be cleaned). Deterministic + idempotent. The decompiler already reads the toml, so `decompiled/*.c` is covered for free. |
| `vm-opcode-labels.py` | ✅ | **VM-opcode handler label generator (2026-05-31, Session B).** `py -3 tools/vm-opcode-labels.py` (report) · `--toml` (lines) · `--write` (idempotent splice into `mesen-labels.toml`). Reads the $F026/$F126 table from ROM, groups 256 opcodes → 163 distinct handlers, and mechanically names each `vm_op_XX_<mnem>` from the VALIDATED `vm_decompile.OPCODE_TO_MNEMONIC` (Layer-1 propagation — the opcode semantics were already known, only the handler addresses were unnamed). Special-cases the shared syscall-trigger sink ($EF92) + flags undecoded opcodes. **Wrote 158 handler labels** → the whole VM instruction set reads in `bank_15_named.asm`. Deterministic. Re-run after `OPCODE_TO_MNEMONIC` improves. |
| `data-xref.py` | ✅ | **Who reads/writes a RAM/data VARIABLE — the data-walk frontier tool (B2).** `py -3 tools/data-xref.py hot [--unlabeled] [--min N] [--lo HEX --hi HEX]` ranks `$6xxx/$7xxx` addresses by ref count; `refs <addr>` groups every ref by its (named) containing sub. Indexes the `$A4-$AB` absolute-memory opcode family in the 4 banks' `*_vm.asm`. ⚠️ WRITE set is a LOWER BOUND (frame-relative/indirect stores carry no static addr). Drove the 56-var B2 RAM data-walk. |
| `rom-xref.py` | ✅ | **Who references a ROM TABLE/STRING — the ROM-side twin (B1), 2026-06-01.** ROM data is reached as IMMEDIATE-WORD pointers (`$8A/$8C/$8E` with a `$8000+` operand), NOT the abs family — so this is a separate index. **Bank-unambiguous:** keys labels by PRG offset via `nobunaga_vm.load_labels_by_prg()` (the flat `load_labels` collides switchable-window banks). `stats` (class breakdown) · `frontier [--unlabeled] [--class str\|tbl\|code] [--bank N]` (ranked, decoded preview, classified str/table/code-ptr — code = lands on an instruction boundary) · `refs <addr> [--bank N]` · `strings [--toml]` (decodes NUL-terminated ASCII in the referencing sub's BANK — per-bank: `$BA10` is a string in bank 0, empty in bank 1 — emits `msg_<slug>` labels) · `tables [--toml]` (provenance-names the non-string tables from the named sub that reads them: clean code-ptr tables → `jumptab_*`, else `<refsub>_data_<addr>`; defers combat-geometry tables). **DONE: 297 strings + 125 tables + the 3 analytical combat tables labeled** (`$B0BA/$B0EC` = strategic_map_fief_x/y, `$B55A` = combat_map_palette — decoded 2026-06-01, the `tbl` frontier is now **0**). Strings/tables are the ORACLE lane (zero agents — the bytes ARE the truth); the 3 combat tables were the analytical lane (named from referencer + adjacency cross-check). Only 12 code-pointers remain (correctly not data). The whole flow is crystallized as the `/data-walk` skill. |
| `vm_assemble.py` | ✅ | **Sea-16 VM ENCODER — the inverse of `vm-disasm.py` (Pass 2, 2026-06-03).** `encode_instr(mem,pc)` / `encode_sub(mem,stub,end)` / `encode_prologue` re-emit an instruction's bytes from its DECODED structured operand values (LE word, signed frame offset, `$D5`/`$D9` switch tables, `$E9` word+adj, `$B7` LE32 inline imm). Reuses `vm-disasm.instr_len` as the single length authority so encoder/disassembler can't disagree. NOT a byte-echo — it decodes then re-encodes, so a wrong layout/endianness is caught. Library for `vm-roundtrip.py`; CLI `py -3 tools/vm_assemble.py <bank>` self-tests one bank. |
| `vm-roundtrip.py` | ✅ | **VM encoding ROUND-TRIP — Rung 1 of validating the VM (Pass 2, 2026-06-03).** `py -3 tools/vm-roundtrip.py [bank] [--verbose]`. Re-encodes every bytecode sub in the 4 code banks and asserts BYTE-IDENTICAL to ROM. **PASS 2026-06-03: 495 subs, 45,024 code bytes, all 4 banks clean** → the opcode + operand-layout + length + endianness model is provably complete. The deterministic gate Pass-2 hangs on; exit 1 on any mismatch (prints first-diff address). Rung 2 (emulator stack-effect/meaning audit) is `vm-stack-audit.py`. |
| `vm_stack_effect.py` | ✅ | **Per-opcode STACK_EFFECT table — the single authority (Pass 2 / M2, 2026-06-03).** Declarative `STACK_EFFECT[op]` = `Effect(mnem, cls, pops, pushes, regL, regR, vcheck)`: the model of what each VM opcode does to the descending word stack + reg file (regL=$08/regR=$0C). Replaces the implicit truth scattered across `vm_decompile.py`'s 350-line dispatch. `cls` routes the audit: `data/push/pop/store/control` (gated on dSP + reg-keep), `call/return` (gated on call-depth), `probe` (report-only: ambiguous/unmodeled/ext-prefix/syscall/dead). `effect(op)` accessor; CLI prints a class census. **M3 (2026-06-03):** `vm_decompile.py`'s `--self-check` drift-guard now reads this table as the authority (the full table-driven dispatch rewrite is deferred to the VM teardown); `$B2` resolved to NOP (oracle micro-test, was disputed). |
| `vm-stack-audit.py` | ✅ | **VM SEMANTIC / stack-effect audit — Rung 2 (Pass 2 / M2, 2026-06-03).** `py -3 tools/vm-stack-audit.py [bank] [--max-per-op N] [--emit-verified] [--write-toml]`. Drives EVERY opcode occurrence in the 4 banks through the **live ROM handler** (`nobunaga_vm.step_one_vm_op`) with a controlled frame, observes SP/regL/regR/depth deltas, and asserts they match `vm_stack_effect.STACK_EFFECT` (+ qimm/abs value-checks). Catches the `$30-$3F`-class *meaning* bug that bytes-round-trip can't. **CLEAN 2026-06-03: 23,430 occurrences, 184/208 gated opcodes confirmed, 0 mismatch** (24 unobserved logged, 1 probe `$B7`). RETURN `$CF` validated via a paired CALL→RETURN micro-test (can't be isolated). **`$B2` settled (M3, 2026-06-03):** 0 ROM occurrences, so `validate_nop_b2` runs a synthetic $B2 through the real handler → NOP (dSP=0, regs kept), resolving the NOP-vs-PUSHL dispute. `--write-toml` conservatively annotates `vm-opcodes-v2.toml` (verified/pops/pushes) from a clean full run. Exit 1 on any gated mismatch. |
| `probe-compare-opcodes.py` | ✅ | **Compare-opcode ORACLE validator — primitive-layer guard the structural gates CANNOT catch (2026-06-04).** `py -3 tools/probe-compare-opcodes.py [-v]`. Drives each binary compare `$C0–$C9` through the **live ROM handler** with a 5-pair matrix (positive pair fixes DIRECTION; −1-vs-1 splits SIGNEDNESS), classifies the truth vector against the 10 candidate predicates, and asserts it matches BOTH the decompiler's `OPCODE_TO_MNEMONIC` + `_BINOP` (symbol+signedness) AND `vm-opcodes-v2.toml`. **Found + fixed a foundational bug: C3/C5 had le↔ge swapped and C6/C9 had lt↔ge swapped → 4 ops rendered BACKWARDS at 323 sites.** Why a dedicated probe: a wrong compare direction changes neither the bytecode (round-trip identical) nor the CFG (orientation flips consistently on raw+structured), so every other gate stays green while the C lies about every threshold/bound/guard. Exit 1 on any disagreement — run after touching the compare map, `_BINOP`, or the toml. [[feedback_foundation_discipline_early]] |
| `probe-ternary-folds.py` | ✅ | **Ternary value-diamond ORACLE validator (2026-06-04).** `py -3 tools/probe-ternary-folds.py [-v]`. A value-diamond (`regA = cond ? a : b`, two value-loading arms that converge) is folded to a C ternary, and `vm_cfg.bytecode_cfg` CONTRACTS the same diamond so the CFG gate stays green — but because the contraction is symmetric (raw + structured), the gate proves only that control-flow collapses consistently, NOT the ternary's VALUE/polarity (a flipped `cond ? b : a` passes every structural gate). So the oracle is the only witness: runs `min_word`/`max_word` ($CB5E/$CB6F, each a pure 2-arg fn whose whole body is one diamond) through the live ROM handler and asserts min/max — a polarity flip swaps them. Exit 1 on mismatch; run after touching the diamond detector or `_ternary_expr`. [[feedback_foundation_discipline_early]] |
| `vm_cfg.py` | ✅ | **CFG extractor + lowering + structured-equivalence relation — structuring epic, Phase 0–1 (2026-06-03).** Two CFGs over ONE key space (bytecode addresses, because the decompiler's `L_XXXX` labels ARE bytecode target addresses): `bytecode_cfg(instructions)` → `({leader: frozenset(succ)}, leaders)` ground truth; `lower_goto_cfg(raw, leaders)` lowers the RAW witness to it (per-block, ADDRESS-based → robust to out-of-order flushed-call lines AND folded statement-less blocks). `lower_struct_cfg(structured, leaders)` lowers the STRUCTURED form (brace-matched header `(true,false)` edges, merges via highest LEADER in the span so empty/folded arms resolve, if-else then-tail→merge redirect). `observable_blocks` + `contract` (bypass pure-routing / bare-`return` blocks) define `structured_equivalent(raw, struct, leaders)` = the relation that ACCEPTS structure_lines' behaviour-preserving folds but catches a misroute. EXIT sentinel. `py -3 tools/vm_cfg.py <bank> <sub_hex>` self-tests one sub. Library for `vm-cfg-gate.py`. |
| `vm-cfg-gate.py` | ✅ | **CFG equivalence gate — the honesty gate that licenses structuring folds (Phase 0–1, 2026-06-03).** `py -3 tools/vm-cfg-gate.py [--banks ..] [--verbose] [--show-structured]`. Two HARD gates (exit 1): (1) `lower_goto_cfg(raw) == bytecode_cfg` every sub — `--raw` is a CFG-faithful round-trip WITNESS + validates the extractor + flags unknown control mnemonics; (2) the GATED structured output `decompile()` emits is CFG-preserving for every sub. **CLEAN: 495/495 + 495/495.** Reports how many subs **fell back to honest goto** (structure_lines was not CFG-preserving there — currently **35**). The gating itself lives in `vm_decompile.decompile()` (Phase 1): a fold is kept only if `vm_cfg.structured_equivalent` holds, else the sub reverts to goto — which FIXED a real correctness bug (`num_to_ascii` $CF98 was returning garbage on its recursive branch). |
| `vm-disasm.py` | ✅ | **Bulk VM bytecode disassembler — RECOVERED 2026-05-30** (had drifted out of `tools/`, ch6). `py -3 tools/vm-disasm.py <bank 0-15> [-o out.asm] [--stdout] [--check]`. Walks every `JSR $E823` bytecode sub in a bank → readable VM-asm. Decode lengths come from the **execution-validated** `nobunaga_vm.OPCODE_INFO` (NOT the deleted old spec, which had wrong operand lengths — see ROADMAP drift note); `$D5`/`$D9` variable-length inline jump tables decoded from their handlers. `--check` runs the **exact-tiling** correctness test (135 stubs must tile the bank). ⚠️ Does **not** clobber the 4 hand-annotated `disasm/*_vm.asm`; its job is fresh banks. **Length fix 2026-05-31:** `instr_len` special-cases `$B7` (LONG/ext_op) — index `$18`/`$19` (`load_a32/b32_from_ptr3`) carry a 4-byte inline immediate, so those are length 6 (else 2). Caught via a `// TODO: op_FF` run in the decompiled C: `$FF`'s handler is a BRK trap, so 4 traps in a row meant the disassembler was decoding an ext_op's imm32 as opcodes; fix corrected 6 sites (5 were silently mis-tiled). |

## Command / effect testing
| Script | Status | Purpose |
|---|---|---|
| `capture-test.py` | ✅ | PRE/POST snapshot manager (the snap protocol in [commands/README](../commands/README.md)). Auto-registers each snap via `data-index.py`; pass `--note` for provenance. *(moved from root → tools/, 2026-05-29)* |
| `data-index.py` | ✅ | Provenance index for empirical captures (`add`/`scan`/`show`) → `traces/INDEX.tsv`. The fix for "what is this dump?" — see [traces/README](../traces/README.md). |
| `mesen-dump.py` | ✅ | Resolve/fetch Mesen Debugger output by convention (2026-05-30). `list` · `path [name] [--trace]` · `get [name] [--trace]` (copies to `traces/` + auto-registers via `data-index`). `.dmp`=SRAM, `.txt`=tracelog; no name → newest by mtime; `name` = case-insensitive substring. Removes the manual hunt for a freshly-saved battle dump. |
| `run-effect.py` | ✅ | Generic effect-handler runner (`grow`/`build`/`dam`/`give…`); captures host_call args (sqrt_int/math32/pct_op). **Absorbed `run-grow.py`, 2026-05-29** — use `run-effect.py grow <pre> [amount]`. ⚠️ **reverses captured args for display** — true frame order is the reverse (see probe-math32). |
| `probe-map-emit.py` | 🧪 | Harness w/ a working **syscall layer**: intercepts `host_call $F226`, reads the param struct from `vm_sp` (`struct[0]`=task id), dispatches — emulates `memcpy_banked`($10)/`rng`($11), stubs `wait_for_nmi`($13)/PPU, auto-stubs+logs unknowns. Runs `$885E` clean. **Finding: `$885E` is the staging→PPU *render* driver, not the ROM→SRAM populator** (task `$14`=ppu_blit reads `struct[12]` staging ptr → PPUDATA). Real emit still unfound — see ROADMAP step 2'. Promote the syscall layer into `nobunaga_vm` next. |
| `probe-math32.py` | ✅ | Probes the 3 VM arithmetic helpers (`math32_3arg`/`math32_2arg`/`pct_op`) in the emulator with controlled args. **Cracked the ext-op blocker 2026-05-29**: `pct_op=⌊ab/100⌋`, `math32_3arg=⌊ab/c⌋`, `math32_2arg=⌊100a/(a+b)⌋`. Self-documents its conclusions. |
| `probe-skill-impact.py` | ✅ | **Settles which direction the SKILL LEVEL (`const_two $6D63`) pushes a formula, by running Grow at skill 1–5 on one identical pre-state (2026-06-02).** `py -3 tools/probe-skill-impact.py [sram_pre] [amount]`. Overrides `$6D63`, runs `effect_grow $87F0`, reports the output-delta per skill + the captured `math32_3arg` args. **Result: gain 76/60/46/30/14 for skill 1/2/3/4/5 (amount 100) → higher skill = LESS development**, confirming `Grow gain = 2·⌊amount·(6−skill)/√(output+amount)⌋` and that `const_two` is the difficulty knob, not a constant. (Tail "opcode $00" is the harmless outermost-return artifact; state is already applied — same pattern as `run-effect.py`.) [[project_nobunaga_main_turn_loop]] |
| `probe-espionage.py` | ✅ | Probes the espionage magnitude subs via the probe-math32 `call_direct` pattern (pre-load `fp+0x0B/0x0D`, jump to body, stop at the sub's own RETURN, sweep rng seeds). **Ninja drain `$A255` EMULATOR-CONFIRMED:** `(rng%max(1,⌊field·√(skill+30)/100⌋)+1)×5`, bands matched `{5,…,X×5}`. **Bribe transfer `$8BC1` probed:** defection scales with target loyalty (≈½–⅘), reduced by a gold/RNG term — NOT a % of output. Also confirmed the CALL frame-arg mapping is **reverse push order** (last-pushed → `arg1`@`fp+0x0B`). **Surfaced + FIXED the rng-stub tech debt (2026-06-02):** `nobunaga_vm.py` `_syscall_dispatch` task $11 now returns the LCG value (`& 0x7FFF`) in regL instead of `None`, so `rng_mod` works — unblocks emulator-probing all RNG-driven formulas. |
| `probe-combat-arena.py` | ✅ | **Settles what `$9675` (`set_combat_arena_rect_by_approach`) does — overturning the old "4-quadrant damage formula" (2026-06-02).** Jumps into the body with `SP=FP-10` (5-local prologue) across attacker/defender fief pairs, reads `$6FF6-$6FFC` + regL. **Proved they're the tactical-arena BOUNDING RECT** (x_min/y_min/x_max/y_max), set to the attacker's entry strip by compass approach (E→[7,0,10,4], W→[0,0,3,4], N→[0,0,10,2], S→[0,2,10,4]); returns edge code {1=N,2=S,3=W,4=E}, NOT damage. Also caught a decompiler vertical-branch store-annotation swap. [[project_nobunaga_combat_engine_decoded]] |
| `probe-combat-casualty.py` | ✅ | **Pins the real combat casualty formula by running `resolve_attack_apply_casualties` ($9E73) on a real combat dump (2026-06-02).** Stops at the contest branch ($9F34 won / $9F37 lost) to read the magnitude (regL) without the UI-sub hang; sweeps attacker/defender morale + enemy strength + rng seeds. **Confirmed:** attack = a **capture/defection contest** — `atk(rng(6−skill)+morale+charisma) > def(morale+charisma+2·skill)` → `pct_op(math32_2arg(atk,atk−def), enemy_str)` men defect to the attacker's slot 0 (cap 9999); else enemy province morale −= skill. **Charisma dominates** (won every morale 40–120 in the Oda matchup). [[project_nobunaga_combat_engine_decoded]] |
| `probe-combat-bleed.py` | ✅ | **Closes the combat damage model — proves the famous "/30" is the RICE/SUPPLY clock, not damage (2026-06-02).** Cross-dump diff (startbattle→firstcontact): the per-side record `$6F7D+6*side = {+0 gold, +2 rice, +4 men}`; rice(+2) drains **men-proportionally** (side0 −12, side1 −8 = the 32:21 men ratio = men/30·days) while men(+4) and the `$6FBC` slots hold **exactly constant**. So `consume_daily_battle_rice` ($830B) drains rice at men/30/day → "you have no rice!" terminator = the rice-exhaustion/doughnut mechanic. The other two mechanisms: kills via `apply_pct_reduction_to_unit_strength` ($9D03, strength-ratio %), capture via `resolve_attack_apply_casualties` ($9E73). [[project_nobunaga_combat_engine_decoded]] |
| `probe-ai-war-target.py` | ✅ | **Pins the AI war-target SELECTION direction against the ROM, killing the reverse-push ambiguity (2026-06-03).** `py -3 tools/probe-ai-war-target.py`. Runs `fief_men_ratio_pct $939D` and `pick_weakest_men_fief $93B3` on a synthetic candidate list of known army sizes. **Results:** (1) the ratio numerator is arg-slot 1 (FP+0x0B) = the *second-displayed* arg in the decompiled C (display is reverse-pushed); (2) the picker returns the **FEWEST-men** (weakest) adjacent enemy in both orderings → the label `pick_fief_with_most_men` was BACKWARDS, renamed `pick_weakest_men_fief`. Confirms the AI attacks its weakest neighbour. [[project_nobunaga_main_turn_loop]] |
| `probe-ai-policy-distribution.py` | ✅ | **Save-state census proving the AI never picks a Grant style — it's always state 0 (2026-06-03).** `py -3 tools/probe-ai-policy-distribution.py`. Reads `province_ai_state $6CF7` (1 byte/fief) out of every `traces/*.dmp` and histograms it. **Result: every ordinary save = {Home:49, Direct:1}** — all 49 AI fiefs at 0 (Home), only the human's fief at 5 (Direct). States 1-4 appear ONLY where the player conquered+granted (scale with extra Direct fiefs). So the 6 Grant policies are a player affordance; the AI is monomorphic, running Home→`ai_econ_command_dispatch` (the Balanced RNG loop) on every fief. **Also censuses `$6DA2`** and proves the bijection (set-count == distinct-owner count in ALL real saves) → `$6DA2 = fief_is_daimyo_capital` (exactly one capital per living daimyo). [[project_nobunaga_ai_command_pass]] |
| `run-selector-full.py` | ⚠️ | Combat-selector ($9675) runner — provenance for the now-SUPERSEDED combat-damage-formula reading. The "damage" framing was wrong (see `probe-combat-arena.py`); kept only as history. |
| `daimyo-table.py` | ✅ | Extract daimyo personality/stat table. |
| `command-table.py` | ✅ | Walk lord-command driver pointer table at `$B9B2` → `command-table.txt`. |
| `driver-call-index.py` | ✅ | For each lord-command driver, list every host_call it reaches (EFFECT/SHARED/BANK15) — drives the per-command formula pass. Reads `disasm/bank_01_vm.asm`. *(moved from root → tools/, 2026-05-29)* |
| `cluster-anon-subs.py` | ✅ | **Anon-sub clusterer for the label-walk (2026-06-01).** `py -3 tools/cluster-anon-subs.py <bank> [--size N] [--by-callgraph] [--json]`. Reads `decompiled/bank_NN.c`, buckets every still-anonymous `sub_XXXX` into ~N-sized clusters (one cluster = one PROPOSER agent's batch). Default = address order; **`--by-callgraph` = LEAVES-FIRST** (call-graph depth via `native-call-index`, roots land in the last batches so they're read against already-named callees — the regen-between-batches fixpoint). The GENERALIZATION of the hand-coded `CLUSTERS` in the bank01-label-walk Workflow. Usually called via `label-walk-prep.py` now (which slices the first K + builds seeds). Deterministic. (bank_00 = 0 anon = DONE; bank_02 = 128 anon → 19 clusters.) ⚠️ leaves-first ordering only became *genuinely* call-graph-driven after the `native-call-index` CALL_abs fix below — before that the bytecode graph was empty and this fell back to ~address order. |
| `native-call-index.py` | ✅ | **Who-calls-whom + true-orphan finder (2026-05-31; CALL_abs FIX 2026-06-01).** The backward half of the labeling walk. `py -3 tools/native-call-index.py callers <addr>` · `calls <addr>` · `orphans [--bank N] [--named-only]` · `stats`. **Fuses edge sources** so it doesn't cry-wolf: native `jsr/jmp` (banks 0/1/2/15), **bytecode `CALL_abs`/`CALL_abs_imm1`** (the `*_vm.asm`), and the ROM dispatch tables (syscall `$C173`, VM-opcode `$F026/$F126`, command `$B9B2`) + hw vectors. Resolves "$C000+ ⇒ fixed bank 15". Imports `mesen-labels.py` for names. **⚠️ FIX 2026-06-01: the bytecode-edge regex matched only an obsolete `host_call` mnemonic (0 occurrences today) instead of the live `CALL_abs` form (2400+ edges) — so the ENTIRE bytecode call graph was missing and every bytecode sub reported "0 callers" (it bit every bank_00 label-walk batch).** Now `callers 8377` → `init_daimyo_arms_record ×4`, etc. Still blind to unresolved `jmp (ind)` / `CALLPTR` — those show as orphans, correctly flagging "needs indirect-edge resolution." |
| `label-walk-prep.py` | ✅ | **Next-batch assembler for the label-walk (2026-06-01).** `py -3 tools/label-walk-prep.py <bank> [--batch-clusters K] [--size N] [--landmarks "text"] [--json]`. Emits the next un-done batch's Workflow args: `{bank, clusters:[first K leaves-first], seeds}` where **seeds = every already-named sub in the bank auto-pulled from the toml** (the mechanical bulk) + optional `--landmarks` (the one bank-specific analytical bit, passed once). `--json` → pipe straight into the Workflow call; default = human summary + remaining-anon count (0 = bank done). Removes the per-batch hand-clustering + hand-seed-building. Deterministic. |
| `label-walk-apply.py` | ✅ | **Deterministic root-writer for the label-walk (2026-06-01).** `py -3 tools/label-walk-apply.py <bank> <workflow-output.json> [--commit] [--no-regen] [--dry-run]`. Consumes the Workflow's returned JSON (full task-output file OR `{bank,verified}`) and mechanically does the 4 between-batch steps: (1) write survivors to `[prg.bankN]` (verdict/confidence-flagged comments from the verifier's `summary`, REFUTED→`helper_<addr>`, dedup + collision guard); (2) regen (`mesen-labels.py --mlb --asm` + `decompile-all.py`); (3) **HARD GUARD (exit 2)**: only `bank_NN.c` may change (no cross-bank bleed) AND its diff must be pure renames; (4) `--commit`. The verifier IS the check (lateral@bytecode) so auto-writing its names is *more* faithful than a coordinator self-review — the guard is the only gate, now a hard assertion not eyeballing. [[feedback_verification_independence_is_altitude]] **Guard backported 2026-06-03 → now shares `walk_guard.py` with var-walk** (SNAPSHOT-vs-pre-regen, dirty-tree-robust; allow-set `{bank_NN.c, all_banks.c}` + commits `all_banks.c`). |
| `var-walk-prep.py` | ✅ | **Next-batch assembler for the var-walk (M4, 2026-06-03).** `py -3 tools/var-walk-prep.py <bank> [--batch N] [--name-match RX] [--name-exclude RX] [--only ADDRS] [--landmarks "text"] [--json]`. The variable-side twin of `label-walk-prep`. Parses `decompiled/bank_NN.c` for **NAMED** subs (anon → `/label-walk` first) that still carry positional slots (`arg1..arg4`/`localN`) and have no `[vars.*]` section yet; orders leaves-first (`cluster-anon-subs.callgraph_order`) and emits `{bank, subs:[{addr,name,slots}], seeds}` — `seeds` = the role-inference vocabulary (province fields, helper arg semantics, the reused-scratch conservatism rule). **`--name-match`/`--name-exclude` (regex on the sub NAME) = the "keystones only" lever; `--only 87F0,88A6` targets an explicit list.** `--json` pipes into the Workflow; default = human summary + remaining count (0 = bank's named subs all var-walked). Deterministic. The Workflow accepts `single:true` to fuse propose+verify into 1 agent/sub (token economy / rate-limit dodge). **`--far` mode (2026-06-03):** surfaces raw `*(word*)(fp - N)` spill locals as keys `fp-38` instead of positional slots, with **per-SLOT** done-tracking (`recorded_slots` — an already-swept sub re-surfaces for its un-named far slots); NEGATIVE offsets only (positives are args/header in this VM); adds the `FAR_SEEDS` role vocabulary; pair with `var-walk-apply --merge`. |
| `walk_guard.py` | ✅ | **Shared regen-guard for the `*-walk-apply.py` root-writers (2026-06-03).** `snapshot_decompiled(dir)` (take BEFORE regen) + `norm_lines(text)` (collapse identifiers → `ID`; also the three far-frame forms `*(word|byte*)(fp±N)`→`ID` / `(fp±N)`→`&ID` so a far-frame substitution reads as a pure rename) + `find_guard_violations(before, after, allowed)` → `(others, nonrename)`, both empty == clean. One artifact so label-walk + var-walk guards can't DRIFT apart ([[feedback_drift_is_rederivation]]). Not a CLI — imported by both apply scripts. |
| `var-walk-apply.py` | ✅ | **Deterministic root-writer for the var-walk (M4, 2026-06-03).** `py -3 tools/var-walk-apply.py <bank> <workflow-output.json> [--commit] [--no-regen] [--dry-run]`. Writes per-sub `[vars.bankN."0xADDR"]` sections (CONFIRMED/AMENDED slots; REFUTED → a `# left positional` note so the sub still counts processed; per-sub collision + dup-name guard), regens (`decompile-all.py` only — frame-slot names aren't in `.mlb`/`.asm`), then **HARD GUARD (exit 2)** via the shared `walk_guard.py`: a **SNAPSHOT-vs-pre-regen** compare (robust to a dirty tree, unlike git-diff-vs-HEAD) asserting ONLY `bank_NN.c` + `all_banks.c` changed and each is a pure identifier rename; then `--commit`. **Far-frame slots (2026-06-03):** SLOT_RE + writer accept quoted far keys `"fp-38"` (spill locals beyond the 12 standard slots). **`--merge` mode:** per-SLOT collision instead of per-sub refuse — splices new slot lines into a sub's EXISTING `[vars.*]` section (or creates one if absent); `section_span`/`existing_slot_keys` parse named + left-positional slots; processes high-addr-first so a splice can't shift a pending section's span. Pair with `var-walk-prep --far` for the far-frame sweep. |

## SRAM / combat decode
| Script | Status | Purpose |
|---|---|---|
| `analyze-sram.py` | ✅ | Analyze an 8 KB SRAM `.dmp`, 2 modes: `structure` (byte distribution / runs / page summaries) and `detail` (ASCII strings / record stride-fitting / region dumps). **Merged sram-analyze + sram-detail, 2026-05-29** — verified byte-identical. |
| `sram-decode-province.py` | ✅ | Decode the live 17×26 province table from an SRAM dump; `--diff <other>` for PRE/POST. The decoder `capture-test.py diff` calls. *(moved from root → tools/, 2026-05-29)* |
| `combat-trace-decode.py` | ✅ | Decode the large `*-combat-init.txt` traces. |
| `sram-decode.py` | 🧪 | Discovery one-shot: located the `$7BFE` tactical staging buffer + metatile patterns. **Its input `sram_dump.txt` no longer exists; finding is canonical in `render-from-sram.py`.** Kept for provenance only. |
| `combat-map-decode.py` | 🧪 | Discovery one-shot: reconstructed the tactical nametable from a Mesen *trace* (not SRAM — wrong cluster). Superseded by `combat-trace-decode.py`. Hardcoded trace path. Provenance only. |

## Rendering
| Script | Status | Purpose |
|---|---|---|
| `render-atlas.py` | ⚠️🧪 | **INVALIDATED — produces WRONG maps.** Reads bank-4 `$A57E` as a "cell-index table," but that hypothesis was disproven (2026-05-21) and re-verified 2026-05-29: its Mino = `{town:18, forest:35}` vs ground-truth `15 clear/26 forest/12 mountain`. Tactical maps are **VM-bytecode-emitted at battle init**, not a static table. Use `render-fief-from-ppu.py` for accurate maps. Kept as provenance for the dead-end. |
| `render-from-sram.py` | ✅ | Tactical map from SRAM `$7BFD` staging buffer. Now also a single-file CLI: `render-from-sram.py <dump> [base_hex] [out] [label]` (no args = render the captured-fief set). Exposes `render()` for import (used by `render-rom-to-map.py`). |
| `render-portrait.py` | ✅ | **Daimyo portrait renderer — PRESET anthology + COMPOSITE inspector (2026-06-01).** `py -3 tools/render-portrait.py anthology [17\|50]` → `atlas/daimyo-anthology-{17,50}.png`: a labeled grid of every scenario daimyo's PRESET (historical, hand-drawn) portrait, rendered NES-exact (48×48 native, real `$F7CC` palette `0x3E/0x38/0x28/0x30`). Preset path CRACKED 2026-06-01: descriptor table **bank 8 `$BBD0`** (4 B/entry `{chr_bank, tile_count, src_ptr}`, mapid-indexed) → CHR packed in **banks 7-8** → **per-portrait** 6×6 tile-index map at **bank 8 `$B144` + mapid×36** (tile base `0x5B`; the bytecode at $E7E4 computes `fief_to_mapid*36+$B144`); names bank 3 `$97AB`(50)/`$B7AB`(17), 9 B/entry; mapid = identity (50-fief) / `province_to_mapid_table $F70E` (17-fief). VISUALLY VALIDATED (recognizable faces; 17- & 50-fief Oda both → descriptor[24]). `py -3 … random [seed]` → `atlas/daimyo-random-36.png`: 36 RNG-generated daimyo via the **COMPOSITE / "dead-daimyo" succession path** — base-5 face code → 4 feature digits (head/eyes/nose/mouth), CHR from bank-8 tables ($A604/$A9C4/$ABA4/$AD84), **6×6 tile-index map from BANK 9** ($AE34/$AE70/$AE8E/$AEAC + digit×len; CHR base tile `0x5B`/`0x67`/`0x6D`/`0x73`), PLUS a generated name (`generate_daimyo_name $DCB2`: syllable1 `$F930` + syllable2 `$F95D`, each `rng_mod(11)`, dup-rejected). `sheet`/`faces` = raw composite feature-tile inspection. [[project_nobunaga_daimyo_portraits]] |
| `build-command-page.py` | ✅ | **Command-page generator + rabbit-hole detector (2026-06-02).** `py -3 tools/build-command-page.py build <cmd>\|all` → `commands/<cmd>.html` (the rich walkthrough, GIF base64-embedded = single file). Two templates: **atomic** (formula commands — driver flow + bytecode formula + validation table + animation) vs **functional** (subsystem entries — thin page + "rabbit hole" links). `classify <cmd>\|all` / **`plan`** = the RABBIT-HOLE DETECTOR: reads `driver-call-index` EFFECT calls, flags a command as ATOMIC (1 known formula handler) vs RABBIT-HOLE (reaches combat/move/etc.) vs turn-skip — so we never author a formula page for War/Bribe. `animid <driver_hex>` extracts the `$E80C` arg = animation id. Per-command content lives in the `COMMANDS` registry; fill as each command is walked. Pairs with `run-animation.py`. [[project_nobunaga_animation_vm]] |
| `build-turn-loop.py` | ✅ | **Turn-loop hub generator — the project SPINE map (2026-06-02).** `py -3 tools/build-turn-loop.py [--print]` → `turn-loop.html`: the main game loop (`vm_bootstrap $A778`) rendered as a status-tracked map of its 7 phases (init / planner tick / uprising / combat / shuffle / command-pass / game-over). Status badges (`inline` dived · `page` has-deep-dive · `stub` to-do) double as the remaining-work tracker. The command palette (`commands/index.html`) and the future combat page are its two branches. **Reuses `build-command-page.py`'s CSS + `_page` via importlib (single style source).** Source-of-truth = the `NODES`/`SEASONS`/`KEYSTONES` registries in the script (edit + regen; never hand-edit the HTML). [[project_nobunaga_main_turn_loop]] |
| `build-ai-overview.py` | ✅ | **Consolidated AI overview generator (2026-06-03).** `py -3 tools/build-ai-overview.py` → `ai.html`: a HUB page telling how the enemy AI plays end-to-end in **four acts** (Plan→planner / Act→command / War→summarized + 2 closed threads / Fight→combat), each linking INTO the existing `turn-loop-*` deep-dives (no content duplication, Chris's call). Reuses `build-turn-loop.py`'s `HUB_CSS` + the command-page shell via importlib. Source-of-truth = the `SECTIONS`/`FINDINGS` registries in the script. [[project_nobunaga_ai_command_pass]] |
| `run-animation.py <id>` | ✅ | **Animation extractor — pull any command/event animation from ROM, headless, no capture (2026-06-02).** `py -3 tools/run-animation.py <id> [--sram X.dmp] [--out atlas/anim] [--pt 0\|1] [--h 8\|16] [--scale N] [--trace]` → `atlas/anim-<id>.gif` + `-frames.png`; `--list` dumps the descriptor table. Drives the bank-14 **animation scripting VM** (`$80AD`, descriptor table `$AF80` 5 B/entry `{bank, data_src, command_stream}`, 22-opcode interpreter) in `nobunaga_vm`, intercepts the graphics syscalls (`ppu_upload_block_wrap`=CHR, `set_sprite`=OAM `$0600`, `palette_write`=`$0700`), snapshots a frame at each present point, renders sprites with the real NES palette. **id 9 = Grow** (peasant-farming). ⚠️ upload `count` is in TILES (×16). Non-develop animations may reference CHR the prior view-screen pre-loaded (not yet pre-seeded). The ANIMATED half of the "render graphics bits from code" frontier. [[project_nobunaga_animation_vm]] |
| `render-text.py "TEXT"` | ✅ | **Render arbitrary text in the game's OWN built-in font, pulled from ROM (2026-06-02).** `py -3 tools/render-text.py "NOBUNAGA" [--out f.png] [--scale N] [--fg HEX] [--bg HEX] [--tracking N]`; `--atlas atlas/font-glyphs.png` dumps the full glyph set. Importable: `from render_text import render_text`. **The font is bank-0 `$B2BA`** (2bpp CHR, uploaded to CHR-RAM by `render_boot_title_screens $88AC` just before the first menu text). **Char→tile = `char_classify $CDCA`**, piecewise (the font omits some punctuation between ASCII runs): digits `c−40`, upper `c−43`, lower `c−47`, named punctuation explicit. Text pipeline decoded: `format_string $CFFC` → `redraw_window $CEC9` → `char_classify`. Used by `build-command-page.py index` to render the command numbers/names in-engine. The TEXT half of "render graphics bits from code." |
| `render-rom-atlas.py` | ✅ | **Whole 50-fief (or 17-fief) tactical atlas from ROM in one pass** (2026-05-30). Loops every province through `render-rom-to-map`'s pipeline → PNG per fief in `atlas/rom-50/` + a terrain-distribution table + anomaly flags. `--scenario 17\|50`, `--count N`. **Cross-validates:** every ROM-rendered map reproduces the matching hand-captured fief's terrain counts exactly. |
| `render-rom-to-map.py <province>` | ✅ | **Tactical map straight from ROM — NO capture needed** (2026-05-30). Seeds province `$6F63` + scenario `$6D9D` into a blank SRAM, runs the populate sub `$8903`, hands the populated `$7BFD` to `render-from-sram`'s decoder. Verified byte-identical to the captured Mino render. `--scenario 17\|50`, `--sram <dump>` (read province/scenario from a dump, render from ROM = validation mode). Warns if a TODO syscall fires (likely a new 50-fief path). |
| `render-strategic-map.py` | ✅ | **The ILLUSTRATED strategic map — the cartridge's own art, from-ROM, no emulation (2026-06-02).** Decodes bank-4 section tilemaps (`$8D5C`, 28×16 tiles = 448 B/section × 9), per-quad attributes (`$9D1C`, 32 B/section), map CHR (tile 91 → bank4 `$845C`), and palette (bank-15 `$F67A`); fief-number labels (tilemap cells ≤ 80) render via the game font (`render-text.py`). The Map command flips between sections as **pages** (`map_helper_af10`: "Arrows: other sections"). **17-fief = 2 overlapping sections** (verified offset −16,+3) → `--stitch` auto-aligns them into one continuous map (`strategic-map-17-stitched.png`); **50-fief = 9 pages** (all fiefs 1–50) → contact sheet (`strategic-map-50.png`). Usage: `--scenario 17\|50`, `--stitch`, `--section N`, `--scale K`. **This is the game's actual map art** — distinct from the schematic adjacency atlases below. |
| `render-strategic-atlas.py` | ✅ | 17-fief: composites the 17 tactical-map PNGs at geographic positions + adjacency overlay. *Not a `-50` twin — see note below. Note: this is the SCHEMATIC analytical graph, not the game art — that's `render-strategic-map.py` above.* |
| `render-strategic-50.py` | ✅ | 50-fief: node-link graph of the `$8004` adjacency table at approx-Japan positions (for eyeball verification). **Different render, different artifact** from the 17 atlas — `--variant` merge rejected 2026-05-29 (pure churn; both are fixed-output references). |
| `render-fief-from-ppu.py <fief>` | ✅ | Parameterized fief PPU renderer (`--list` shows available dumps). **Replaced the 16 per-fief `render-<fief>-from-ppu.py` copies, 2026-05-29.** |
| `render-mino-full.py`, `render-iga-full.py` | 🔁 | Full-map-from-SRAM variants → generalize (still pending). |
| `title-render.py` | ✅🧪 | Title-screen renderer, ROM-sourced CHR (`$AC4A`) → grayscale PPM. Canonical of the title one-shots; **`render-title2.py` retired 2026-05-29** (placeholder palette, depended on extracted intermediates). |
| `title-nametable.py`, `title-tiles-extract.py`, `find-title-assets.py`, `extract-chr-from-trace.py` | 🧪 | Title-screen one-shots (asset located + rendered; keep for provenance). |

## Strategic / scenario analysis
| Script | Status | Purpose |
|---|---|---|
| `adjacency.py` | ✅ | 17-fief adjacency from bank-4 `$8300` → `adjacency.txt`. |
| `adjacency-50.py` | ✅ | 50-fief adjacency from bank-4 `$8004` → `adjacency-50.txt` (+ names, degree stats, historical sanity checks). **Different table + artifact** from the 17 dumper — `--variant` merge rejected 2026-05-29 (different province numbering, output format; tables fixed & located → zero maintenance surface). |
| `find-adjacency-50.py` | 🧪 | Located the 50-fief table offset (done). |
| `fief-analysis-50.py` | ✅ | 50-fief **bulk** table: per-fief aggregates (Econ/Army/Daimyo geomeans) + neighbor threat/prey/ignition/backfield for all 50. |
| `profile-fief-50.py` | ✅ | 50-fief **drill-down**: full play-profile for named fief(s) (`py profile-fief-50.py Ezo Noto …`). Distinct granularity from the bulk table — both canonical, not duplicates. |
| `verify-17-subset-of-50.py` | 🧪 | Confirmed 17⊂50 (done). |
| `tier-analysis.py` | ✅ | Fief tier heuristic. |

## Lua (Mesen 2 runtime)
| Script | Status | Purpose |
|---|---|---|
| `dispatch-logger.lua` | ✅ | Log bytecode dispatch at `$E867` (console v1 / CSV v2). |
| `dashboard.lua` | ✅ | Live econ/army overlay during play. |
| `ai-war-logger.lua` | ✅ | Empirical AI war-target measurement (hooks `$B79B`). |
| `vm-trace.lua` | 🔁 | Lower-level VM trace; overlaps `dispatch-logger`. |
| `test-overlay.lua` | 🧪 | Diagnostic overlay. |

## Discovery / one-shots (🧪 — keep for provenance, don't extend)
`find-cell-table.py` · `find-table-refs.py` · `find-market.py` · `find-mikawa.py` · `find-tokugawa.py` · `find-tokugawa2.py` · `find-rom-defaults.py` · `dump-rom-defaults.py` · `dump-defaults.py` · `locate-tables.py` · `secondary-tables.py` · `map-17fief.py` · `map-bank3.py` · `paste-analyze.py` · `paste-full.py` · `zp-harvest.py` · `ui-strings.py`
> These located a specific table/value once. Their *output* (in `mesen-labels.toml` / the `.txt` data files) is the artifact; the scripts are provenance.

## Tests
`test-cpu6502.py` ✅ · `test-dispatcher.py` ✅ · `test-vm-cfg.py` ✅ (non-vacuousness of the CFG equivalence relation — injects a misroute, asserts it's REJECTED) · `test-loadl-quick.py` 🧪 · `debug-trace.py` 🧪

## Reference
| File | Purpose |
|---|---|
| `sea16-reference.c` | NovaSquirrel's Sea-16 VM interpreter (external reference). |
| `EXTERNAL.md` | Pointer to external `nes-disasm6` tool. |
| `vm-opcodes-v2.toml` | ✅ Canonical opcode spec (no v1 exists). |
| `presets/*.toml` | Mesen 2 debug-session presets (audio-trace, brk-trace). |

## ⚠️ Known gaps
- ~~**`mesen-labels.py` is referenced by `mesen-labels.toml` but does not exist.**~~ — **BUILT 2026-05-31** (see Core VM / labeling table). The projector is real: toml → `.mlb` + named disasm, with anchor-insertion for indirect-only handlers. Labels are no longer applied manually.
- ~~**`vm-disasm.py` (bulk VM disassembler) is missing**~~ — **RECOVERED 2026-05-30** (see Core VM table). Rebuilt on execution-validated lengths, validated by exact-tiling on all 4 known banks (0 misaligned / 495 subs). Recovery also surfaced two findings: (1) the old `disasm/*_vm.asm` had **wrong operand lengths** for ~10 opcodes ($84/$86/$88, $A0-$A3, $AA, $AF, $DE/$DF) — partially hand-corrected, never finished; the new tool is correct. (2) **banks 03-14 are almost entirely DATA** (0 bytecode stubs except bank 10 = 1, bank 14 = 3); all ~495 bytecode subs live in banks 0/1/2/15 — which are already dumped. This shrinks the "see the whole picture" epic dramatically. See [ROADMAP](../ROADMAP.md).
- Consolidation pass **complete 2026-05-29**: opcode trio → `analyze-vm-opcodes.py`; sram pair → `analyze-sram.py`; 4 selector iterations + `run-grow.py` + `render-title2.py` retired. The remaining 🔎 re-confirms resolved (the 17/50 "twins" and the two `*-fief-50` profilers are parallel canonical tools, not collapse targets — see their rows). Only `render-mino/iga-full` generalization still pending.
