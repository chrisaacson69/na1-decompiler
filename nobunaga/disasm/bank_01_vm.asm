VM bytecode disassembly
  ROM:          Nobunaga's Ambition (USA).nes  (262144 bytes after header)
  mode:         bulk dump of bank 1
  opcode spec:  vm-opcodes.toml (256 of 256 opcodes defined)
  labels:       227 CPU addresses named

  bank 1: found 131 bytecode-subroutine stubs


; ============================================================
; sub $8003   (frame_off=+0, body @ $8008)
; ============================================================
  $8008  8E 10 BA                    push_imm_word            $BA10                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $800B  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $800F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8010  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8013  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8014  D8 21 80                    branch_z_abs             $8021                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8017  8E FD B9                    push_imm_word            $B9FD                     ; "Attack"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $801A  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $801E  D6 44 80                    jump_abs                 $8044                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8021  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8022  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8023  8C 8A F9                    loadB_imm_word           $F98A                     ; "\nno fiefs"  | regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8026  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8027  D8 30 80                    branch_z_abs             $8030                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $802A  8A 11 BA                    loadA_imm_word           $BA11                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $802D  D6 33 80                    jump_abs                 $8033                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8030  8A 94 F9                    loadA_imm_word           $F994                     ; "  Fiefs "  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
 >$8033  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8034  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8035  D1                          decA                                               ; regA -= 1
  $8036  D2                          aslA                                               ; regA <<= 1
  $8037  8C 16 F8                    loadB_imm_word           $F816                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $803A  BB                          add                                                ; regA = regA + regB
  $803B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $803C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $803D  8E F6 B9                    push_imm_word            $B9F6                     ; "%s%s%s"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8040  E9 34 D1 08                 host_call                $D134 (ui_helper_d134) {bytecode}, $08 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$8044  8E 12 BA                    push_imm_word            $BA12                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8047  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $804B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $804C (province_select_prompt)   (frame_off=-8, body @ $8051)
; ============================================================
  $8051  A4 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8054  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8055  A4 CF 7F                    loadA_mem_word           $7FCF                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8058  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
 >$8059  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $805A  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $805B  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $805F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8060  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8061  AA 9D 6D 61                 op_AA_wb                 $6D9D, $61
  $8065  E9 9D D5 04                 host_call                $D59D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8069  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $806A  D7 88 80                    branch_nz_abs            $8088                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $806D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $806E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$806F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8070  D1                          decA                                               ; regA -= 1
  $8071  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8072  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8073  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8074  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8075  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8076  D8 85 80                    branch_z_abs             $8085                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8079  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $807A  D1                          decA                                               ; regA -= 1
  $807B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $807C  E9 99 D9 02                 host_call                $D999 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8080  D7 85 80                    branch_nz_abs            $8085                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8083  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8084  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$8085  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8086  D0                          incA                                               ; regA += 1
  $8087  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8088  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8089  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $808A  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $808D  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $808E  D7 6F 80                    branch_nz_abs            $806F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8091  D6 59 80                    jump_abs                 $8059                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)

; ============================================================
; sub $8094   (frame_off=-2, body @ $8099)
; ============================================================
  $8099  8D 14                       op_8D_sbyte              +20
  $809B  8D 14                       op_8D_sbyte              +20
  $809D  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80A1  8E 14 BA                    push_imm_word            $BA14                     ; "__Market__"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $80A4  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80A8  8D 15                       op_8D_sbyte              +21
  $80AA  8D 14                       op_8D_sbyte              +20
  $80AC  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80B0  A4 0B 6E                    loadA_mem_word           $6E0B                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $80B3  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $80B4  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80B5  B9                          mod_signed                                         ; regA = regA % regB  (signed remainder)
  $80B6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80B7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $80B8  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80B9  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $80BA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80BB  8E 1F BA                    push_imm_word            $BA1F                     ; "Rate  %2d.%1d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $80BE  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80C2  8D 16                       op_8D_sbyte              +22
  $80C4  8D 14                       op_8D_sbyte              +20
  $80C6  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80CA  A4 0D 6E                    loadA_mem_word           $6E0D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $80CD  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $80CE  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80CF  B9                          mod_signed                                         ; regA = regA % regB  (signed remainder)
  $80D0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80D1  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $80D2  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80D3  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $80D4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80D5  8E 2D BA                    push_imm_word            $BA2D                     ; "Rice  %2d.%1d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $80D8  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80DC  8D 17                       op_8D_sbyte              +23
  $80DE  8D 14                       op_8D_sbyte              +20
  $80E0  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80E4  A4 0F 6E                    loadA_mem_word           $6E0F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $80E7  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $80E8  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80E9  B9                          mod_signed                                         ; regA = regA % regB  (signed remainder)
  $80EA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80EB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $80EC  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $80ED  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $80EE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $80EF  8E 3B BA                    push_imm_word            $BA3B                     ; "Arms  %2d.%1d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $80F2  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80F6  8D 18                       op_8D_sbyte              +24
  $80F8  8D 14                       op_8D_sbyte              +20
  $80FA  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $80FE  A4 11 6E                    loadA_mem_word           $6E11                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8101  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8102  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8103  B9                          mod_signed                                         ; regA = regA % regB  (signed remainder)
  $8104  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8105  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8106  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8107  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8108  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8109  8E 49 BA                    push_imm_word            $BA49                     ; "Men   %2d.%1d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $810C  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8110  8D 19                       op_8D_sbyte              +25
  $8112  8D 14                       op_8D_sbyte              +20
  $8114  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8118  A4 13 6E                    loadA_mem_word           $6E13                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $811B  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $811C  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $811D  B9                          mod_signed                                         ; regA = regA % regB  (signed remainder)
  $811E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $811F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8120  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8121  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8122  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8123  8E 57 BA                    push_imm_word            $BA57                     ; "Ninja %2d.%1d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8126  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $812A  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $812B  A8 F9 7B                    loadA_mem_word           $7BF9                     ; regA = word read using <word> as address (handler $EA75)
  $812E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $812F   (frame_off=-2, body @ $8134)
; ============================================================
  $8134  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8135  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $8136  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8137  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8138  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $8139  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $813A  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $813B  81 02                       op_81_byte               $02
  $813D  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $813E  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $813F  81 04                       op_81_byte               $04
  $8141  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8142  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8143  81 06                       op_81_byte               $06
  $8145  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8146  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $8147  81 08                       op_81_byte               $08
  $8149  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $814A  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $814B  81 0F                       op_81_byte               $0F
  $814D  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $814E  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $814F  81 13                       op_81_byte               $13
  $8151  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8152  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8153  81 FF                       op_81_byte               $FF
  $8155  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8156  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $8157  81 64                       op_81_byte               $64
  $8159  81 3D                       op_81_byte               $3D
  $815B  E9 B7 D9 02                 host_call                $D9B7 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $815F  D6 63 81                    jump_abs                 $8163                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
  ; ---- gap $8162-$8162 (1 bytes, not on any traced path) ----
 >$8163  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8164  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8165  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

  ; ---- gap $8166-$8171 (12 bytes, not on any traced path) ----

; ============================================================
; sub $8172   (frame_off=-4, body @ $8177)
; ============================================================
  $8177  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8178  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8179  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $817A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $817B  D6 EE 81                    jump_abs                 $81EE                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$817E  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $817F  D1                          decA                                               ; regA -= 1
  $8180  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8181  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8182  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8183  E9 2F 81 04                 host_call                $812F {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8187  D8 EE 81                    branch_z_abs             $81EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $818A  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $818B  D1                          decA                                               ; regA -= 1
  $818C  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $818D  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8190  BB                          add                                                ; regA = regA + regB
  $8191  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8192  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8195  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $8196  D8 EE 81                    branch_z_abs             $81EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8199  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $819A  D0                          incA                                               ; regA += 1
  $819B  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $819C  0E                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $819D  D8 EE 81                    branch_z_abs             $81EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $81A0  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $81A1  D1                          decA                                               ; regA -= 1
  $81A2  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $81A3  D0                          incA                                               ; regA += 1
  $81A4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $81A5  8E 65 BA                    push_imm_word            $BA65                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $81A8  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $81AC  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $81AD  D0                          incA                                               ; regA += 1
  $81AE  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $81AF  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $81B0  8B 12                       loadB_imm_byte           +18                       ; regB = byte literal (handler $EABE)
  $81B2  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $81B3  D8 C7 81                    branch_z_abs             $81C7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $81B6  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $81B7  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $81B8  BA                          mod_unsigned                                       ; regA = regA % regB  (unsigned remainder)
  $81B9  D7 C0 81                    branch_nz_abs            $81C0                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $81BC  4A                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $81BD  D6 C2 81                    jump_abs                 $81C2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$81C0  89 20                       loadA_imm_sbyte          +32                       ; regA = sign-extended signed byte literal (handler $EAAF)
 >$81C2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $81C3  E9 81 CE 02                 host_call                $CE81 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$81C7  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $81C8  8B 12                       loadB_imm_byte           +18                       ; regB = byte literal (handler $EABE)
  $81CA  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $81CB  D8 EE 81                    branch_z_abs             $81EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $81CE  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $81CF  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $81D0  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $81D3  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $81D4  D8 EE 81                    branch_z_abs             $81EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $81D7  AC 09 D6                    host_call_simple         $D609 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $81DA  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $81DB  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $81DC  D8 E4 81                    branch_z_abs             $81E4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $81DF  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $81E0  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $81E1  D6 FA 81                    jump_abs                 $81FA                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$81E4  8E 69 BA                    push_imm_word            $BA69                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $81E7  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $81E8  E9 03 80 04                 host_call                $8003 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $81EC  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $81ED  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$81EE  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $81EF  D0                          incA                                               ; regA += 1
  $81F0  2D                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $81F1  D1                          decA                                               ; regA -= 1
  $81F2  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $81F3  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $81F6  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $81F7  D7 7E 81                    branch_nz_abs            $817E                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$81FA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $81FB  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $81FC (effect_war_a)   (frame_off=-4, body @ $8201)
; ============================================================
  $8201  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8204  A6 63 6F                    loadB_mem_word           $6F63                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8207  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8208  D8 17 82                    branch_z_abs             $8217                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $820B  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $820E  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $8210  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8211  A6 63 6F                    loadB_mem_word           $6F63                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8214  D6 20 82                    jump_abs                 $8220                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8217  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $821A  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $821C  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $821D  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
 >$8220  BB                          add                                                ; regA = regA + regB
  $8221  8C 93 61                    loadB_imm_word           $6193                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8224  BB                          add                                                ; regA = regA + regB
  $8225  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8226  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8227  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8228  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $822B  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $822C  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $8230  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8231  D7 02 2A                    branch_nz_abs            $2A02                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8234  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8235  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $8236  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8237  D8 42 82                    branch_z_abs             $8242                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $823A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $823B  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $823D  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $823E  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $823F  D6 47 82                    jump_abs                 $8247                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8242  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8243  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $8245  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8246  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
 >$8247  BB                          add                                                ; regA = regA + regB
  $8248  8C 93 61                    loadB_imm_word           $6193                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $824B  BB                          add                                                ; regA = regA + regB
  $824C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $824D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $824E  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $824F  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8250   (frame_off=+0, body @ $8255)
; ============================================================
  $8255  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8256  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8257  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8258  1E                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8259  DC                          bitxor                                             ; regA = regA ^ regB
  $825A  D8 65 82                    branch_z_abs             $8265                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $825D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $825E  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $8260  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8261  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8262  D6 6A 82                    jump_abs                 $826A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8265  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8266  8B 36                       loadB_imm_byte           +54                       ; regB = byte literal (handler $EABE)
  $8268  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8269  1C                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
 >$826A  BB                          add                                                ; regA = regA + regB
  $826B  8C 93 61                    loadB_imm_word           $6193                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $826E  BB                          add                                                ; regA = regA + regB
  $826F  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8270  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8271   (frame_off=+0, body @ $8276)
; ============================================================
  $8276  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8277  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8278  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $827C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $827D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $827E  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8282  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8283  E9 50 82 06                 host_call                $8250 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8287  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8288  8D 64                       op_8D_sbyte              +100
  $828A  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $828E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $828F  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8290  D7 AA 82                    branch_nz_abs            $82AA                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8293  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8294  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8295  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8296  E9 50 82 06                 host_call                $8250 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $829A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $829B  8D 64                       op_8D_sbyte              +100
  $829D  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $82A1  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $82A2  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $82A3  D7 AA 82                    branch_nz_abs            $82AA                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $82A6  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $82A7  D6 AB 82                    jump_abs                 $82AB                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$82AA  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$82AB  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $82AC (helper_82AC)   (frame_off=+0, body @ $82B1)
; ============================================================
  $82B1  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $82B2  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82B3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $82B4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $82B5  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $82B8  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $82BA  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $82BB  8C 19 70                    loadB_imm_word           $7019                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $82BE  BB                          add                                                ; regA = regA + regB
  $82BF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $82C0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $82C1  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $82C5  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $82C6  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $82C7  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82C8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $82C9  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $82CA  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $82CB  D8 D2 82                    branch_z_abs             $82D2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $82CE  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $82CF  D6 D4 82                    jump_abs                 $82D4                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$82D2  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82D3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
 >$82D4  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $82D5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $82D6 (effect_tax)   (frame_off=-4, body @ $82DB)
; ============================================================
  $82DB  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82DC  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $82DD  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $82DE  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $82DF  D8 EA 82                    branch_z_abs             $82EA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $82E2  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $82E3  E9 4C CB 02                 host_call                $CB4C {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $82E7  D6 EB 82                    jump_abs                 $82EB                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$82EA  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
 >$82EB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $82EC  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $82ED  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $82F1  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $82F2  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $82F3  D8 FC 82                    branch_z_abs             $82FC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $82F6  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82F7  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $82F8  BC                          sub                                                ; regA = regA - regB
  $82F9  D6 00 83                    jump_abs                 $8300                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$82FC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $82FD  CD                          swap_AB                                            ; regA <-> regB
  $82FE  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $82FF  BB                          add                                                ; regA = regA + regB
 >$8300  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8301  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8302  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8303   (frame_off=+0, body @ $8308)
; ============================================================
  $8308  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8309  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $830B  B7 14                       ext_op                   $14                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $830D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $830E  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8310  B7 15                       ext_op                   $15                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8312  B7 01                       ext_op                   $01                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8314  B7 19                       ext_op                   $19                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8316  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8317  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8318  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8319  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $831A  B7 03                       ext_op                   $03                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $831C  B7 19                       ext_op                   $19                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $831E  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $831F  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8320  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8321  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8322  B7 02                       ext_op                   $02                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8324  B7 27                       ext_op                   $27                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8326  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8327   (frame_off=+0, body @ $832C)
; ============================================================
  $832C  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $832D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $832E  E9 03 83 04                 host_call                $8303 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8332  1E                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8333  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $8334  D8 40 83                    branch_z_abs             $8340                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8337  B7 18                       ext_op                   $18                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8339  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $833A  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $833B  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $833C  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $833D  D6 54 83                    jump_abs                 $8354                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8340  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8341  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8343  B7 14                       ext_op                   $14                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8345  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8346  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8348  B7 15                       ext_op                   $15                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $834A  B7 01                       ext_op                   $01                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $834C  B7 19                       ext_op                   $19                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $834E  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $834F  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8350  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8351  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8352  B7 02                       ext_op                   $02                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
 >$8354  B7 27                       ext_op                   $27                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8356  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8357 (helper_8357)   (frame_off=+0, body @ $835C)
; ============================================================
  $835C  3E                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $835D  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $835E  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8360  B7 14                       ext_op                   $14                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8362  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8363  B7 25                       ext_op                   $25                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8365  B7 19                       ext_op                   $19                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8367  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8368  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8369  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $836A  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $836B  B7 01                       ext_op                   $01                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $836D  B7 15                       ext_op                   $15                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $836F  B7 02                       ext_op                   $02                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8371  B7 27                       ext_op                   $27                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $8373  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8374  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8378  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8379   (frame_off=-2, body @ $837E)
; ============================================================
  $837E  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8381  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8382  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8383  A8 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word read using <word> as address (handler $EA75)
  $8386  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8387  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8388  E9 40 D9 04                 host_call                $D940 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $838C  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $838D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $838E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $838F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8390  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8391  BC                          sub                                                ; regA = regA - regB
  $8392  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8393  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8394  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8395  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $8396  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $8397  D8 9D 83                    branch_z_abs             $839D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $839A  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $839B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $839C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$839D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $839E  A8 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word read using <word> as address (handler $EA75)
  $83A1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $83A2   (frame_off=+0, body @ $83A7)
; ============================================================
  $83A7  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $83A8  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $83AB  BB                          add                                                ; regA = regA + regB
  $83AC  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $83AD  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $83B0  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $83B1  D8 BF 83                    branch_z_abs             $83BF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $83B4  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $83B5  8E 6B BA                    push_imm_word            $BA6B                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $83B8  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $83BC  D6 C6 83                    jump_abs                 $83C6                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$83BF  8E 6F BA                    push_imm_word            $BA6F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $83C2  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$83C6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $83C7 (helper_83C7)   (frame_off=+0, body @ $83CC)
; ============================================================
  $83CC  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $83CD  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $83D1  A2 83                       op_A0_A3_byte            $83                       ; 1-byte operand; indexed load/store via ptr0 (handlers $EAED-$EB1E)
  $83D3  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $83D4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $83D5   (frame_off=+0, body @ $83DA)
; ============================================================
  $83DA  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $83DB  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $83DE  BB                          add                                                ; regA = regA + regB
  $83DF  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $83E0  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $83E3  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $83E4  D8 F2 83                    branch_z_abs             $83F2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $83E7  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $83E8  8E 74 BA                    push_imm_word            $BA74                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $83EB  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $83EF  D6 F9 83                    jump_abs                 $83F9                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$83F2  8E 78 BA                    push_imm_word            $BA78                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $83F5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$83F9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $83FA (effect_view_a)   (frame_off=-12, body @ $83FF)
; ============================================================
  $83FF  AC 20 CD                    host_call_simple         $CD20 (ui_helper_cd20) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8402  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8403  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8407  8D 23                       op_8D_sbyte              +35
  $8409  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $840A  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $840E  8D 25                       op_8D_sbyte              +37
  $8410  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $8411  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8415  8D 2B                       op_8D_sbyte              +43
  $8417  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $8418  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $841C  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $841D  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $841E  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $841F  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $8420  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8421  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8425  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8426  8D 12                       op_8D_sbyte              +18
  $8428  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $8429  6D                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 13)
  $842A  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $842B  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $842F  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8430  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8431  8D 13                       op_8D_sbyte              +19
  $8433  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $8434  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8435  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8439  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $843A  8D 12                       op_8D_sbyte              +18
  $843C  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $843D  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $843E  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $843F  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8443  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8444  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $8445  8D 1D                       op_8D_sbyte              +29
  $8447  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $8448  8D 16                       op_8D_sbyte              +22
  $844A  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $844E  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $844F  8D 12                       op_8D_sbyte              +18
  $8451  8D 17                       op_8D_sbyte              +23
  $8453  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $8454  8D 17                       op_8D_sbyte              +23
  $8456  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $845A  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $845B  A8 C9 7F                    loadA_mem_word           $7FC9                     ; regA = word read using <word> as address (handler $EA75)
  $845E  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $845F  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8460  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8464  AC 77 D6                    host_call_simple         $D677 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8467  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8468  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $8469  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $846D  AC 87 D6                    host_call_simple         $D687 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8470  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8471  E9 36 D8 02                 host_call                $D836 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8475  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8476  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8477  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $847B  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $847C  E9 99 D9 02                 host_call                $D999 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8480  D8 87 84                    branch_z_abs             $8487                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8483  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8484  D6 88 84                    jump_abs                 $8488                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8487  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8488  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8489  D8 C3 84                    branch_z_abs             $84C3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $848C  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $848D  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8490  BB                          add                                                ; regA = regA + regB
  $8491  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8492  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $8493  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8494  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8498  D8 B9 84                    branch_z_abs             $84B9                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $849B  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $849C  D8 A3 84                    branch_z_abs             $84A3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $849F  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $84A0  D6 A9 84                    jump_abs                 $84A9                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$84A3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $84A4  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $84A7  BB                          add                                                ; regA = regA + regB
  $84A8  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
 >$84A9  D2                          aslA                                               ; regA <<= 1
  $84AA  8C D4 F7                    loadB_imm_word           $F7D4                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $84AD  BB                          add                                                ; regA = regA + regB
  $84AE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $84AF  D6 B5 84                    jump_abs                 $84B5                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$84B2  8A 7C BA                    loadA_imm_word           $BA7C                     ; "Home fief"  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
 >$84B5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $84B6  D6 C6 84                    jump_abs                 $84C6                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$84B9  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $84BA  D7 B2 84                    branch_nz_abs            $84B2                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $84BD  8A 86 BA                    loadA_imm_word           $BA86                     ; "Direct"  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $84C0  D6 B5 84                    jump_abs                 $84B5                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$84C3  8E 8D BA                    push_imm_word            $BA8D                     ; "Empty"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$84C6  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84CA  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $84CB  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $84CC  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84D0  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $84D1  D0                          incA                                               ; regA += 1
  $84D2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $84D3  8E 93 BA                    push_imm_word            $BA93                     ; "Fief %2d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $84D6  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84DA  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $84DB  8D 16                       op_8D_sbyte              +22
  $84DD  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84E1  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $84E2  E9 D3 D9 02                 host_call                $D9D3 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84E6  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $84E7  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $84E8  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84EC  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $84ED  D8 F8 84                    branch_z_abs             $84F8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $84F0  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $84F1  E9 4B DB 02                 host_call                $DB4B {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $84F5  D6 FF 84                    jump_abs                 $84FF                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$84F8  8E 9C BA                    push_imm_word            $BA9C                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $84FB  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$84FF  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8500  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8504  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8505  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8506  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8508  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8509  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $850C  BB                          add                                                ; regA = regA + regB
  $850D  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $850E  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $850F  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8510  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8511  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8512  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $8513  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8514  D8 1E 85                    branch_z_abs             $851E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8517  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8518  7D                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 13)
  $8519  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $851A  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $851B  D6 22 85                    jump_abs                 $8522                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$851E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $851F  D0                          incA                                               ; regA += 1
  $8520  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8521  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
 >$8522  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8526  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8527  D2                          aslA                                               ; regA <<= 1
  $8528  8C AE F8                    loadB_imm_word           $F8AE                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $852B  BB                          add                                                ; regA = regA + regB
  $852C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $852D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $852E  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8532  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8533  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $8534  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8535  D8 4A 85                    branch_z_abs             $854A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8538  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8539  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $853C  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $853D  D0                          incA                                               ; regA += 1
  $853E  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $853F  D1                          decA                                               ; regA -= 1
  $8540  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8541  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8542  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8543  E9 D5 83 04                 host_call                $83D5 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8547  D6 5B 85                    jump_abs                 $855B                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$854A  89 10                       loadA_imm_sbyte          +16                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $854C  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $854F  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8550  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8551  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8552  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $8554  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8555  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8556  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8557  E9 A2 83 04                 host_call                $83A2 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$855B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $855C  D0                          incA                                               ; regA += 1
  $855D  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $855E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $855F  8B 12                       loadB_imm_byte           +18                       ; regB = byte literal (handler $EABE)
  $8561  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8562  D7 11 85                    branch_nz_abs            $8511                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8565  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8566  D8 8C 85                    branch_z_abs             $858C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8569  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $856A  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $856E  A8 5B 6F                    loadA_mem_word           $6F5B                     ; regA = word read using <word> as address (handler $EA75)
  $8571  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $8572  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8573  E9 6F E7 04                 host_call                $E76F (marry_helper_e76f) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8577  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8578  E9 72 D9 02                 host_call                $D972 (war_helper_d972) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $857C  D8 8C 85                    branch_z_abs             $858C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $857F  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $8580  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8581  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8585  8E A5 BA                    push_imm_word            $BAA5                     ; "Sick"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8588  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$858C  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $858D  E9 1E 87 02                 host_call                $871E (fief_info_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8591  AC 94 80                    host_call_simple         $8094 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8594  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8595  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8599  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $859A   (frame_off=+0, body @ $859F)
; ============================================================
  $859F  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $85A3  FA                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $85A4  83 02                       op_83_byte               $02
  $85A6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $85A7 (effect_view_c)   (frame_off=+0, body @ $85AC)
; ============================================================
  $85AC  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $85B0  FA                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $85B1  83 02                       op_83_byte               $02
  $85B3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $85B4   (frame_off=-6, body @ $85B9)
; ============================================================
  $85B9  A4 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $85BC  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $85BD  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $85BE  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $85C1  BB                          add                                                ; regA = regA + regB
  $85C2  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $85C3  D8 D9 85                    branch_z_abs             $85D9                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $85C6  8E AA BA                    push_imm_word            $BAAA                     ; "Home\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $85C9  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $85CD  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $85CE  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $85D1  8E B0 BA                    push_imm_word            $BAB0                     ; "Fief"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $85D4  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $85D8  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$85D9  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $85DA  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $85DE  D2                          aslA                                               ; regA <<= 1
  $85DF  8C E9 FE                    loadB_imm_word           $FEE9                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $85E2  BB                          add                                                ; regA = regA + regB
  $85E3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $85E4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $85E5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $85E9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $85EA (effect_view_b)   (frame_off=-14, body @ $85EF)
; ============================================================
  $85EF  8A 89 6F                    loadA_imm_word           $6F89                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $85F2  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $85F3  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $85F4  D6 15 86                    jump_abs                 $8615                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$85F7  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $85F8  E9 B7 D9 02                 host_call                $D9B7 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $85FC  D8 13 86                    branch_z_abs             $8613                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $85FF  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8600  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8603  BB                          add                                                ; regA = regA + regB
  $8604  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8605  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8608  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $8609  D8 13 86                    branch_z_abs             $8613                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $860C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $860D  D0                          incA                                               ; regA += 1
  $860E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $860F  D1                          decA                                               ; regA -= 1
  $8610  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8611  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8612  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$8613  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8614  D0                          incA                                               ; regA += 1
 >$8615  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8616  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8617  A6 9D 6D                    loadB_mem_word           $6D9D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $861A  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $861B  D7 F7 85                    branch_nz_abs            $85F7                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $861E  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $861F  89 FF                       loadA_imm_sbyte          -1                        ; regA = sign-extended signed byte literal (handler $EAAF)
  $8621  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8622  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8623  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8624  8A 89 6F                    loadA_imm_word           $6F89                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $8627  D6 05 87                    jump_abs                 $8705                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$862A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $862B  D7 64 86                    branch_nz_abs            $8664                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $862E  AC 20 CD                    host_call_simple         $CD20 (ui_helper_cd20) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8631  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8632  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8636  8D 2B                       op_8D_sbyte              +43
  $8638  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $8639  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $863D  8D 23                       op_8D_sbyte              +35
  $863F  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $8640  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8644  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8645  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8646  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8647  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8648  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8649  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $864D  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $864E  8D 11                       op_8D_sbyte              +17
  $8650  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $8651  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8652  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8653  E9 6A CF 0A                 host_call                $CF6A {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8657  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8658  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8659  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $865D  8E B5 BA                    push_imm_word            $BAB5                     ; "Fief\n\nGold\nTown\nRice\nOutput\nDams\nLylty\nWealth\nMe"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8660  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$8664  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8665  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8666  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8667  E9 36 D8 02                 host_call                $D836 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $866B  43                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $866C  A8 CF 7F                    loadA_mem_word           $7FCF                     ; regA = word read using <word> as address (handler $EA75)
  $866F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8670  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $8671  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8672  7B                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 11)
  $8673  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $8674  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $8677  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8678  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8679  D0                          incA                                               ; regA += 1
  $867A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $867B  8E 02 BB                    push_imm_word            $BB02                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $867E  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8682  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8683  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8684  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8686  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8687  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $868A  BB                          add                                                ; regA = regA + regB
  $868B  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $868C  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $868D  D1                          decA                                               ; regA -= 1
  $868E  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $8691  A4 CF 7F                    loadA_mem_word           $7FCF                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8694  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8695  A8 CF 7F                    loadA_mem_word           $7FCF                     ; regA = word read using <word> as address (handler $EA75)
  $8698  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8699  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $869A  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $869B  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $869D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $869E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $869F  8E 06 BB                    push_imm_word            $BB06                     ; "%4d\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $86A2  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $86A6  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $86A7  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
 >$86A8  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $86A9  D1                          decA                                               ; regA -= 1
  $86AA  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $86AD  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $86AE  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $86AF  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $86B0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $86B1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $86B2  8E 0B BB                    push_imm_word            $BB0B                     ; "%4d\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $86B5  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $86B9  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $86BA  D0                          incA                                               ; regA += 1
  $86BB  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $86BC  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $86BD  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $86BE  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $86BF  D7 A8 86                    branch_nz_abs            $86A8                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $86C2  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $86C3  D1                          decA                                               ; regA -= 1
  $86C4  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $86C7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $86C8  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $86C9  8C 2D 6D                    loadB_imm_word           $6D2D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $86CC  BB                          add                                                ; regA = regA + regB
  $86CD  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $86CE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $86CF  8E 10 BB                    push_imm_word            $BB10                     ; "%4d\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $86D2  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $86D6  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $86D7  D1                          decA                                               ; regA -= 1
  $86D8  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $86DB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $86DC  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $86DD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $86DE  E9 B4 85 02                 host_call                $85B4 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $86E2  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $86E3  D0                          incA                                               ; regA += 1
  $86E4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $86E5  54                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 4)
  $86E6  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $86E7  D7 F4 86                    branch_nz_abs            $86F4                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $86EA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $86EB  D0                          incA                                               ; regA += 1
  $86EC  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $86ED  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $86F0  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $86F1  D8 03 87                    branch_z_abs             $8703                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$86F4  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $86F5  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $86F9  AC 09 D6                    host_call_simple         $D609 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $86FC  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $86FD  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $86FE  D7 0F 87                    branch_nz_abs            $870F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8701  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8702  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$8703  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8704  D0                          incA                                               ; regA += 1
 >$8705  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8706  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8707  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8708  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $870B  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $870C  D7 2A 86                    branch_nz_abs            $862A                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$870F  8D 21                       op_8D_sbyte              +33
  $8711  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $8712  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8716  8D 30                       op_8D_sbyte              +48
  $8718  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $8719  E9 8B CF 04                 host_call                $CF8B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $871D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $871E (fief_info_display)   (frame_off=-2, body @ $8723)
; ============================================================
  $8723  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8724  A8 E2 7B                    loadA_mem_word           $7BE2                     ; regA = word read using <word> as address (handler $EA75)
  $8727  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8728  8D 16                       op_8D_sbyte              +22
  $872A  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $872E  8E 15 BB                    push_imm_word            $BB15                     ; "__Menu__"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8731  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8735  AC 69 CC                    host_call_simple         $CC69 (trade_helper_cc69) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8738  A4 E2 7B                    loadA_mem_word           $7BE2                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $873B  D7 6B 87                    branch_nz_abs            $876B                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $873E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $873F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8740  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8741  77                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 7)
  $8742  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8743  8D 16                       op_8D_sbyte              +22
  $8745  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8749  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $874A  D0                          incA                                               ; regA += 1
  $874B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $874C  8E 1E BB                    push_imm_word            $BB1E                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $874F  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8753  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8754  D2                          aslA                                               ; regA <<= 1
  $8755  8C 16 F8                    loadB_imm_word           $F816                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8758  BB                          add                                                ; regA = regA + regB
  $8759  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $875A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $875B  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $875F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8760  D0                          incA                                               ; regA += 1
  $8761  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8762  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8763  5C                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 12)
  $8764  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8765  D7 40 87                    branch_nz_abs            $8740                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8768  D6 97 87                    jump_abs                 $8797                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$876B  4C                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $876C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$876D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $876E  8F FB                       addA_imm_sbyte           -5                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $8770  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8771  8D 16                       op_8D_sbyte              +22
  $8773  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8777  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8778  D0                          incA                                               ; regA += 1
  $8779  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $877A  8E 22 BB                    push_imm_word            $BB22                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $877D  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8781  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8782  D2                          aslA                                               ; regA <<= 1
  $8783  8C 16 F8                    loadB_imm_word           $F816                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8786  BB                          add                                                ; regA = regA + regB
  $8787  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8788  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8789  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $878D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $878E  D0                          incA                                               ; regA += 1
  $878F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8790  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8791  8B 15                       loadB_imm_byte           +21                       ; regB = byte literal (handler $EABE)
  $8793  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8794  D7 6D 87                    branch_nz_abs            $876D                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$8797  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $879A  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $879D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $879E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $879F (province_select_helper)   (frame_off=+0, body @ $87A4)
; ============================================================
  $87A4  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $87A5  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87A6  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87A7  E9 72 81 06                 host_call                $8172 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87AB  D7 BB 87                    branch_nz_abs            $87BB                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $87AE  8E 8A F9                    push_imm_word            $F98A                     ; "\nno fiefs"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $87B1  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87B2  E9 03 80 04                 host_call                $8003 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87B6  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $87B9  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $87BA  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$87BB  8E 26 BB                    push_imm_word            $BB26                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $87BE  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87BF  E9 03 80 04                 host_call                $8003 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87C3  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $87C4  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87C5  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $87C6  E9 72 81 06                 host_call                $8172 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87CA  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $87CB (helper_87CB)   (frame_off=+0, body @ $87D0)
; ============================================================
  $87D0  8E 28 BB                    push_imm_word            $BB28                     ; "Not home fief"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $87D3  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87D7  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $87D8 (effect_dam)   (frame_off=+0, body @ $87DD)
; ============================================================
  $87DD  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $87DE  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $87DF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $87E0  D7 E7 87                    branch_nz_abs            $87E7                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $87E3  8A 10 27                    loadA_imm_word           $2710                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $87E6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$87E7  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $87E8  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $87E9  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $87EA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $87EB  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $87EF  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $87F0 (effect_grow)   (frame_off=-4, body @ $87F5)
; ============================================================
  $87F5  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $87F6  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $87F7  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $87F8  D8 FD 87                    branch_z_abs             $87FD                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $87FB  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $87FC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$87FD  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $87FE  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $87FF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8800  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8801  BB                          add                                                ; regA = regA + regB
  $8802  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8803  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8807  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8808  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8809  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $880C  BC                          sub                                                ; regA = regA - regB
  $880D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $880E  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $880F  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8813  D2                          aslA                                               ; regA <<= 1
  $8814  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8815  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8816  D7 1B 88                    branch_nz_abs            $881B                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8819  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $881A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$881B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $881C  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $881D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $881E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $881F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8820  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8822  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8823  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8824  BC                          sub                                                ; regA = regA - regB
  $8825  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8826  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8827  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8828  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8829  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $882A  D8 2F 88                    branch_z_abs             $882F                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $882D  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $882E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$882F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8830  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $8831  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8832  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8833  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8834  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8835  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8836  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8837  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $8838  D8 40 88                    branch_z_abs             $8840                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $883B  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $883D  D6 49 88                    jump_abs                 $8849                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8840  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8841  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $8842  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8843  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8844  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $8845  E9 DE D6 04                 host_call                $D6DE (math32_2arg) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$8849  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $884A  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $884B  CD                          swap_AB                                            ; regA <-> regB
  $884C  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $884D  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $884E  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $884F  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $8850  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8851  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $8852  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8853  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8854  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8858  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8859  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $885A  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $885B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $885C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $885D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $885E  BC                          sub                                                ; regA = regA - regB
  $885F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8860  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $8861  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8862  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $8863  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8864  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8865  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8869  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $886A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $886B  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $886C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $886D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $886E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $886F  BC                          sub                                                ; regA = regA - regB
  $8870  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8871  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $8872  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8873  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $8874  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8875  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8876  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8877  BB                          add                                                ; regA = regA + regB
  $8878  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8879  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $887A  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $887B  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $887C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $887D (helper_dam_rounding)   (frame_off=-2, body @ $8882)
; ============================================================
  $8882  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8883  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8884  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $8888  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8889  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $888A  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $888B  D8 99 88                    branch_z_abs             $8899                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $888E  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $888F  CD                          swap_AB                                            ; regA <-> regB
  $8890  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8891  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8892  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8893  D7 99 88                    branch_nz_abs            $8899                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8896  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8897  D0                          incA                                               ; regA += 1
  $8898  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8899  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $889A  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $889B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $889C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $889D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $889E  BB                          add                                                ; regA = regA + regB
  $889F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $88A0  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $88A1  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $88A5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $88A6 (effect_build)   (frame_off=-4, body @ $88AB)
; ============================================================
  $88AB  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88AC  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $88AD  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $88AE  D8 B3 88                    branch_z_abs             $88B3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $88B1  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $88B2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$88B3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88B4  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $88B5  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $88B6  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $88B7  BB                          add                                                ; regA = regA + regB
  $88B8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88B9  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $88BD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88BE  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $88BF  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $88C2  BC                          sub                                                ; regA = regA - regB
  $88C3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88C4  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $88C5  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $88C9  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $88CA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $88CB  D7 D0 88                    branch_nz_abs            $88D0                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $88CE  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $88CF  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$88D0  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88D1  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $88D2  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $88D3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88D4  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88D5  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $88D7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $88D8  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $88D9  BC                          sub                                                ; regA = regA - regB
  $88DA  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $88DB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88DC  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $88DD  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $88DE  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $88DF  D8 E4 88                    branch_z_abs             $88E4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $88E2  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $88E3  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$88E4  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88E5  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $88E6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $88E7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88E8  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $88E9  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $88EA  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $88EB  D8 F3 88                    branch_z_abs             $88F3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $88EE  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $88F0  D6 FC 88                    jump_abs                 $88FC                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$88F3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $88F4  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $88F5  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $88F6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $88F7  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $88F8  E9 DE D6 04                 host_call                $D6DE (math32_2arg) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$88FC  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $88FD  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $88FE  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $88FF  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8900  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8901  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8902  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $8903  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8904  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8905  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8909  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $890A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $890B  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $890C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $890D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $890E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $890F  BC                          sub                                                ; regA = regA - regB
  $8910  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8911  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8912  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $8913  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8914  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8915  D2                          aslA                                               ; regA <<= 1
  $8916  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8917  E9 7D 88 04                 host_call                $887D (helper_dam_rounding) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $891B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $891C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $891D   (frame_off=-4, body @ $8922)
; ============================================================
  $8922  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8923  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $8924  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $8925  D8 2A 89                    branch_z_abs             $892A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8928  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8929  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$892A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $892B  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $892C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $892D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $892E  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $892F  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $8930  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8931  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8932  BB                          add                                                ; regA = regA + regB
  $8933  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8934  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8935  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8936  BB                          add                                                ; regA = regA + regB
  $8937  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8938  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $893C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $893D  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $893E  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8941  BC                          sub                                                ; regA = regA - regB
  $8942  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8943  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8944  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8948  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8949  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $894A  D7 4F 89                    branch_nz_abs            $894F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $894D  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $894E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$894F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8950  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $8951  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8952  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8953  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8954  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8956  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8957  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8958  BC                          sub                                                ; regA = regA - regB
  $8959  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $895A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $895B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $895C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $895D  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $895E  D8 63 89                    branch_z_abs             $8963                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8961  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8962  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8963  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8964  D2                          aslA                                               ; regA <<= 1
  $8965  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8966  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8967  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $8968  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8969  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $896A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $896B  BB                          add                                                ; regA = regA + regB
  $896C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $896D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $896E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $896F   (frame_off=-4, body @ $8974)
; ============================================================
  $8974  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8975  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $8976  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $8977  D8 7C 89                    branch_z_abs             $897C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $897A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $897B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$897C  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $897D  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $897E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $897F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8980  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8981  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $8982  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8983  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8984  BB                          add                                                ; regA = regA + regB
  $8985  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8986  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8987  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8988  BB                          add                                                ; regA = regA + regB
  $8989  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $898A  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $898E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $898F  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8990  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8993  BC                          sub                                                ; regA = regA - regB
  $8994  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8995  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8996  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $899A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $899B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $899C  D7 A1 89                    branch_nz_abs            $89A1                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $899F  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $89A0  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$89A1  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89A2  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $89A3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89A4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89A5  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89A6  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $89A8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89A9  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $89AA  BC                          sub                                                ; regA = regA - regB
  $89AB  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $89AC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89AD  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $89AE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $89AF  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $89B0  D8 B5 89                    branch_z_abs             $89B5                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $89B3  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $89B4  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$89B5  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $89B6  D2                          aslA                                               ; regA <<= 1
  $89B7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89B8  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89B9  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $89BA  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $89BB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89BC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89BD  BB                          add                                                ; regA = regA + regB
  $89BE  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $89BF  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $89C0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $89C1   (frame_off=-4, body @ $89C6)
; ============================================================
  $89C6  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89C7  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $89C8  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $89C9  D8 CE 89                    branch_z_abs             $89CE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $89CC  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $89CD  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$89CE  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89CF  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $89D1  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89D2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89D3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89D4  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $89D6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89D7  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $89D8  BB                          add                                                ; regA = regA + regB
  $89D9  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $89DA  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $89DB  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $89DC  BB                          add                                                ; regA = regA + regB
  $89DD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89DE  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $89E2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89E3  46                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $89E4  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $89E7  BC                          sub                                                ; regA = regA - regB
  $89E8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89E9  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $89EA  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $89EE  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $89EF  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $89F0  D7 F5 89                    branch_nz_abs            $89F5                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $89F3  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $89F4  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$89F5  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89F6  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $89F8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89F9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $89FA  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $89FB  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $89FD  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $89FE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $89FF  BC                          sub                                                ; regA = regA - regB
  $8A00  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8A01  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8A02  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8A03  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8A04  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $8A05  D8 0A 8A                    branch_z_abs             $8A0A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8A08  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8A09  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8A0A  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $8A0B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8A0C  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8A0E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8A0F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8A10  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8A11  BB                          add                                                ; regA = regA + regB
  $8A12  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8A13  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8A14  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8A15 (effect_trade)   (frame_off=+0, body @ $8A1A)
; ============================================================
  $8A1A  A4 9D 6D                    loadA_mem_word           $6D9D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8A1D  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $8A1F  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8A20  D8 28 8A                    branch_z_abs             $8A28                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8A23  89 1E                       loadA_imm_sbyte          +30                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $8A25  D6 29 8A                    jump_abs                 $8A29                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8A28  4D                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8A29  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8A2C  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8A2D  D7 46 8A                    branch_nz_abs            $8A46                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8A30  A4 9D 6D                    loadA_mem_word           $6D9D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8A33  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $8A35  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8A36  D8 3E 8A                    branch_z_abs             $8A3E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8A39  89 20                       loadA_imm_sbyte          +32                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $8A3B  D6 3F 8A                    jump_abs                 $8A3F                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8A3E  4E                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8A3F  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8A42  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8A43  D8 48 8A                    branch_z_abs             $8A48                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$8A46  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8A47  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$8A48  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $8A4B  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $8A4C  DA                          bitand                                             ; regA = regA & regB
  $8A4D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8A4E (helper_8A4E)   (frame_off=-6, body @ $8A53)
; ============================================================
  $8A53  A4 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8A56  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8A57  A4 CF 7F                    loadA_mem_word           $7FCF                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8A5A  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8A5B  89 1A                       loadA_imm_sbyte          +26                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $8A5D  A8 CD 7F                    loadA_mem_word           $7FCD                     ; regA = word read using <word> as address (handler $EA75)
  $8A60  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8A61  D5                          switch                                             ; Jump-table dispatch on regA (handler $EC65) — reads inline (value,target) pairs from the bytecode stream, jumps to the matching target. Variable-length; the disassembler stops the fall-through run here.

  ; ---- gap $8A62-$8B09 (168 bytes, not on any traced path) ----

; ============================================================
; sub $8B0A (effect_subhandler_A003)   (frame_off=-10, body @ $8B0F)
; ============================================================
  $8B0F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8B12  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8B14  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8B15  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8B18  BB                          add                                                ; regA = regA + regB
  $8B19  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8B1A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8B1B  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $8B1C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8B1D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8B1E  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8B20  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B21  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8B22  A4 0D 6E                    loadA_mem_word           $6E0D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8B25  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8B26  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8B27  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $8B28  E9 03 83 04                 host_call                $8303 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8B2C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B2D  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8B2E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8B2F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B30  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B31  BB                          add                                                ; regA = regA + regB
  $8B32  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8B33  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8B34  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8B35  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8B36  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B37  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B38  BC                          sub                                                ; regA = regA - regB
  $8B39  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8B3A  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8B3B  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8B3F  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8B40 (effect_subhandler_9F04)   (frame_off=-10, body @ $8B45)
; ============================================================
  $8B45  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8B48  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8B4A  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8B4B  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8B4E  BB                          add                                                ; regA = regA + regB
  $8B4F  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8B50  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $8B51  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8B52  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8B53  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8B54  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8B55  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B56  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8B57  A4 0B 6E                    loadA_mem_word           $6E0B                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8B5A  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $8B5B  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8B5C  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8B5D  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $8B5E  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $8B5F  D8 74 8B                    branch_z_abs             $8B74                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8B62  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8B63  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B64  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B65  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8B66  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8B67  BC                          sub                                                ; regA = regA - regB
  $8B68  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B69  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $8B6A  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $8B6B  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8B6F  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $8B70  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8B71  D6 7A 8B                    jump_abs                 $8B7A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8B74  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8B75  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $8B76  E9 03 83 04                 host_call                $8303 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$8B7A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8B7B  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $8B7C  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8B7D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8B7E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B7F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B80  BB                          add                                                ; regA = regA + regB
  $8B81  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8B82  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8B83  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $8B84  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8B85  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8B86  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8B87  BB                          add                                                ; regA = regA + regB
  $8B88  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8B89  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8B8A  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8B8E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8B8F (helper_8B8F)   (frame_off=+0, body @ $8B94)
; ============================================================
  $8B94  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8B95  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8B96  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8B97  E9 26 F2 06                 host_call                $F226 (syscall_dispatch) {native}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8B9B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8B9C  D7 A3 8B                    branch_nz_abs            $8BA3                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8B9F  47                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8BA0  D6 A4 8B                    jump_abs                 $8BA4                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8BA3  48                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8BA4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BA5  E9 03 CA 02                 host_call                $CA03 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BA9  A4 4D 6F                    loadA_mem_word           $6F4D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8BAC  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $8BAD  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8BAE  D8 BB 8B                    branch_z_abs             $8BBB                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$8BB1  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8BB2  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8BB3  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8BB4  E9 26 F2 06                 host_call                $F226 (syscall_dispatch) {native}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BB8  D7 B1 8B                    branch_nz_abs            $8BB1                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$8BBB  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8BBC  E9 03 CA 02                 host_call                $CA03 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BC0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8BC1   (frame_off=+0, body @ $8BC6)
; ============================================================
  $8BC6  8D 19                       op_8D_sbyte              +25
  $8BC8  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BCC  8F 1E                       addA_imm_sbyte           +30                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8BCE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BCF  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8BD0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8BD1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BD2  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8BD3  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BD7  D0                          incA                                               ; regA += 1
  $8BD8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BD9  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BDD  D0                          incA                                               ; regA += 1
  $8BDE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BDF  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8BE0  E9 31 CA 04                 host_call                $CA31 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BE4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8BE5 (effect_send)   (frame_off=+0, body @ $8BEA)
; ============================================================
  $8BEA  3E                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8BEB  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8BEC  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $8BED  BC                          sub                                                ; regA = regA - regB
  $8BEE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BEF  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8BF3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8BF4   (frame_off=+0, body @ $8BF9)
; ============================================================
  $8BF9  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8BFA  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8BFC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8BFD  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8BFE  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C00  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C01  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C02  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C03  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C05  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C06  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C07  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8C08  8D 14                       op_8D_sbyte              +20
  $8C0A  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C0E  8F 28                       addA_imm_sbyte           +40                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C10  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C11  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C12  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C14  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C15  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C16  E9 24 DA 0A                 host_call                $DA24 {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C1A  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8C1B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C1C  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C1E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C1F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C20  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C22  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C23  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C24  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C25  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C27  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C28  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C29  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8C2A  8D 14                       op_8D_sbyte              +20
  $8C2C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C30  8F 3C                       addA_imm_sbyte           +60                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C32  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C33  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C34  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C36  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C37  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C38  E9 24 DA 0A                 host_call                $DA24 {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C3C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8C3D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C3E  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C40  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C41  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C42  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C44  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C45  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C46  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C47  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C49  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C4A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C4B  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8C4C  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8C4D  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C51  8F 32                       addA_imm_sbyte           +50                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C53  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C54  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C55  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C57  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C58  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C59  E9 24 DA 0A                 host_call                $DA24 {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C5D  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8C5E  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8C5F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8C60  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $8C62  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8C63  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C64  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8C65  BB                          add                                                ; regA = regA + regB
  $8C66  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8C67  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8C68   (frame_off=-2, body @ $8C6D)
; ============================================================
  $8C6D  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $8C71  8D D9                       op_8D_sbyte              -39
  $8C73  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $8C74  D7 A1 8C                    branch_nz_abs            $8CA1                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8C77  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8C78  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C7C  D7 A1 8C                    branch_nz_abs            $8CA1                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8C7F  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8C82  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8C83  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8C84  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $8C85  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8C89  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $8C8A  BB                          add                                                ; regA = regA + regB
  $8C8B  D0                          incA                                               ; regA += 1
  $8C8C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8C8D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C8E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8C8F  BB                          add                                                ; regA = regA + regB
  $8C90  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8C91  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8C92  D0                          incA                                               ; regA += 1
  $8C93  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8C94  8C C8 00                    loadB_imm_word           $00C8                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8C97  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $8C98  D8 A1 8C                    branch_z_abs             $8CA1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8C9B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8C9C  D0                          incA                                               ; regA += 1
  $8C9D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8C9E  89 C8                       loadA_imm_sbyte          -56                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $8CA0  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$8CA1  8A FF 00                    loadA_imm_word           $00FF                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $8CA4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8CA5 (effect_move)   (frame_off=-10, body @ $8CAA)
; ============================================================
  $8CAA  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8CAD  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8CAF  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8CB0  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8CB3  BB                          add                                                ; regA = regA + regB
  $8CB4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8CB5  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8CB6  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8CB7  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8CB8  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8CBB  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8CBD  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8CBE  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8CC1  BB                          add                                                ; regA = regA + regB
  $8CC2  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8CC3  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8CC4  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8CC5  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8CC6  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8CC7  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8CC8  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $8CC9  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8CCA  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $8CCB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CCC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CCD  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8CCE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CCF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CD0  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8CD1  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8CD2  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8CD3  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8CD4  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $8CD6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CD7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CD8  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $8CD9  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CDA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CDB  E9 24 DA 0A                 host_call                $DA24 {bytecode}, $0A     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8CDF  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8CE0  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $8CE1  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8CE2  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8CE3  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8CE4  D0                          incA                                               ; regA += 1
  $8CE5  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8CE6  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8CE7  53                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 3)
  $8CE8  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8CE9  D7 C8 8C                    branch_nz_abs            $8CC8                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8CEC  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8CED  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8CEE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8CEF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CF0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CF1  BB                          add                                                ; regA = regA + regB
  $8CF2  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8CF3  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8CF4  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8CF5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8CF6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8CF7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8CF8  BC                          sub                                                ; regA = regA - regB
  $8CF9  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8CFA  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $8CFE  15                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 5)
  $8CFF  D8 02 CF                    branch_z_abs             $CF02                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.

; ============================================================
; sub $8D02   (frame_off=-4, body @ $8D07)
; ============================================================
  $8D07  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8D08  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D0C  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $8D0D  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8D0E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8D0F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8D10  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8D12  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8D13  8C 0D 70                    loadB_imm_word           $700D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8D16  BB                          add                                                ; regA = regA + regB
  $8D17  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8D18  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8D19  BB                          add                                                ; regA = regA + regB
  $8D1A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8D1B  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8D1C  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D20  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $8D21  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8D22  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8D23  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8D24  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8D26  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8D27  8C 0D 70                    loadB_imm_word           $700D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8D2A  BB                          add                                                ; regA = regA + regB
  $8D2B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8D2C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8D2D  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8D2E  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D32  A6 63 6D                    loadB_mem_word           $6D63 (const_two)         ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8D35  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8D36  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8D37  BB                          add                                                ; regA = regA + regB
  $8D38  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8D39  BB                          add                                                ; regA = regA + regB
  $8D3A  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8D3B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8D3C  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $8D3D  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $8D3E  D8 4B 8D                    branch_z_abs             $8D4B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8D41  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8D42  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D46  D8 4B 8D                    branch_z_abs             $8D4B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8D49  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8D4A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$8D4B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8D4C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8D4D (effect_bribe)   (frame_off=-12, body @ $8D52)
; ============================================================
  $8D52  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8D55  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8D57  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8D58  8C 09 70                    loadB_imm_word           $7009                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8D5B  BB                          add                                                ; regA = regA + regB
  $8D5C  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8D5D  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8D5E  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $8D5F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8D60  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8D61  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8D62  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8D63  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8D66  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8D68  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8D69  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8D6C  BB                          add                                                ; regA = regA + regB
  $8D6D  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $8D6E  AA 63 6F AA                 op_AA_wb                 $6F63, $AA
  $8D72  5F                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 15)
  $8D73  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $8D74  E9 02 8D 04                 host_call                $8D02 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D78  D8 BF 8D                    branch_z_abs             $8DBF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8D7B  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8D7C  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D80  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8D81  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $8D82  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $8D83  E9 C1 8B 04                 host_call                $8BC1 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D87  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $8D88  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8D89  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8D8A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8D8B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8D8C  BC                          sub                                                ; regA = regA - regB
  $8D8D  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8D8E  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $8D8F  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8D90  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $8D91  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8D92  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8D93  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8D94  BB                          add                                                ; regA = regA + regB
  $8D95  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8D96  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $8D97  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8D9B  8D 1A                       op_8D_sbyte              +26
  $8D9D  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DA1  8E 3E BB                    push_imm_word            $BB3E                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8DA4  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DA8  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $8DA9  8E 36 FC                    push_imm_word            $FC36                     ; "%d peasants\nhave defected"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8DAC  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DB0  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8DB3  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $8DB7  F7                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $8DB8  D7 02 41                    branch_nz_abs            $4102                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8DBB  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $8DBC  D6 E6 8D                    jump_abs                 $8DE6                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8DBF  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8DC0  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DC4  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8DC5  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8DC8  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $8DC9  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8DCA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8DCB  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8DCC  BC                          sub                                                ; regA = regA - regB
  $8DCD  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8DCE  8E 50 FC                    push_imm_word            $FC50                     ; "No peasants\ndefected!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8DD1  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DD5  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8DD8  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8DD9  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $8DDA  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $8DDB  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8DDC  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8DDD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8DDE  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $8DDF  8F F8                       addA_imm_sbyte           -8                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $8DE1  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8DE2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8DE3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8DE4  BB                          add                                                ; regA = regA + regB
  $8DE5  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$8DE6  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $8DE7  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $8DE8  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8DE9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8DEA  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8DEB  BC                          sub                                                ; regA = regA - regB
  $8DEC  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8DED  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $8DF1  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $8DF2  D8 02 AA                    branch_z_abs             $AA02                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8DF5  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $8DF6  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $8DF7  E9 36 D8 02                 host_call                $D836 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8DFB  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $8DFC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $8DFD   (frame_off=-34, body @ $8E02)
; ============================================================
  $8E02  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8E03  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8E04  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8E07  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $8E08  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $8E0C  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E0D  D7 02 28                    branch_nz_abs            $2802                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8E10  8A 7F 6F                    loadA_imm_word           $6F7F                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $8E13  24                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 4)
  $8E14  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8E17  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8E19  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8E1A  8C 13 70                    loadB_imm_word           $7013                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8E1D  BB                          add                                                ; regA = regA + regB
  $8E1E  23                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 3)
  $8E1F  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $8E20  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E21  22                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 2)
  $8E22  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $8E23  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E24  21                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 1)
  $8E25  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $8E26  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E27  20                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 0)
  $8E28  8A 85 6F                    loadA_imm_word           $6F85                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $8E2B  85 E6                       op_85_byte               $E6
  $8E2D  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8E30  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $8E32  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8E33  8C 13 70                    loadB_imm_word           $7013                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8E36  BB                          add                                                ; regA = regA + regB
  $8E37  85 E4                       op_85_byte               $E4
  $8E39  81 E4                       op_81_byte               $E4
  $8E3B  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E3C  85 E2                       op_85_byte               $E2
  $8E3E  81 E2                       op_81_byte               $E2
  $8E40  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E41  85 E0                       op_85_byte               $E0
  $8E43  81 E6                       op_81_byte               $E6
  $8E45  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E46  85 DE                       op_85_byte               $DE
  $8E48  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $8E49  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8E4D  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8E50  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8E53  BB                          add                                                ; regA = regA + regB
  $8E54  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8E55  57                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 7)
  $8E56  BD                          shl_by_regB                                        ; regA <<= regB  (shift left by regB count)
  $8E57  A9 66 6F                    storeA_mem_byte          $6F66                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $8E5A  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $8E5B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E5C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E5D  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $8E5E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E5F  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8E60  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8E61  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E62  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $8E63  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E64  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8E65  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8E66  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E67  BB                          add                                                ; regA = regA + regB
  $8E68  D2                          aslA                                               ; regA <<= 1
  $8E69  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E6A  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8E6B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E6C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E6D  A5 65 6F                    loadA_mem_byte           $6F65                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $8E70  8C 80 00                    loadB_imm_word           $0080 (skip_vblank_wait)  ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8E73  DA                          bitand                                             ; regA = regA & regB
  $8E74  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $8E75  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $8E76  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E77  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E78  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8E79  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E7A  BB                          add                                                ; regA = regA + regB
  $8E7B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E7C  BB                          add                                                ; regA = regA + regB
  $8E7D  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $8E7E  81 E4                       op_81_byte               $E4
  $8E80  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E81  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E82  81 E2                       op_81_byte               $E2
  $8E84  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E85  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8E86  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8E87  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E88  81 E0                       op_81_byte               $E0
  $8E8A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E8B  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8E8C  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8E8D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E8E  BB                          add                                                ; regA = regA + regB
  $8E8F  D2                          aslA                                               ; regA <<= 1
  $8E90  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E91  81 DE                       op_81_byte               $DE
  $8E93  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8E94  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8E95  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8E98  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8E9B  BB                          add                                                ; regA = regA + regB
  $8E9C  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8E9D  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8E9E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8E9F  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8EA0  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8EA1  BB                          add                                                ; regA = regA + regB
  $8EA2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8EA3  BB                          add                                                ; regA = regA + regB
  $8EA4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8EA5  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $8EA9  DA                          bitand                                             ; regA = regA & regB
  $8EAA  D7 02 25                    branch_nz_abs            $2502                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8EAD  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8EAE  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8EAF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8EB0  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8EB3  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $8EB4  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8EB5  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8EB6  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8EB7  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $8EB8  D8 CB 8E                    branch_z_abs             $8ECB                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8EBB  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8EBC  81 E4                       op_81_byte               $E4
  $8EBE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8EBF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8EC0  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8EC4  CD                          swap_AB                                            ; regA <-> regB
  $8EC5  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8EC6  BB                          add                                                ; regA = regA + regB
  $8EC7  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $8EC8  D6 D7 8E                    jump_abs                 $8ED7                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8ECB  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $8ECC  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $8ECD  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8ECE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8ECF  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8ED3  CD                          swap_AB                                            ; regA <-> regB
  $8ED4  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $8ED5  BB                          add                                                ; regA = regA + regB
  $8ED6  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$8ED7  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $8ED8  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $8ED9  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $8EDA  D8 F4 8E                    branch_z_abs             $8EF4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8EDD  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8EE0  A8 57 6F                    loadA_mem_word           $6F57                     ; regA = word read using <word> as address (handler $EA75)
  $8EE3  A4 87 6F                    loadA_mem_word           $6F87                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8EE6  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8EE7  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8EE8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8EE9  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $8EEA  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8EEB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8EEC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8EED  BB                          add                                                ; regA = regA + regB
  $8EEE  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8EEF  87 DE                       op_87_byte               $DE
  $8EF1  D6 08 8F                    jump_abs                 $8F08                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8EF4  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8EF7  A8 57 6F                    loadA_mem_word           $6F57                     ; regA = word read using <word> as address (handler $EA75)
  $8EFA  A4 81 6F                    loadA_mem_word           $6F81                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8EFD  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $8EFE  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8EFF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F00  81 DE                       op_81_byte               $DE
  $8F02  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $8F03  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F04  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $8F05  BB                          add                                                ; regA = regA + regB
  $8F06  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8F07  30                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 0)
 >$8F08  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8F09  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $8F0A  A4 57 6F                    loadA_mem_word           $6F57                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8F0D  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $8F10  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $8F11  D8 42 8F                    branch_z_abs             $8F42                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F14  A5 65 6F                    loadA_mem_byte           $6F65                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $8F17  8C 80 00                    loadB_imm_word           $0080 (skip_vblank_wait)  ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F1A  DA                          bitand                                             ; regA = regA & regB
  $8F1B  D8 A1 8F                    branch_z_abs             $8FA1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F1E  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $8F1F  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $8F20  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8F21  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8F22  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8F23  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F24  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8F28  D8 3E 8F                    branch_z_abs             $8F3E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F2B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8F2C  A9 65 6F                    storeA_mem_byte          $6F65                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $8F2F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8F32  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F35  BB                          add                                                ; regA = regA + regB
  $8F36  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F37  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8F38  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8F39  43                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8F3A  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8F3B  D6 A1 8F                    jump_abs                 $8FA1                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8F3E  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8F3F  D6 3A 8F                    jump_abs                 $8F3A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8F42  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8F45  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F48  BB                          add                                                ; regA = regA + regB
  $8F49  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8F4A  D8 A1 8F                    branch_z_abs             $8FA1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F4D  8E 5F 6F                    push_imm_word            $6F5F (selected_province_idx) ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8F50  8E 63 6F                    push_imm_word            $6F63                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8F53  E9 94 CB 04                 host_call                $CB94 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8F57  AC D7 DA                    host_call_simple         $DAD7 (combat_helper_dad7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8F5A  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8F5D  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $8F5E  E9 3A DD 04                 host_call                $DD3A (combat_helper_dd3a) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8F62  A5 4F 6F                    loadA_mem_byte           $6F4F                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $8F65  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F68  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $8F69  D8 95 8F                    branch_z_abs             $8F95                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F6C  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $8F6D  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $8F6E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $8F6F  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $8F70  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $8F71  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F72  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8F76  D8 95 8F                    branch_z_abs             $8F95                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $8F79  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $8F7C  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F7F  BB                          add                                                ; regA = regA + regB
  $8F80  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F81  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8F82  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8F83  A5 4F 6F                    loadA_mem_byte           $6F4F                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $8F86  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8F89  BB                          add                                                ; regA = regA + regB
  $8F8A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8F8B  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8F8C  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $8F8D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $8F8E  A9 66 6F                    storeA_mem_byte          $6F66                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $8F91  44                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $8F92  D6 96 8F                    jump_abs                 $8F96                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$8F95  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$8F96  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $8F97  8E 5F 6F                    push_imm_word            $6F5F (selected_province_idx) ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8F9A  8E 63 6F                    push_imm_word            $6F63                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8F9D  E9 94 CB 04                 host_call                $CB94 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$8FA1  AA 57 6F E9                 op_AA_wb                 $6F57, $E9
  $8FA5  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $8FA6  D7 02 59                    branch_nz_abs            $5902                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $8FA9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $8FAA  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $8FAD  BB                          add                                                ; regA = regA + regB
  $8FAE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $8FAF  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8FB3  8E 3F BB                    push_imm_word            $BB3F                     ; " has lost"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $8FB6  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $8FBA  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $8FBD  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $8FBE  D5                          switch                                             ; Jump-table dispatch on regA (handler $EC65) — reads inline (value,target) pairs from the bytecode stream, jumps to the matching target. Variable-length; the disassembler stops the fall-through run here.

  ; ---- gap $8FBF-$9008 (74 bytes, not on any traced path) ----

; ============================================================
; sub $9009   (frame_off=+0, body @ $900E)
; ============================================================
  $900E  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9011  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $9012  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9013  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9016  BB                          add                                                ; regA = regA + regB
  $9017  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9018  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $901C  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $901F  D0                          incA                                               ; regA += 1
  $9020  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9021  8E 62 BB                    push_imm_word            $BB62                     ; " from\nfief %2d has\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9024  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9028  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $902B  D0                          incA                                               ; regA += 1
  $902C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $902D  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $9031  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9032  D7 02 59                    branch_nz_abs            $5902                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9035  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9036  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9039  BB                          add                                                ; regA = regA + regB
  $903A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $903B  8E 76 BB                    push_imm_word            $BB76                     ; "invaded %s\nin fief %2d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $903E  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9042  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9045  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9046   (frame_off=-6, body @ $904B)
; ============================================================
  $904B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $904C  A8 79 6F                    loadA_mem_word           $6F79                     ; regA = word read using <word> as address (handler $EA75)
  $904F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9052  8C 67 6D                    loadB_imm_word           $6D67                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9055  BB                          add                                                ; regA = regA + regB
  $9056  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9057  D7 6F 90                    branch_nz_abs            $906F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $905A  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $905D  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9060  BB                          add                                                ; regA = regA + regB
  $9061  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9062  D8 6F 90                    branch_z_abs             $906F                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9065  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $9066  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $906A  57                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 7)
  $906B  BD                          shl_by_regB                                        ; regA <<= regB  (shift left by regB count)
  $906C  D6 70 90                    jump_abs                 $9070                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$906F  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
 >$9070  A9 65 6F                    storeA_mem_byte          $6F65                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $9073  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9076  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9078  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9079  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $907C  BB                          add                                                ; regA = regA + regB
  $907D  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $907E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $907F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9080  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9081  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9082  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9084  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9085  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9086  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $908A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $908B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $908C  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $908D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $908E  D2                          aslA                                               ; regA <<= 1
  $908F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9090  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9094  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9095  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9096  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $9097  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $9098  D8 9D 90                    branch_z_abs             $909D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $909B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $909C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$909D  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $90A0  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $90A2  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $90A3  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $90A6  BB                          add                                                ; regA = regA + regB
  $90A7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $90A8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $90A9  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $90AC  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $90AE  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $90AF  8C 07 70                    loadB_imm_word           $7007                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $90B2  BB                          add                                                ; regA = regA + regB
  $90B3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $90B4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $90B5  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $90B9  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $90BA  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $90BB  D8 C0 90                    branch_z_abs             $90C0                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $90BE  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $90BF  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$90C0  AA 5F 6F AA                 op_AA_wb                 $6F5F (selected_province_idx), $AA
  $90C4  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $90C5  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $90C6  E9 71 82 04                 host_call                $8271 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $90CA  D8 CF 90                    branch_z_abs             $90CF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $90CD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $90CE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$90CF  A5 65 6F                    loadA_mem_byte           $6F65                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $90D2  D7 E0 90                    branch_nz_abs            $90E0                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $90D5  8D 14                       op_8D_sbyte              +20
  $90D7  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $90DB  8F 32                       addA_imm_sbyte           +50                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $90DD  D6 E8 90                    jump_abs                 $90E8                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$90E0  8D 1E                       op_8D_sbyte              +30
  $90E2  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $90E6  8F 3C                       addA_imm_sbyte           +60                       ; regA += sign-extended signed byte literal (handler $EAD9)
 >$90E8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $90E9  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $90EA  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $90EE  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $90EF  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $90F0  A8 81 6F                    loadA_mem_word           $6F81                     ; regA = word read using <word> as address (handler $EA75)
  $90F3  A8 7D 6F                    loadA_mem_word           $6F7D                     ; regA = word read using <word> as address (handler $EA75)
  $90F6  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $90F7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $90F8  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $90F9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $90FA  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $90FB  BC                          sub                                                ; regA = regA - regB
  $90FC  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $90FD  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $90FE  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $90FF  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9101  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9102  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9103  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9104  BC                          sub                                                ; regA = regA - regB
  $9105  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9106  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9107  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9108  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9109  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $910A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $910B  E9 DE D6 04                 host_call                $D6DE (math32_2arg) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $910F  8B 37                       loadB_imm_byte           +55                       ; regB = byte literal (handler $EABE)
  $9111  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $9112  D8 19 91                    branch_z_abs             $9119                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9115  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9116  D6 30 91                    jump_abs                 $9130                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9119  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $911A  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $911E  8F 32                       addA_imm_sbyte           +50                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9120  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9121  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9122  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9126  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9127  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9128  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9129  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $912A  D1                          decA                                               ; regA -= 1
  $912B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $912C  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$9130  A8 7F 6F                    loadA_mem_word           $6F7F                     ; regA = word read using <word> as address (handler $EA75)
  $9133  AA 7F 6F 0B                 op_AA_wb                 $6F7F, $0B
  $9137  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9138  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9139  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $913A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $913B  BC                          sub                                                ; regA = regA - regB
  $913C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $913D  A4 7D 6E                    loadA_mem_word           $6E7D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9140  D7 4F 91                    branch_nz_abs            $914F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9143  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $9147  8D D9                       op_8D_sbyte              -39
  $9149  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $914A  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $914B  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $914C  D8 59 91                    branch_z_abs             $9159                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$914F  AC 09 90                    host_call_simple         $9009 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9152  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9153  A8 79 6F                    loadA_mem_word           $6F79                     ; regA = word read using <word> as address (handler $EA75)
  $9156  D6 7F 91                    jump_abs                 $917F                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9159  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $915C  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $915E  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $915F  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9162  BB                          add                                                ; regA = regA + regB
  $9163  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9164  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9165  A8 83 6F                    loadA_mem_word           $6F83                     ; regA = word read using <word> as address (handler $EA75)
  $9168  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9169  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $916A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $916B  A8 85 6F                    loadA_mem_word           $6F85                     ; regA = word read using <word> as address (handler $EA75)
  $916E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $916F  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9171  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9172  A8 87 6F                    loadA_mem_word           $6F87                     ; regA = word read using <word> as address (handler $EA75)
  $9175  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9176  A8 79 6F                    loadA_mem_word           $6F79                     ; regA = word read using <word> as address (handler $EA75)
  $9179  AC 09 90                    host_call_simple         $9009 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $917C  AC FD 8D                    host_call_simple         $8DFD {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$917F  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9180  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9181   (frame_off=+0, body @ $9186)
; ============================================================
  $9186  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9187  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9188  E9 12 CA 04                 host_call                $CA12 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $918C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $918D   (frame_off=-20, body @ $9192)
; ============================================================
  $9192  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9195  23                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 3)
  $9196  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9197  25                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 5)
  $9198  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $919C  8D D9                       op_8D_sbyte              -39
  $919E  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $919F  D7 E3 92                    branch_nz_abs            $92E3                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $91A2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $91A3  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $91A4  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $91A7  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $91A9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $91AA  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $91AD  BB                          add                                                ; regA = regA + regB
  $91AE  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $91AF  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $91B3  DA                          bitand                                             ; regA = regA & regB
  $91B4  D7 02 22                    branch_nz_abs            $2202                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $91B7  D0                          incA                                               ; regA += 1
  $91B8  24                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 4)
  $91B9  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $91BA  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $91BB  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $91BC  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $91BD  D0                          incA                                               ; regA += 1
  $91BE  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $91BF  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $91C0  75                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 5)
  $91C1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91C2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91C3  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $91C4  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91C5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91C6  BB                          add                                                ; regA = regA + regB
  $91C7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91C8  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $91C9  75                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 5)
  $91CA  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91CB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91CC  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $91CD  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91CE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91CF  BB                          add                                                ; regA = regA + regB
  $91D0  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91D1  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $91D2  D8 E3 92                    branch_z_abs             $92E3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $91D5  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $91D8  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $91D9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $91DA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91DB  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $91DF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91E0  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $91E1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91E2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91E3  BC                          sub                                                ; regA = regA - regB
  $91E4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91E5  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $91E8  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $91E9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $91EA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91EB  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $91EF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $91F0  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $91F1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $91F2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91F3  BC                          sub                                                ; regA = regA - regB
  $91F4  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $91F5  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $91F6  D8 13 92                    branch_z_abs             $9213                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $91F9  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $91FA  D0                          incA                                               ; regA += 1
  $91FB  24                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 4)
  $91FC  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $91FD  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $91FE  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9201  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9203  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9204  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9207  BB                          add                                                ; regA = regA + regB
  $9208  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9209  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $920A  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $920B  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $920C  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $920D  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $920E  35                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 5)
  $920F  E9 81 91 02                 host_call                $9181 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$9213  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9214  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9215  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $9216  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $921A  D0                          incA                                               ; regA += 1
  $921B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $921C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $921D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $921E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $921F  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $9220  D8 60 92                    branch_z_abs             $9260                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9223  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9224  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9225  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9226  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $922A  8F 32                       addA_imm_sbyte           +50                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $922C  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $922D  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $922F  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9230  D8 36 92                    branch_z_abs             $9236                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9233  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $9235  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$9236  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $9237  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9238  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $9239  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $923A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $923B  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $923F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9240  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9241  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9242  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9243  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9244  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9248  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9249  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $924A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $924B  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $924C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $924D  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $924E  D8 60 92                    branch_z_abs             $9260                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9251  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $9252  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9253  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $9255  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $9256  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9257  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $925B  D8 60 92                    branch_z_abs             $9260                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $925E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $925F  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
 >$9260  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9261  D8 BC 92                    branch_z_abs             $92BC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9264  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9265  D0                          incA                                               ; regA += 1
  $9266  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9267  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $9268  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9269  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $926A  B8                          div_unsigned                                       ; regA = regA / regB  (unsigned quotient)
  $926B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $926C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9270  D0                          incA                                               ; regA += 1
  $9271  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9272  34                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 4)
  $9273  E9 12 CA 04                 host_call                $CA12 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9277  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9278  D8 81 92                    branch_z_abs             $9281                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $927B  8E 8D BB                    push_imm_word            $BB8D                     ; "Counterattacked!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $927E  D6 84 92                    jump_abs                 $9284                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9281  8E BB FB                    push_imm_word            $FBBB                     ; "Enemy morale is\nfalling to pieces!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$9284  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9288  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $928B  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $928C  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$9290  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $9291  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9292  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9293  8A E6 00                    loadA_imm_word           $00E6                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $9296  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9297  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9298  D7 A0 92                    branch_nz_abs            $92A0                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $929B  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $929C  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $929D  D7 F2 92                    branch_nz_abs            $92F2                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$92A0  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $92A1  D7 F2 92                    branch_nz_abs            $92F2                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $92A4  34                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 4)
  $92A5  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $92A6  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $92A7  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $92AA  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $92AD  BB                          add                                                ; regA = regA + regB
  $92AE  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $92AF  D8 D9 92                    branch_z_abs             $92D9                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $92B2  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $92B6  25                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 5)
  $92B7  E4 02                       branch_nz_rel            +2                        ; if regA != 0: ptr3 += signed byte (handler $EBE7)
  $92B9  D6 F2 92                    jump_abs                 $92F2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
  ; ---- overlap: $92BB re-entered mid-stream ----
 >$92BB  92                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
 >$92BC  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $92BD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $92BE  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $92BF  D1                          decA                                               ; regA -= 1
  $92C0  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $92C1  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $92C2  D8 E3 92                    branch_z_abs             $92E3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $92C5  8E 8D BB                    push_imm_word            $BB8D                     ; "Counterattacked!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $92C8  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $92CC  8E 9E BB                    push_imm_word            $BB9E                     ; "\nYou have repelled!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $92CF  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $92D3  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $92D6  D6 90 92                    jump_abs                 $9290                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$92D9  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $92DD  54                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 4)
  $92DE  E4 02                       branch_nz_rel            +2                        ; if regA != 0: ptr3 += signed byte (handler $EBE7)
  $92E0  D6 F2 92                    jump_abs                 $92F2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
  ; ---- overlap: $92E2 re-entered mid-stream ----
 >$92E2  92                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
 >$92E3  35                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 5)
  $92E4  E9 81 91 02                 host_call                $9181 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $92E8  8E B2 BB                    push_imm_word            $BBB2                     ; "Your ninja failed"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $92EB  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $92EF  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$92F2  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $92F6  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $92F7  D8 02 AA                    branch_z_abs             $AA02                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $92FA  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $92FB  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $92FC  E9 36 D8 02                 host_call                $D836 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9300  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9301   (frame_off=+0, body @ $9306)
; ============================================================
  $9306  0E                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9307  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9308  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9309  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $930A  D8 20 93                    branch_z_abs             $9320                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $930D  3E                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $930E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $930F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9310  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9311  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9315  D8 20 93                    branch_z_abs             $9320                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9318  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9319  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $931D  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$9320  0E                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9321  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9322  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9323 (effect_war_combat_prep_a)   (frame_off=+0, body @ $9328)
; ============================================================
  $9328  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9329  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $932B  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $932C  8C 07 70                    loadB_imm_word           $7007                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $932F  BB                          add                                                ; regA = regA + regB
  $9330  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9331  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9332  8E EB FD                    push_imm_word            $FDEB                     ; "You have no rice!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9335  E9 01 93 06                 host_call                $9301 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9339  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $933A (effect_war_combat_prep_b)   (frame_off=+0, body @ $933F)
; ============================================================
  $933F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9340  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9342  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9343  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9346  BB                          add                                                ; regA = regA + regB
  $9347  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9348  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9349  8E FD FD                    push_imm_word            $FDFD                     ; "You have no\nsoldiers!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $934C  E9 01 93 06                 host_call                $9301 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9350  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9351 (effect_war_combat_prep_c)   (frame_off=+0, body @ $9356)
; ============================================================
  $9356  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9357  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9359  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $935A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $935D  BB                          add                                                ; regA = regA + regB
  $935E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $935F  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9360  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9363  E9 01 93 06                 host_call                $9301 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9367  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9368 (effect_war_combat_prep_d)   (frame_off=-2, body @ $936D)
; ============================================================
  $936D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $936E  E9 72 D9 02                 host_call                $D972 (war_helper_d972) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9372  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9373  D8 80 93                    branch_z_abs             $9380                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9376  8E D6 FA                    push_imm_word            $FAD6                     ; "Lord, you are too\nweak for battle!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9379  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $937D  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$9380  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9381  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9382   (frame_off=-2, body @ $9387)
; ============================================================
  $9387  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9388  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $938A  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $938B  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $938E  BB                          add                                                ; regA = regA + regB
  $938F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9390  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9391  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9392  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9393  D7 98 93                    branch_nz_abs            $9398                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9396  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9397  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9398  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9399  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $939B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $939C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $939D   (frame_off=+0, body @ $93A2)
; ============================================================
  $93A2  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $93A3  E9 82 93 02                 host_call                $9382 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93A7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $93A8  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $93A9  E9 82 93 02                 host_call                $9382 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93AD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $93AE  E9 DE D6 04                 host_call                $D6DE (math32_2arg) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93B2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $93B3   (frame_off=-2, body @ $93B8)
; ============================================================
  $93B8  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $93B9  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $93BA  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $93BB  D6 D2 93                    jump_abs                 $93D2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$93BE  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $93BF  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $93C0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $93C1  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $93C2  E9 9D 93 04                 host_call                $939D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93C6  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $93C8  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $93C9  D8 CF 93                    branch_z_abs             $93CF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $93CC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $93CD  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $93CE  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$93CF  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $93D0  D0                          incA                                               ; regA += 1
  $93D1  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
 >$93D2  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $93D3  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $93D4  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $93D7  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $93D8  D7 BE 93                    branch_nz_abs            $93BE                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $93DB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $93DC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $93DD   (frame_off=+0, body @ $93E2)
; ============================================================
  $93E2  8D 64                       op_8D_sbyte              +100
  $93E4  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93E8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $93E9  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $93EA  AA 5F 6F 3C                 op_AA_wb                 $6F5F (selected_province_idx), $3C
  $93EE  E9 50 82 06                 host_call                $8250 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93F2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $93F3  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $93F4  D7 12 94                    branch_nz_abs            $9412                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $93F7  8D 64                       op_8D_sbyte              +100
  $93F9  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $93FD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $93FE  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $93FF  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9402  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9403  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9404  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9408  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9409  E9 50 82 06                 host_call                $8250 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $940D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $940E  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $940F  D8 1D 94                    branch_z_abs             $941D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$9412  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9415  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9416  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9417  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $9419  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $941A  D7 21 94                    branch_nz_abs            $9421                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$941D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $941E  D6 22 94                    jump_abs                 $9422                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9421  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$9422  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9423   (frame_off=-2, body @ $9428)
; ============================================================
  $9428  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9429  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $942A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $942B  D6 52 94                    jump_abs                 $9452                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$942E  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $942F  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9433  D8 4F 94                    branch_z_abs             $944F                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9436  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9437  E9 DD 93 02                 host_call                $93DD {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $943B  D7 4F 94                    branch_nz_abs            $944F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $943E  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $943F  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9440  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9441  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9442  E9 9D 93 04                 host_call                $939D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9446  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $9448  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $9449  D8 4F 94                    branch_z_abs             $944F                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $944C  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $944D  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $944E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$944F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9450  D0                          incA                                               ; regA += 1
  $9451  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
 >$9452  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9453  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9454  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9457  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $9458  D7 2E 94                    branch_nz_abs            $942E                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $945B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $945C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $945D   (frame_off=-2, body @ $9462)
; ============================================================
  $9462  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9463  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9466  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $9467  D8 96 94                    branch_z_abs             $9496                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $946A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $946B  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $946C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $946D  D6 8D 94                    jump_abs                 $948D                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9470  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9471  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9475  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $9476  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $9477  D8 8A 94                    branch_z_abs             $948A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $947A  AA 5F 6F 3B                 op_AA_wb                 $6F5F (selected_province_idx), $3B
  $947E  E9 9D 93 04                 host_call                $939D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9482  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $9484  C9                          cmp_ult                                            ; regA = (regA < regB) ? 1 : 0   (unsigned)
  $9485  D8 96 94                    branch_z_abs             $9496                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9488  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9489  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$948A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $948B  D0                          incA                                               ; regA += 1
  $948C  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
 >$948D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $948E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $948F  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9492  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $9493  D7 70 94                    branch_nz_abs            $9470                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$9496  8A FF 00                    loadA_imm_word           $00FF                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $9499  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $949A   (frame_off=-12, body @ $949F)
; ============================================================
  $949F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $94A2  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $94A3  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $94A6  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $94A8  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $94A9  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $94AC  BB                          add                                                ; regA = regA + regB
  $94AD  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $94AE  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $94AF  E9 72 D9 02                 host_call                $D972 (war_helper_d972) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $94B3  D7 C3 94                    branch_nz_abs            $94C3                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $94B6  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $94B7  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $94B9  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $94BA  D8 C3 94                    branch_z_abs             $94C3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $94BD  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $94BE  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $94BF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $94C0  D7 C5 94                    branch_nz_abs            $94C5                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$94C3  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $94C4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$94C5  AC D7 DA                    host_call_simple         $DAD7 (combat_helper_dad7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $94C8  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $94CB  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $94CC  E9 3A DD 04                 host_call                $DD3A (combat_helper_dd3a) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $94D0  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $94D3  E9 B3 93 02                 host_call                $93B3 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $94D7  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $94D8  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $94D9  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $94DC  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $94DD  D8 E2 94                    branch_z_abs             $94E2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $94E0  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $94E1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$94E2  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $94E5  E9 23 94 02                 host_call                $9423 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $94E9  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $94EA  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $94ED  8C 7F 6E                    loadB_imm_word           $6E7F                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $94F0  BB                          add                                                ; regA = regA + regB
  $94F1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $94F2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $94F3  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $94F6  E9 5D 94 04                 host_call                $945D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $94FA  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $94FB  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $94FC  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $94FF  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $9500  D8 05 95                    branch_z_abs             $9505                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9503  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9504  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$9505  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9508  53                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 3)
  $9509  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $950A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $950B  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $950F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9510  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9511  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9512  E9 9D 93 04                 host_call                $939D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9516  8F F6                       addA_imm_sbyte           -10                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9518  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9519  BC                          sub                                                ; regA = regA - regB
  $951A  8B 3C                       loadB_imm_byte           +60                       ; regB = byte literal (handler $EABE)
  $951C  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $951D  D7 2D 95                    branch_nz_abs            $952D                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9520  8D 64                       op_8D_sbyte              +100
  $9522  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9526  D8 2D 95                    branch_z_abs             $952D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9529  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $952A  D6 2E 95                    jump_abs                 $952E                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$952D  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$952E  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $952F  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9530  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9534  D8 47 95                    branch_z_abs             $9547                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9537  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9538  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9539  E9 9D 93 04                 host_call                $939D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $953D  8B 2F                       loadB_imm_byte           +47                       ; regB = byte literal (handler $EABE)
  $953F  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $9540  D8 47 95                    branch_z_abs             $9547                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9543  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9544  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $9545  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9546  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$9547  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9548  8B 1E                       loadB_imm_byte           +30                       ; regB = byte literal (handler $EABE)
  $954A  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $954B  D8 61 95                    branch_z_abs             $9561                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $954E  8D 50                       op_8D_sbyte              +80
  $9550  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9554  8F 5A                       addA_imm_sbyte           +90                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9556  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9557  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $955A  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $955B  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $955C  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $955D  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $955E  D7 84 95                    branch_nz_abs            $9584                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$9561  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9562  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9566  D7 71 95                    branch_nz_abs            $9571                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9569  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $956A  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $956E  D8 84 95                    branch_z_abs             $9584                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$9571  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9572  D8 84 95                    branch_z_abs             $9584                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9575  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9576  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9579  AC 46 90                    host_call_simple         $9046 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $957C  D8 84 95                    branch_z_abs             $9584                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $957F  AC FC 81                    host_call_simple         $81FC (effect_war_a) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9582  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9583  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9584  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9585  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9586 (effect_train)   (frame_off=-4, body @ $958B)
; ============================================================
  $958B  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $958E  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $958F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9592  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9594  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9595  8C 15 70                    loadB_imm_word           $7015                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9598  BB                          add                                                ; regA = regA + regB
  $9599  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $959A  8D 14                       op_8D_sbyte              +20
  $959C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $95A0  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $95A1  D2                          aslA                                               ; regA <<= 1
  $95A2  D2                          aslA                                               ; regA <<= 1
  $95A3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95A4  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95A5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $95A6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95A7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $95A8  BB                          add                                                ; regA = regA + regB
  $95A9  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $95AA  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $95AB  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $95AF  8F 5A                       addA_imm_sbyte           +90                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $95B1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95B2  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $95B3  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $95B4  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $95B5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95B6  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $95B7  75                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 5)
  $95B8  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $95B9  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $95BA  BB                          add                                                ; regA = regA + regB
  $95BB  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $95BC  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $95BD  D8 C7 95                    branch_z_abs             $95C7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $95C0  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $95C1  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95C2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $95C3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95C4  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $95C5  BB                          add                                                ; regA = regA + regB
  $95C6  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$95C7  4B                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $95C8  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $95C9   (frame_off=-4, body @ $95CE)
; ============================================================
  $95CE  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $95CF  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $95D3  D7 DE 95                    branch_nz_abs            $95DE                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $95D6  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $95D9  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $95DA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95DB  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $95DC  D0                          incA                                               ; regA += 1
  $95DD  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$95DE  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $95E1  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $95E3  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $95E4  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $95E7  BB                          add                                                ; regA = regA + regB
  $95E8  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $95E9  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $95EA  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $95EB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95EC  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $95EE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $95EF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95F0  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95F1  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $95F2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $95F3  BC                          sub                                                ; regA = regA - regB
  $95F4  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $95F5  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $95F6  D8 0A 96                    branch_z_abs             $960A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $95F9  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95FA  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $95FC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $95FD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $95FE  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $95FF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9600  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9601  BC                          sub                                                ; regA = regA - regB
  $9602  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9603  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9604  E9 79 83 04                 host_call                $8379 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9608  4E                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9609  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$960A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $960B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $960C   (frame_off=-9, body @ $9611)
; ============================================================
  $9611  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9614  D0                          incA                                               ; regA += 1
  $9615  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9616  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $961A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $961B  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $961E  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $961F  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9620  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9621  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9622  BB                          add                                                ; regA = regA + regB
  $9623  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9624  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9627  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9629  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $962A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $962D  BB                          add                                                ; regA = regA + regB
  $962E  85 F7                       op_85_byte               $F7
  $9630  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9632  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9633  58                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 8)
  $9634  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9635  D8 93 96                    branch_z_abs             $9693                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9638  89 32                       loadA_imm_sbyte          +50                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $963A  A2 FB                       op_A0_A3_byte            $FB                       ; 1-byte operand; indexed load/store via ptr0 (handlers $EAED-$EB1E)
  $963C  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $963D  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $963E  85 F9                       op_85_byte               $F9
 >$9640  DE FB                       loadA_frameaddr          $FB                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $9642  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $9643  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9644  81 F9                       op_81_byte               $F9
  $9646  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9647  BB                          add                                                ; regA = regA + regB
  $9648  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9649  45                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $964A  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $964B  81 F9                       op_81_byte               $F9
  $964D  D0                          incA                                               ; regA += 1
  $964E  85 F9                       op_85_byte               $F9
  $9650  81 F9                       op_81_byte               $F9
  $9652  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $9653  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $9654  D7 40 96                    branch_nz_abs            $9640                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9657  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9658  85 F9                       op_85_byte               $F9
 >$965A  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $965B  DE FB                       loadA_frameaddr          $FB                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $965D  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $965E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $965F  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $9660  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9664  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9665  BB                          add                                                ; regA = regA + regB
  $9666  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9667  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9668  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9669  BB                          add                                                ; regA = regA + regB
  $966A  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $966B  81 F9                       op_81_byte               $F9
  $966D  D0                          incA                                               ; regA += 1
  $966E  85 F9                       op_85_byte               $F9
  $9670  81 F9                       op_81_byte               $F9
  $9672  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $9673  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $9674  D7 5A 96                    branch_nz_abs            $965A                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9677  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $9678  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $967A  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $967B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $967C  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $967D  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9681  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9682  BB                          add                                                ; regA = regA + regB
  $9683  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9684  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9685  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9686  BB                          add                                                ; regA = regA + regB
  $9687  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9688  DE FB                       loadA_frameaddr          $FB                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $968A  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $968B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $968C  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $9690  AF E2                       op_AF_byte               $E2
  $9692  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
 >$9693  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $9694  81 F7                       op_81_byte               $F7
  $9696  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9697  D0                          incA                                               ; regA += 1
  $9698  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9699  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $969D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $969E  81 F7                       op_81_byte               $F7
  $96A0  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $96A1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96A2  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $96A3  BB                          add                                                ; regA = regA + regB
  $96A4  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $96A5  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $96A6  81 F7                       op_81_byte               $F7
  $96A8  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $96A9  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $96AA  D0                          incA                                               ; regA += 1
  $96AB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96AC  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $96B0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96B1  81 F7                       op_81_byte               $F7
  $96B3  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $96B4  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $96B5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96B6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $96B7  BB                          add                                                ; regA = regA + regB
  $96B8  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $96B9  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $96BA  81 F7                       op_81_byte               $F7
  $96BC  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $96BE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $96BF  D0                          incA                                               ; regA += 1
  $96C0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96C1  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $96C5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96C6  81 F7                       op_81_byte               $F7
  $96C8  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $96CA  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $96CB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $96CC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $96CD  BB                          add                                                ; regA = regA + regB
  $96CE  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $96CF  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $96D0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $96D1 (driver_move)   (frame_off=-8, body @ $96D6)
; ============================================================
  $96D6  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $96DA  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $96DB  93                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $96DC  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $96DD  D7 E2 96                    branch_nz_abs            $96E2                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $96E0  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $96E1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$96E2  AC D7 DA                    host_call_simple         $DAD7 (combat_helper_dad7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $96E5  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $96E8  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $96E9  E9 3A DD 04                 host_call                $DD3A (combat_helper_dd3a) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $96ED  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $96F0  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $96F1  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $96F5  D8 BF 97                    branch_z_abs             $97BF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $96F8  8E CD F9                    push_imm_word            $F9CD                     ; "\nMove where?\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $96FB  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $96FF  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9702  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9706  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9709  D8 BF 97                    branch_z_abs             $97BF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $970C  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $970F  D1                          decA                                               ; regA -= 1
  $9710  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9713  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9716  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9719  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $971B  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $971C  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $971F  BB                          add                                                ; regA = regA + regB
  $9720  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9721  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9724  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9726  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9727  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $972A  BB                          add                                                ; regA = regA + regB
  $972B  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $972C  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $972D  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $972F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9730  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9731  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9732  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9734  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9735  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9736  BC                          sub                                                ; regA = regA - regB
  $9737  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9738  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9739  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $973B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $973C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $973D  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9741  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9742  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9743  D8 B5 97                    branch_z_abs             $97B5                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9746  8E DB F9                    push_imm_word            $F9DB                     ; "How many men"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9749  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $974D  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $974E  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $974F  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9753  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9754  D8 BF 97                    branch_z_abs             $97BF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9757  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $975A  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $975D  BB                          add                                                ; regA = regA + regB
  $975E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $975F  D8 97 97                    branch_z_abs             $9797                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9762  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $9766  68                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 8)
  $9767  93                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $9768  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $9769  D7 97 97                    branch_nz_abs            $9797                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $976C  8E E8 F9                    push_imm_word            $F9E8                     ; "Will you lead\nthem personally"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $976F  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9773  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9776  D8 97 97                    branch_z_abs             $9797                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9779  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $977C  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $977F  BB                          add                                                ; regA = regA + regB
  $9780  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9781  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9782  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9783  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9786  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9789  BB                          add                                                ; regA = regA + regB
  $978A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $978B  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $978C  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $978D  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9790  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9793  BB                          add                                                ; regA = regA + regB
  $9794  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9795  45                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9796  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$9797  68                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 8)
  $9798  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $979C  8E 06 FA                    push_imm_word            $FA06                     ; "They have\narrived safely"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $979F  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $97A3  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $97A6  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $97A7  E9 A5 8C 02                 host_call                $8CA5 (effect_move) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $97AB  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $97AC  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $97B0  FE                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $97B1  DF 04                       loadB_frameaddr          $04                       ; regB = ptr2 + byte  (address of a frame local; handler $EAA0)
  $97B3  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $97B4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$97B5  8E C4 BB                    push_imm_word            $BBC4                     ; "That fief can`t\nhold more men"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $97B8  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $97BC  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$97BF  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $97C0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $97C1   (frame_off=-2, body @ $97C6)
; ============================================================
  $97C6  8A 4F 6F                    loadA_imm_word           $6F4F                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $97C9  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $97CA  D6 E0 97                    jump_abs                 $97E0                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$97CD  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $97CE  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $97CF  1C                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $97D0  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $97D1  D8 D7 97                    branch_z_abs             $97D7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $97D4  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $97D5  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $97D6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$97D7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $97D8  D0                          incA                                               ; regA += 1
  $97D9  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $97DA  D1                          decA                                               ; regA -= 1
  $97DB  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $97DC  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $97DD  2D                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $97DE  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
 >$97E0  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $97E1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $97E2  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $97E5  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $97E6  D7 CD 97                    branch_nz_abs            $97CD                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $97E9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $97EA   (frame_off=+0, body @ $97EF)
; ============================================================
  $97EF  8D 64                       op_8D_sbyte              +100
  $97F1  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $97F5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $97F6  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $97F7  D8 08 98                    branch_z_abs             $9808                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $97FA  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $97FB  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $97FC  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9800  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9801  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9804  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9805  D6 0D 98                    jump_abs                 $980D                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9808  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9809  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $980A  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  ; ---- overlap: $980D re-entered mid-stream ----
 >$980D  E9 50 82 06                 host_call                $8250 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  ; ---- overlap: $980E re-entered mid-stream ----
  $980E  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $980F  82 06 B4                    op_82_bb                 $06, $B4
  ; ---- overlap: $9811 re-entered mid-stream ----
  $9811  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9812  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $9813  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9814 (effect_war_e)   (frame_off=-2, body @ $9819)
; ============================================================
  $9819  8A 4F 6F                    loadA_imm_word           $6F4F                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $981C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $981D  D6 46 98                    jump_abs                 $9846                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9820  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9821  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9822  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9823  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9824  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9825  E9 EA 97 04                 host_call                $97EA {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9829  D7 3B 98                    branch_nz_abs            $983B                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $982C  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $982D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $982E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $982F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9830  E9 EA 97 04                 host_call                $97EA {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9834  D7 3B 98                    branch_nz_abs            $983B                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9837  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9838  D6 3C 98                    jump_abs                 $983C                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$983B  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$983C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $983D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $983E  D0                          incA                                               ; regA += 1
  $983F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9840  D1                          decA                                               ; regA -= 1
  $9841  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9842  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9843  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $9844  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
 >$9846  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9847  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9848  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $984B  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $984C  D7 20 98                    branch_nz_abs            $9820                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $984F  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9850 (driver_war)   (frame_off=-22, body @ $9855)
; ============================================================
  $9855  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9858  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9859  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $985A  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $985D  BB                          add                                                ; regA = regA + regB
  $985E  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $985F  D8 6C 98                    branch_z_abs             $986C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9862  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9863  E9 68 93 02                 host_call                $9368 (effect_war_combat_prep_d) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9867  D8 6C 98                    branch_z_abs             $986C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $986A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $986B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$986C  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $986D  E9 51 93 02                 host_call                $9351 (effect_war_combat_prep_c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9871  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9874  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9875  E9 23 93 02                 host_call                $9323 (effect_war_combat_prep_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9879  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $987C  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $987D  E9 3A 93 02                 host_call                $933A (effect_war_combat_prep_b) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9881  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9884  AC D7 DA                    host_call_simple         $DAD7 (combat_helper_dad7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9887  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $988A  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $988B  E9 3A DD 04                 host_call                $DD3A (combat_helper_dd3a) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $988F  DE EA                       loadA_frameaddr          $EA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $9891  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $9892  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9893  E9 14 98 02                 host_call                $9814 (effect_war_e) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9897  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $989A  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $989B  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $989F  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $98A2  8E 1F FA                    push_imm_word            $FA1F                     ; "\nAttack where?\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $98A5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $98A9  8E 4F 6F                    push_imm_word            $6F4F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $98AC  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $98B0  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $98B3  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $98B6  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $98B9  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $98BC  D1                          decA                                               ; regA -= 1
  $98BD  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $98C0  DE EA                       loadA_frameaddr          $EA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $98C2  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $98C3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $98C4  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $98C8  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $98C9  97                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $98CA  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $98CB  D8 DA 98                    branch_z_abs             $98DA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $98CE  8E 29 FE                    push_imm_word            $FE29                     ; "They are your\nallies!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $98D1  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $98D5  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $98D8  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $98D9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$98DA  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $98DD  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $98DF  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $98E0  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $98E3  BB                          add                                                ; regA = regA + regB
  $98E4  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $98E5  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $98E6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $98E7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $98E8  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $98E9  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $98EB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $98EC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $98ED  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $98F1  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $98F2  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $98F3  E9 72 D9 02                 host_call                $D972 (war_helper_d972) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $98F7  D8 06 99                    branch_z_abs             $9906                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $98FA  44                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $98FB  CD                          swap_AB                                            ; regA <-> regB
  $98FC  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $98FD  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $98FE  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $98FF  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9900  D7 06 99                    branch_nz_abs            $9906                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9903  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9904  D0                          incA                                               ; regA += 1
  $9905  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$9906  8E 2F FA                    push_imm_word            $FA2F                     ; "How many men"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9909  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $990D  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $990E  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $990F  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9913  A8 81 6F                    loadA_mem_word           $6F81                     ; regA = word read using <word> as address (handler $EA75)
  $9916  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9919  8E 3C FA                    push_imm_word            $FA3C                     ; "How much rice\nwill they take"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $991C  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9920  AA 81 6F 0B                 op_AA_wb                 $6F81, $0B
  $9924  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9925  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9926  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9927  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $992B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $992C  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $992D  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9931  A8 7F 6F                    loadA_mem_word           $6F7F                     ; regA = word read using <word> as address (handler $EA75)
  $9934  D8 98 99                    branch_z_abs             $9998                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9937  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9938  A9 65 6F                    storeA_mem_byte          $6F65                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $993B  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $993C  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $993F  BB                          add                                                ; regA = regA + regB
  $9940  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9941  D8 59 99                    branch_z_abs             $9959                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9944  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9947  8E 59 FA                    push_imm_word            $FA59                     ; "Will you lead\nthem personally"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $994A  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $994E  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9951  D8 59 99                    branch_z_abs             $9959                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9954  89 80                       loadA_imm_sbyte          -128                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $9956  A9 65 6F                    storeA_mem_byte          $6F65                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
 >$9959  A4 81 6F                    loadA_mem_word           $6F81                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $995C  A8 7D 6F                    loadA_mem_word           $6F7D                     ; regA = word read using <word> as address (handler $EA75)
  $995F  AA 7D 6F 0B                 op_AA_wb                 $6F7D, $0B
  $9963  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9964  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9965  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9966  BC                          sub                                                ; regA = regA - regB
  $9967  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9968  AA 7F 6F 0B                 op_AA_wb                 $6F7F, $0B
  $996C  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $996D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $996E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $996F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9970  BC                          sub                                                ; regA = regA - regB
  $9971  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9972  AA 81 6F 0B                 op_AA_wb                 $6F81, $0B
  $9976  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9978  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9979  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $997A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $997B  BC                          sub                                                ; regA = regA - regB
  $997C  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $997D  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $997E  A8 79 6F                    loadA_mem_word           $6F79                     ; regA = word read using <word> as address (handler $EA75)
  $9981  89 8F                       loadA_imm_sbyte          -113                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $9983  CD                          swap_AB                                            ; regA <-> regB
  $9984  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $9987  DA                          bitand                                             ; regA = regA & regB
  $9988  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $998B  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $998E  6D                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 13)
  $998F  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9993  AC FC 81                    host_call_simple         $81FC (effect_war_a) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9996  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9997  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9998  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9999  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $999A (driver_tax)   (frame_off=-14, body @ $999F)
; ============================================================
  $999F  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $99A2  8C 2D 6D                    loadB_imm_word           $6D2D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $99A5  BB                          add                                                ; regA = regA + regB
  $99A6  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $99A7  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $99AA  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $99AC  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $99AD  8C 0D 70                    loadB_imm_word           $700D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $99B0  BB                          add                                                ; regA = regA + regB
  $99B1  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $99B2  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $99B3  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $99B4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $99B5  8E E2 BB                    push_imm_word            $BBE2                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $99B8  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $99BC  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $99BD  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $99BE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $99BF  8E 77 FA                    push_imm_word            $FA77                     ; "Tax is %d\nEnter new tax"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $99C2  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $99C6  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $99C9  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $99CC  AC 12 DB                    host_call_simple         $DB12 (tax_helper_db12) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $99CF  D8 D7 99                    branch_z_abs             $99D7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $99D2  89 1E                       loadA_imm_sbyte          +30                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $99D4  D6 D9 99                    jump_abs                 $99D9                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$99D7  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
 >$99D9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $99DA  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $99DB  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $99DF  25                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 5)
  $99E0  D8 5B 9A                    branch_z_abs             $9A5B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $99E3  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $99E4  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $99E5  15                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 5)
  $99E6  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $99E7  D8 5B 9A                    branch_z_abs             $9A5B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $99EA  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $99EB  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $99EC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $99ED  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $99EE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $99EF  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $99F0  D8 FF 99                    branch_z_abs             $99FF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $99F3  8D 13                       op_8D_sbyte              +19
  $99F5  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $99F9  8E AB FA                    push_imm_word            $FAAB                     ; "The peasants are\nprotesting!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $99FC  D6 08 9A                    jump_abs                 $9A08                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$99FF  8D 12                       op_8D_sbyte              +18
  $9A01  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A05  8E 8F FA                    push_imm_word            $FA8F                     ; "The peasants are\ndelighted!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$9A08  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A0C  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9A0D  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9A0E  15                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 5)
  $9A0F  BC                          sub                                                ; regA = regA - regB
  $9A10  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9A11  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9A12  D8 19 9A                    branch_z_abs             $9A19                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9A15  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9A16  D6 1B 9A                    jump_abs                 $9A1B                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9A19  89 FF                       loadA_imm_sbyte          -1                        ; regA = sign-extended signed byte literal (handler $EAAF)
 >$9A1B  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $9A1C  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $9A1D  8B FF                       loadB_imm_byte           -1                        ; regB = byte literal (handler $EABE)
  $9A1F  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $9A20  D8 2D 9A                    branch_z_abs             $9A2D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9A23  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9A26  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9A27  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9A28  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $9A29  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $9A2A  D7 38 9A                    branch_nz_abs            $9A38                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$9A2D  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $9A2E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9A2F  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9A32  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9A33  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9A34  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9A35  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9A36  BB                          add                                                ; regA = regA + regB
  $9A37  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$9A38  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9A39  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9A3A  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9A3B  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $9A3C  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $9A3D  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9A3E  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9A3F  15                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 5)
  $9A40  BC                          sub                                                ; regA = regA - regB
  $9A41  25                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 5)
  $9A42  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9A43  35                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 5)
  $9A44  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9A45  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9A46  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9A47  E9 D6 82 04                 host_call                $82D6 (effect_tax) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A4B  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9A4C  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9A4D  35                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 5)
  $9A4E  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9A4F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9A50  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9A51  E9 D6 82 04                 host_call                $82D6 (effect_tax) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A55  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9A56  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9A59  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9A5A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9A5B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9A5C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9A5D (driver_send)   (frame_off=-12, body @ $9A62)
; ============================================================
  $9A62  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9A63  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A67  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9A6A  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $9A6B  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A6F  D8 7C 9B                    branch_z_abs             $9B7C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9A72  8E C8 FA                    push_imm_word            $FAC8                     ; "\nSend where? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9A75  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A79  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9A7C  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9A80  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9A81  D8 7C 9B                    branch_z_abs             $9B7C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9A84  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9A85  D1                          decA                                               ; regA -= 1
  $9A86  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9A87  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9A89  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9A8A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9A8D  BB                          add                                                ; regA = regA + regB
  $9A8E  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $9A8F  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9A91  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9A92  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $9A93  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9A94  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9A95  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9A96  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9A99  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $9A9D  23                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 3)
  $9A9E  93                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $9A9F  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $9AA0  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9AA1  D8 E3 9A                    branch_z_abs             $9AE3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9AA4  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9AA5  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9AA6  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9AA7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9AA8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9AA9  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $9AAA  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9AAE  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9AAF  D8 D9 9A                    branch_z_abs             $9AD9                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9AB2  8E FF FA                    push_imm_word            $FAFF                     ; "Rice"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9AB5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9AB9  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9ABA  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $9ABD  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9ABE  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9ABF  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9AC3  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9AC4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9AC5  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $9AC8  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9AC9  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9ACA  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $9ACB  D8 E3 9A                    branch_z_abs             $9AE3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9ACE  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9AD1  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9AD2  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9AD3  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $9AD4  D8 E3 9A                    branch_z_abs             $9AE3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9AD7  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9AD8  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9AD9  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9ADC  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9AE0  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$9AE3  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $9AE7  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $9AE8  93                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $9AE9  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $9AEA  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9AEB  D8 2C 9B                    branch_z_abs             $9B2C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9AEE  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9AEF  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9AF0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9AF1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9AF2  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $9AF3  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9AF7  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9AF8  D8 22 9B                    branch_z_abs             $9B22                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9AFB  8E FA FA                    push_imm_word            $FAFA                     ; "Gold"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9AFE  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B02  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9B03  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $9B06  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9B07  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9B08  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B0C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9B0D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9B0E  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $9B11  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9B12  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9B13  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $9B14  D8 2C 9B                    branch_z_abs             $9B2C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9B17  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9B1A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9B1B  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $9B1C  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $9B1D  D8 2C 9B                    branch_z_abs             $9B2C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9B20  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9B21  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9B22  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9B25  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B29  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$9B2C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9B2D  D7 34 9B                    branch_nz_abs            $9B34                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9B30  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9B31  D8 7C 9B                    branch_z_abs             $9B7C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$9B34  8E E3 BB                    push_imm_word            $BBE3                     ; "Is this OK"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9B37  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B3B  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9B3E  D8 7C 9B                    branch_z_abs             $9B7C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9B41  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9B42  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9B43  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9B44  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9B45  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9B46  BB                          add                                                ; regA = regA + regB
  $9B47  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9B48  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9B49  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9B4A  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9B4B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9B4C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9B4D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9B4E  BB                          add                                                ; regA = regA + regB
  $9B4F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9B50  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9B53  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9B55  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9B56  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9B59  BB                          add                                                ; regA = regA + regB
  $9B5A  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $9B5B  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9B5C  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9B5D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9B5E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9B5F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9B60  BC                          sub                                                ; regA = regA - regB
  $9B61  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9B62  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9B63  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $9B64  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $9B65  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9B66  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9B67  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9B68  BC                          sub                                                ; regA = regA - regB
  $9B69  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9B6A  8D 11                       op_8D_sbyte              +17
  $9B6C  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B70  8E 04 FB                    push_imm_word            $FB04                     ; "The supplies have\narrived safely"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9B73  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B77  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9B7A  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9B7B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9B7C  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9B7D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9B7E (driver_dam)   (frame_off=-8, body @ $9B83)
; ============================================================
  $9B83  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9B86  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9B88  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9B89  8C 09 70                    loadB_imm_word           $7009                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9B8C  BB                          add                                                ; regA = regA + regB
  $9B8D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9B8E  D7 9D 9B                    branch_nz_abs            $9B9D                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9B91  8E 13 FE                    push_imm_word            $FE13                     ; "You have no\npeasants!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9B94  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9B98  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9B9B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9B9C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9B9D  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9BA0  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9BA2  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9BA3  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9BA6  BB                          add                                                ; regA = regA + regB
  $9BA7  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9BA8  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $9BA9  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9BAA  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9BAB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9BAC  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $9BAE  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $9BAF  D8 BE 9B                    branch_z_abs             $9BBE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9BB2  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9BB5  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BB9  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9BBC  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9BBD  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9BBE  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9BBF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9BC0  D7 CF 9B                    branch_nz_abs            $9BCF                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9BC3  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9BC6  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BCA  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9BCD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9BCE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9BCF  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9BD0  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $9BD1  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9BD2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9BD3  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $9BD5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9BD6  BC                          sub                                                ; regA = regA - regB
  $9BD7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9BD8  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $9BD9  E9 D8 87 02                 host_call                $87D8 (effect_dam) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BDD  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9BDE  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9BDF  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $9BE0  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $9BE1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9BE2  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9BE3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9BE4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9BE5  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BE9  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9BEA  8E EE BB                    push_imm_word            $BBEE                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9BED  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BF1  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9BF2  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9BF3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9BF4  8E EF BB                    push_imm_word            $BBEF                     ; "Dams value"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9BF7  8E 25 FB                    push_imm_word            $FB25                     ; "%s is %d\nSpend how much\non it"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9BFA  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9BFE  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9BFF  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9C00  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C04  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9C05  D8 4E 9C                    branch_z_abs             $9C4E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9C08  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9C09  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9C0A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9C0B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9C0C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9C0D  BC                          sub                                                ; regA = regA - regB
  $9C0E  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9C0F  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $9C10  E9 D8 87 02                 host_call                $87D8 (effect_dam) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C14  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9C15  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9C16  D2                          aslA                                               ; regA <<= 1
  $9C17  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9C18  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $9C19  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9C1A  D7 20 9C                    branch_nz_abs            $9C20                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9C1D  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9C1E  D0                          incA                                               ; regA += 1
  $9C1F  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
 >$9C20  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9C21  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $9C22  E9 7D 88 04                 host_call                $887D (helper_dam_rounding) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C26  8D 15                       op_8D_sbyte              +21
  $9C28  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C2C  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9C2D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9C2E  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $9C30  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9C31  D8 38 9C                    branch_z_abs             $9C38                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9C34  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9C35  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $9C37  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$9C38  8E FA BB                    push_imm_word            $BBFA                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9C3B  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C3F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9C40  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9C41  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9C42  8E 43 FB                    push_imm_word            $FB43                     ; "Dams value is\nnow %d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9C45  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C49  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9C4C  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9C4D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9C4E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9C4F (driver_pact)   (frame_off=-6, body @ $9C54)
; ============================================================
  $9C54  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9C57  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9C59  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9C5A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9C5D  BB                          add                                                ; regA = regA + regB
  $9C5E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9C5F  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9C60  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9C61  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9C62  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C66  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9C69  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $9C6A  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C6E  D8 3B 9D                    branch_z_abs             $9D3B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9C71  8E 9C FC                    push_imm_word            $FC9C                     ; "\nWhich fief? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9C74  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C78  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9C7B  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9C7F  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9C82  D8 39 9D                    branch_z_abs             $9D39                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9C85  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9C88  D1                          decA                                               ; regA -= 1
  $9C89  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9C8C  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9C8F  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9C90  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9C91  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9C92  D1                          decA                                               ; regA -= 1
  $9C93  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9C94  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9C95  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9C96  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9C99  AC A4 E3                    host_call_simple         $E3A4 (diplomacy_helper) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9C9C  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9C9D  D8 19 9D                    branch_z_abs             $9D19                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9CA0  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9CA1  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $9CA5  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9CA6  D7 02 59                    branch_nz_abs            $5902                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9CA9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9CAA  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9CAD  BB                          add                                                ; regA = regA + regB
  $9CAE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9CAF  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9CB2  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $9CB3  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9CB4  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9CB7  BB                          add                                                ; regA = regA + regB
  $9CB8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9CB9  8E 27 FD                    push_imm_word            $FD27                     ; "Lord %s,\n%s wants\n%d gold\nPay"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9CBC  E9 34 D1 08                 host_call                $D134 (ui_helper_d134) {bytecode}, $08 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9CC0  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9CC3  D8 05 9D                    branch_z_abs             $9D05                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9CC6  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9CC7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9CC8  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $9CC9  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $9CCA  D8 D7 9C                    branch_z_abs             $9CD7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9CCD  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9CD0  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9CD4  D6 14 9D                    jump_abs                 $9D14                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9CD7  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9CD8  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9CDC  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9CDD  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9CDE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9CDF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9CE0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9CE1  BC                          sub                                                ; regA = regA - regB
  $9CE2  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9CE3  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9CE4  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9CE7  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9CE9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9CEA  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9CED  BB                          add                                                ; regA = regA + regB
  $9CEE  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9CEF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9CF0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9CF1  BB                          add                                                ; regA = regA + regB
  $9CF2  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9CF3  AC 4F DA                    host_call_simple         $DA4F (diplomacy_helper2) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9CF6  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $9CF7  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9CFB  8E 63 FD                    push_imm_word            $FD63                     ; "War is inevitable\nso don`t let your\nguard down!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9CFE  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D02  D6 14 9D                    jump_abs                 $9D14                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9D05  8E 93 FD                    push_imm_word            $FD93                     ; "Who needs him\nanyway"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D08  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$9D0C  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9D0F  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9D10  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D11  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9D12  D1                          decA                                               ; regA -= 1
  $9D13  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$9D14  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9D17  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9D18  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9D19  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9D1A  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D1E  8E FB BB                    push_imm_word            $BBFB                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D21  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D25  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9D28  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $9D29  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9D2A  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9D2D  BB                          add                                                ; regA = regA + regB
  $9D2E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D2F  8E AA FC                    push_imm_word            $FCAA                     ; "Lord %s\nthey`ve refused!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D32  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D36  D6 0C 9D                    jump_abs                 $9D0C                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9D39  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9D3A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9D3B  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9D3C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9D3D (driver_grow)   (frame_off=-4, body @ $9D42)
; ============================================================
  $9D42  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9D45  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9D47  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9D48  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9D4B  BB                          add                                                ; regA = regA + regB
  $9D4C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9D4D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D4E  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $9D4F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D50  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D51  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D52  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9D54  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D55  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9D56  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $9D57  D8 66 9D                    branch_z_abs             $9D66                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9D5A  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D5D  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D61  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9D64  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9D65  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9D66  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D67  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D68  D8 B8 9D                    branch_z_abs             $9DB8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9D6B  8E FC BB                    push_imm_word            $BBFC                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D6E  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D72  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D73  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $9D74  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D75  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D76  8E FD BB                    push_imm_word            $BBFD                     ; "Output"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D79  8E 25 FB                    push_imm_word            $FB25                     ; "%s is %d\nSpend how much\non it"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9D7C  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D80  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D81  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D82  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D83  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9D84  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D88  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9D89  D8 B6 9D                    branch_z_abs             $9DB6                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9D8C  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9D8D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9D8E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9D8F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9D90  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9D91  BC                          sub                                                ; regA = regA - regB
  $9D92  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9D93  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9D94  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9D95  E9 F0 87 04                 host_call                $87F0 (effect_grow) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D99  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9D9A  69                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 9)
  $9D9B  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9D9F  8E 04 BC                    push_imm_word            $BC04                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DA2  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DA6  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9DA7  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $9DA8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9DA9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9DAA  8E 58 FB                    push_imm_word            $FB58                     ; "Output is now\n%d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DAD  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DB1  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9DB4  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9DB5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9DB6  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9DB7  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9DB8  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DBB  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DBF  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9DC2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9DC3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9DC4 (driver_marry)   (frame_off=-8, body @ $9DC9)
; ============================================================
  $9DC9  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9DCA  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9DCB  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9DCE  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $9DCF  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9DD2  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9DD5  BB                          add                                                ; regA = regA + regB
  $9DD6  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9DD7  D8 FC 9E                    branch_z_abs             $9EFC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9DDA  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9DDD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9DDE  E9 DC E4 02                 host_call                $E4DC (marry_helper_e4dc) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DE2  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DE5  68                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 8)
  $9DE6  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DEA  D8 FA 9E                    branch_z_abs             $9EFA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9DED  8E 9C FC                    push_imm_word            $FC9C                     ; "\nWhich fief? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DF0  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DF4  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9DF7  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9DFB  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9DFE  D8 F8 9E                    branch_z_abs             $9EF8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9E01  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9E02  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9E03  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E04  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9E05  D1                          decA                                               ; regA -= 1
  $9E06  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9E07  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9E08  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9E09  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9E0C  D1                          decA                                               ; regA -= 1
  $9E0D  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $9E10  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9E13  AC 15 E3                    host_call_simple         $E315 (marry_helper_e315) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9E16  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9E17  D8 C6 9E                    branch_z_abs             $9EC6                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9E1A  8D 16                       op_8D_sbyte              +22
  $9E1C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E20  8F 35                       addA_imm_sbyte           +53                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9E22  A8 5B 6F                    loadA_mem_word           $6F5B                     ; regA = word read using <word> as address (handler $EA75)
  $9E25  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9E26  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E2A  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $9E2B  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $9E2C  E9 6F E7 04                 host_call                $E76F (marry_helper_e76f) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E30  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9E31  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E35  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9E36  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $9E3A  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9E3B  D7 02 59                    branch_nz_abs            $5902                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $9E3E  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9E3F  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9E42  BB                          add                                                ; regA = regA + regB
  $9E43  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E44  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9E47  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $9E48  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9E49  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9E4C  BB                          add                                                ; regA = regA + regB
  $9E4D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E4E  8E 45 FD                    push_imm_word            $FD45                     ; "Lord %s,\n%s wants\n%d gold\nPay"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9E51  E9 34 D1 08                 host_call                $D134 (ui_helper_d134) {bytecode}, $08 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E55  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9E58  D8 A4 9E                    branch_z_abs             $9EA4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9E5B  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9E5E  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9E60  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9E61  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9E64  BB                          add                                                ; regA = regA + regB
  $9E65  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $9E66  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9E67  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9E68  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E69  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $9E6A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9E6B  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $9E6C  D8 9E 9E                    branch_z_abs             $9E9E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9E6F  AC 7D DA                    host_call_simple         $DA7D (diplomacy_helper3) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9E72  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9E73  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E77  8D 10                       op_8D_sbyte              +16
  $9E79  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E7D  8E C3 FC                    push_imm_word            $FCC3                     ; "Your bride-to-be\nhas arrived"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9E80  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9E84  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9E85  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $9E86  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9E87  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E88  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9E89  BC                          sub                                                ; regA = regA - regB
  $9E8A  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9E8B  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9E8C  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9E8F  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $9E91  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9E92  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9E95  BB                          add                                                ; regA = regA + regB
  $9E96  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9E97  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9E98  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9E99  BB                          add                                                ; regA = regA + regB
  $9E9A  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9E9B  D6 AB 9E                    jump_abs                 $9EAB                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9E9E  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9EA1  D6 A7 9E                    jump_abs                 $9EA7                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9EA4  8E E0 FC                    push_imm_word            $FCE0                     ; "Don`t you long to\nhear the\npitter-patter of\na li"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$9EA7  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$9EAB  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9EAE  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9EB1  A8 5B 6F                    loadA_mem_word           $6F5B                     ; regA = word read using <word> as address (handler $EA75)
  $9EB4  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9EB5  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9EB9  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $9EBA  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $9EBB  E9 6F E7 04                 host_call                $E76F (marry_helper_e76f) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9EBF  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9EC0  E9 35 CC 02                 host_call                $CC35 (marry_helper_cc35) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9EC4  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9EC5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9EC6  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9EC7  E9 8F 8B 02                 host_call                $8B8F (helper_8B8F) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9ECB  8E 05 BC                    push_imm_word            $BC05                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9ECE  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9ED2  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9ED5  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $9ED6  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $9ED7  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $9EDA  BB                          add                                                ; regA = regA + regB
  $9EDB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9EDC  8E AA FC                    push_imm_word            $FCAA                     ; "Lord %s\nthey`ve refused!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9EDF  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9EE3  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9EE4  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9EE5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9EE6  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9EE7  D1                          decA                                               ; regA -= 1
  $9EE8  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9EE9  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9EEA  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9EEB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9EEC  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9EED  D1                          decA                                               ; regA -= 1
  $9EEE  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9EEF  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $9EF0  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $9EF1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9EF2  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $9EF3  D1                          decA                                               ; regA -= 1
  $9EF4  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $9EF5  D6 FF 9E                    jump_abs                 $9EFF                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9EF8  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9EF9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9EFA  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9EFB  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9EFC  AC CB 87                    host_call_simple         $87CB (helper_87CB) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$9EFF  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9F02  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $9F03  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9F04 (subhandler_9F04)   (frame_off=-4, body @ $9F09)
; ============================================================
  $9F09  8E 06 BC                    push_imm_word            $BC06                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9F0C  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F10  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F11  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9F13  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F14  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F15  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F16  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F17  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9F18  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $9F19  D8 2F 9F                    branch_z_abs             $9F2F                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9F1C  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $9F20  8B D7                       loadB_imm_byte           -41                       ; regB = byte literal (handler $EABE)
  $9F22  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $9F23  8E 07 BC                    push_imm_word            $BC07                     ; "\nThe treasure room\nis already full!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9F26  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F2A  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9F2D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9F2E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9F2F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F30  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9F31  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F32  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F33  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F34  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9F35  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F36  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9F37  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $9F38  D8 A3 9F                    branch_z_abs             $9FA3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9F3B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F3C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F3D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F3E  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F3F  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $9F41  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F42  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9F43  BC                          sub                                                ; regA = regA - regB
  $9F44  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F45  A4 0B 6E                    loadA_mem_word           $6E0B                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $9F48  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $9F49  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F4A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F4B  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9F4C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F4D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F4E  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F4F  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $9F50  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F51  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9F52  BC                          sub                                                ; regA = regA - regB
  $9F53  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F54  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F58  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $9F59  D8 A3 9F                    branch_z_abs             $9FA3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9F5C  8E 9D F9                    push_imm_word            $F99D                     ; "How much"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9F5F  8E 2B BC                    push_imm_word            $BC2B                     ; "%s would\nyou like to\nborrow"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9F62  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F66  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $9F67  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9F68  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F6C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9F6D  D8 9E 9F                    branch_z_abs             $9F9E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9F70  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9F71  E9 40 8B 02                 host_call                $8B40 (effect_subhandler_9F04) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F75  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $9F76  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F7A  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $9F7B  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F7F  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $9F80  8D 10                       op_8D_sbyte              +16
  $9F82  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F86  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F87  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F88  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F89  E9 C7 83 02                 host_call                $83C7 (helper_83C7) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F8D  68                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 8)
  $9F8E  8D 10                       op_8D_sbyte              +16
  $9F90  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F94  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9F95  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9F96  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9F97  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9F98  E9 C7 83 02                 host_call                $83C7 (helper_83C7) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9F9C  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9F9D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9F9E  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9FA1  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9FA2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9FA3  8E 47 BC                    push_imm_word            $BC47                     ; "You can`t borrow!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9FA6  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9FAA  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9FAD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9FAE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $9FAF (subhandler_9FAF)   (frame_off=-2, body @ $9FB4)
; ============================================================
  $9FB4  8E 59 BC                    push_imm_word            $BC59                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9FB7  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9FBB  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FBC  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9FBD  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FBE  D8 F7 9F                    branch_z_abs             $9FF7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9FC1  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FC2  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FC3  D8 F1 9F                    branch_z_abs             $9FF1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9FC6  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FC7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FC8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9FC9  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FCA  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9FCB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FCC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9FCD  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9FD1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9FD2  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $9FD3  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9FD7  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $9FD8  D8 EC 9F                    branch_z_abs             $9FEC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $9FDB  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9FDC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FDD  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9FDE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9FDF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FE0  BC                          sub                                                ; regA = regA - regB
  $9FE1  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9FE2  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $9FE3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $9FE4  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $9FE5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $9FE6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $9FE7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $9FE8  BC                          sub                                                ; regA = regA - regB
  $9FE9  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $9FEA  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $9FEB  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9FEC  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $9FEF  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $9FF0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$9FF1  8E 5A BC                    push_imm_word            $BC5A                     ; "\nNo gold"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $9FF4  D6 FA 9F                    jump_abs                 $9FFA                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$9FF7  8E 63 BC                    push_imm_word            $BC63                     ; "\nDebt? What debt?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$9FFA  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $9FFE  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A001  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A002  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A003 (subhandler_A003)   (frame_off=-4, body @ $A008)
; ============================================================
  $A008  8E 75 BC                    push_imm_word            $BC75                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A00B  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A00F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A010  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A011  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A012  D8 59 A0                    branch_z_abs             $A059                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A015  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A016  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A017  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A018  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A019  AA 0D 6E 0C                 op_AA_wb                 $6E0D, $0C
  $A01D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A01E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A01F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A020  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A022  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A023  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A024  BC                          sub                                                ; regA = regA - regB
  $A025  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A026  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A02A  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A02B  D8 53 A0                    branch_z_abs             $A053                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A02E  8E 9D F9                    push_imm_word            $F99D                     ; "How much"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A031  8E 76 BC                    push_imm_word            $BC76                     ; "%s rice\nwill you sell"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A034  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A038  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A039  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A03A  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A03E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A03F  D8 63 A0                    branch_z_abs             $A063                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A042  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A043  E9 0A 8B 02                 host_call                $8B0A (effect_subhandler_A003) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A047  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A048  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A04C  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A04D  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A051  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A052  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A053  8E 8C BC                    push_imm_word            $BC8C                     ; "Treasury full"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A056  D6 5C A0                    jump_abs                 $A05C                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A059  8E 9A BC                    push_imm_word            $BC9A                     ; "No rice"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$A05C  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A060  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A063  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A066  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A067  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A068 (subhandler_A068)   (frame_off=-4, body @ $A06D)
; ============================================================
  $A06D  8E A2 BC                    push_imm_word            $BCA2                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A070  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A074  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A077  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A079  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A07A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A07D  BB                          add                                                ; regA = regA + regB
  $A07E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A07F  D8 DE A0                    branch_z_abs             $A0DE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A082  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A083  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A084  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A085  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A086  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A087  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A089  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A08A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A08B  BC                          sub                                                ; regA = regA - regB
  $A08C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A08D  AA 0D 6E 0C                 op_AA_wb                 $6E0D, $0C
  $A091  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A092  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A093  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A097  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A098  D8 D8 A0                    branch_z_abs             $A0D8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A09B  8E 9D F9                    push_imm_word            $F99D                     ; "How much"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A09E  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0A2  8E A3 BC                    push_imm_word            $BCA3                     ; " rice\nwill you buy"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A0A5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0A9  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A0AA  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A0AB  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0AF  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A0B0  D8 E8 A0                    branch_z_abs             $A0E8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A0B3  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A0B4  AA 0D 6E E9                 op_AA_wb                 $6E0D, $E9
  $A0B8  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $A0B9  83 04                       op_83_byte               $04
  $A0BB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0BC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0BD  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A0BE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0BF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A0C0  BC                          sub                                                ; regA = regA - regB
  $A0C1  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A0C2  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A0C3  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0C4  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A0C5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A0C6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0C7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A0C8  BB                          add                                                ; regA = regA + regB
  $A0C9  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A0CA  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0CB  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A0CC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0CD  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0D1  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $A0D2  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0D6  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A0D7  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A0D8  8E B6 BC                    push_imm_word            $BCB6                     ; "Storehouse full"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A0DB  D6 E1 A0                    jump_abs                 $A0E1                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A0DE  8E C6 BC                    push_imm_word            $BCC6                     ; "Sorry, no credit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$A0E1  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A0E5  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A0E8  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A0EB  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A0EC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A0ED (effect_subhandler_A113)   (frame_off=-2, body @ $A0F2)
; ============================================================
  $A0F2  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0F3  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A0F5  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A0F6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0F7  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $A0F8  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0F9  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A0FB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A0FC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A0FD  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A0FE  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A100  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A101  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A102  BB                          add                                                ; regA = regA + regB
  $A103  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A104  E9 B8 D6 06                 host_call                $D6B8 (math32_3arg) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A108  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A109  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A10A  D7 0F A1                    branch_nz_abs            $A10F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A10D  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A10E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$A10F  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A110  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $A111  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $A112  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A113 (subhandler_A113)   (frame_off=-4, body @ $A118)
; ============================================================
  $A118  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A119  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A11B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A11C  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $A11D  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $A11E  D8 2D A1                    branch_z_abs             $A12D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A121  8E D7 BC                    push_imm_word            $BCD7                     ; "Weapons? For who?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A124  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A128  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A12B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A12C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A12D  8E E9 BC                    push_imm_word            $BCE9                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A130  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A134  A4 0F 6E                    loadA_mem_word           $6E0F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A137  79                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 9)
  $A138  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $A139  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $A13A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A13B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A13C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A13D  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $A13E  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $A13F  D8 A0 A1                    branch_z_abs             $A1A0                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A142  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A143  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A145  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A146  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A147  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A148  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A14A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A14B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A14C  BC                          sub                                                ; regA = regA - regB
  $A14D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A14E  AA 0F 6E 0C                 op_AA_wb                 $6E0F, $0C
  $A152  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A153  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A154  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A158  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A159  D8 9A A1                    branch_z_abs             $A19A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A15C  8E A6 F9                    push_imm_word            $F9A6                     ; "How many"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A15F  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A163  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A164  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A165  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A169  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A16A  D8 AA A1                    branch_z_abs             $A1AA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A16D  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A16E  AA 0F 6E E9                 op_AA_wb                 $6E0F, $E9
  $A172  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $A173  83 04                       op_83_byte               $04
  $A175  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A176  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A177  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A178  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A179  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A17A  BC                          sub                                                ; regA = regA - regB
  $A17B  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A17C  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A17D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A17E  E9 ED A0 04                 host_call                $A0ED (effect_subhandler_A113) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A182  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A183  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A184  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A186  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A187  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A188  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A189  BB                          add                                                ; regA = regA + regB
  $A18A  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A18B  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A18C  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A18E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A18F  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A193  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $A194  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A198  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A199  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A19A  8E EA BC                    push_imm_word            $BCEA                     ; "Armory full"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A19D  D6 A3 A1                    jump_abs                 $A1A3                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A1A0  8E F6 BC                    push_imm_word            $BCF6                     ; "We merchants deal\nonly in cash"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$A1A3  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1A7  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A1AA  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A1AD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A1AE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A1AF (subhandler_A1AF)   (frame_off=+0, body @ $A1B4)
; ============================================================
  $A1B4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A1B5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A1B6 (driver_trade)   (frame_off=-4, body @ $A1BB)
; ============================================================
  $A1BB  AC 15 8A                    host_call_simple         $8A15 (effect_trade) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A1BE  D8 49 A2                    branch_z_abs             $A249                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A1C1  AC 69 CC                    host_call_simple         $CC69 (trade_helper_cc69) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A1C4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A1C5  D6 E9 A1                    jump_abs                 $A1E9                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A1C8  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A1C9  77                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 7)
  $A1CA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A1CB  8D 16                       op_8D_sbyte              +22
  $A1CD  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1D1  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A1D2  D0                          incA                                               ; regA += 1
  $A1D3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A1D4  8E 15 BD                    push_imm_word            $BD15                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A1D7  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1DB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A1DC  D2                          aslA                                               ; regA <<= 1
  $A1DD  8C 4F FF                    loadB_imm_word           $FF4F                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A1E0  BB                          add                                                ; regA = regA + regB
  $A1E1  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A1E2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A1E3  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1E7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A1E8  D0                          incA                                               ; regA += 1
 >$A1E9  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A1EA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A1EB  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $A1EC  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $A1ED  D7 C8 A1                    branch_nz_abs            $A1C8                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$A1F0  8E 19 BD                    push_imm_word            $BD19                     ; "Lord,how may I\nserve you?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A1F3  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1F7  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $A1F8  E9 A6 B1 02                 host_call                $B1A6 (submenu_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A1FC  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A1FD  D2                          aslA                                               ; regA <<= 1
  $A1FE  8C DC B9                    loadB_imm_word           $B9DC                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A201  BB                          add                                                ; regA = regA + regB
  $A202  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A203  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A204  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A207  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A209  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A20A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A20D  BB                          add                                                ; regA = regA + regB
  $A20E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A20F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A210  EA                          host_call_indirect                                 ; JSR to native code at address in regA; then ptr1 adjust (handler $EB2D)
  $A211  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $A212  D8 31 A2                    branch_z_abs             $A231                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A215  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $A216  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A21A  8E 33 BD                    push_imm_word            $BD33                     ; "Let`s do business\nagain"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A21D  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A221  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A224  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A225  D7 2F A2                    branch_nz_abs            $A22F                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A228  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $A229  E9 1E 87 02                 host_call                $871E (fief_info_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A22D  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A22E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A22F  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A230  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A231  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A232  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $A233  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A234  D8 F0 A1                    branch_z_abs             $A1F0                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A237  8E 4B BD                    push_imm_word            $BD4B                     ; "Bye!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A23A  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A23E  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A241  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $A242  E9 1E 87 02                 host_call                $871E (fief_info_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A246  D6 53 A2                    jump_abs                 $A253                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A249  8E 69 FB                    push_imm_word            $FB69                     ; "No merchant in\nthe area"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A24C  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A250  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A253  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A254  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A255   (frame_off=+0, body @ $A25A)
; ============================================================
  $A25A  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A25B  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A25C  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A260  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A261  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A262  E9 0D D7 04                 host_call                $D70D (pct_op) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A266  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A267  E9 6F CB 04                 host_call                $CB6F {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A26B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A26C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A270  D0                          incA                                               ; regA += 1
  $A271  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $A272  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A273  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A274   (frame_off=+0, body @ $A279)
; ============================================================
  $A279  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A27A  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A27B  57                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 7)
  $A27C  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A27D  D8 86 A2                    branch_z_abs             $A286                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A280  8A 71 BD                    loadA_imm_word           $BD71                     ; "Town value"  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $A283  D6 8D A2                    jump_abs                 $A28D                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A286  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A287  D2                          aslA                                               ; regA <<= 1
  $A288  8C AE F8                    loadB_imm_word           $F8AE                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A28B  BB                          add                                                ; regA = regA + regB
  $A28C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
 >$A28D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A28E  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A291  D0                          incA                                               ; regA += 1
  $A292  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A293  8E 50 BD                    push_imm_word            $BD50                     ; "fief %d`s\n%s has\ndeclined by %d\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A296  E9 34 D1 08                 host_call                $D134 (ui_helper_d134) {bytecode}, $08 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A29A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A29B   (frame_off=+0, body @ $A2A0)
; ============================================================
  $A2A0  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A2A1  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A2A4  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $A2A5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A2A6  E9 12 CA 04                 host_call                $CA12 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2AA  8E 7C BD                    push_imm_word            $BD7C                     ; "Your ninja\nfailed!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A2AD  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2B1  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A2B4  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A2B5  AA 13 6E E9                 op_AA_wb                 $6E13, $E9
  $A2B9  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $A2BA  83 04                       op_83_byte               $04
  $A2BC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A2BD  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A2C0  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A2C2  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A2C3  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A2C6  BB                          add                                                ; regA = regA + regB
  $A2C7  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A2C8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A2C9  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A2CA  BC                          sub                                                ; regA = regA - regB
  $A2CB  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A2CC  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $A2CD  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2D1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A2D2 (effect_hire)   (frame_off=-10, body @ $A2D7)
; ============================================================
  $A2D7  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A2D8  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A2DA  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A2DB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A2DC  AA 13 6E 0C                 op_AA_wb                 $6E13, $0C
  $A2E0  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A2E1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A2E2  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2E6  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A2E7  D8 60 A3                    branch_z_abs             $A360                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A2EA  8E A6 F9                    push_imm_word            $F9A6                     ; "How many"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A2ED  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2F1  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A2F2  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A2F3  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A2F7  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $A2F8  D8 51 A5                    branch_z_abs             $A551                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A2FB  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $A2FC  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A300  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A303  8E FF 00                    push_imm_word            $00FF                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A306  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A30A  D8 51 A5                    branch_z_abs             $A551                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A30D  8E C8 FA                    push_imm_word            $FAC8                     ; "\nSend where? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A310  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A314  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A317  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A31B  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $A31E  D8 51 A5                    branch_z_abs             $A551                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A321  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A324  D1                          decA                                               ; regA -= 1
  $A325  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $A328  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A32B  8E AE FB                    push_imm_word            $FBAE                     ; "What mission"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A32E  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A332  8E 8F BD                    push_imm_word            $BD8F                     ; "\n1-Uprisng 2-Revolt\n3-Dams 4-Assassin\n5-Arson"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A335  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A339  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $A33A  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A33B  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A33F  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A340  D8 51 A5                    branch_z_abs             $A551                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A343  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A344  54                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 4)
  $A345  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A346  D8 6C A3                    branch_z_abs             $A36C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A349  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A34C  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A34F  BB                          add                                                ; regA = regA + regB
  $A350  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A351  D7 6C A3                    branch_nz_abs            $A36C                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A354  8E BD BD                    push_imm_word            $BDBD                     ; "the daimyo is out"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A357  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A35B  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A35E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A35F  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A360  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A363  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A367  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A36A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A36B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A36C  6C                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 12)
  $A36D  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A371  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A374  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A376  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A377  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A37A  BB                          add                                                ; regA = regA + regB
  $A37B  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $A37C  8E CF BD                    push_imm_word            $BDCF                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A37F  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A383  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A386  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $A387  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A388  8F 1E                       addA_imm_sbyte           +30                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A38A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A38B  AA 63 6F E9                 op_AA_wb                 $6F63, $E9
  $A38F  DA                          bitand                                             ; regA = regA & regB
  $A390  D7 02 73                    branch_nz_abs            $7302                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A393  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A394  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A395  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $A396  D8 49 A5                    branch_z_abs             $A549                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A399  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A39A  D5                          switch                                             ; Jump-table dispatch on regA (handler $EC65) — reads inline (value,target) pairs from the bytecode stream, jumps to the matching target. Variable-length; the disassembler stops the fall-through run here.
  ; ---- gap $A39B-$A548 (430 bytes, not on any traced path) ----
 >$A549  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $A54A  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $A54B  E9 9B A2 04                 host_call                $A29B {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A54F  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A550  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A551  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A552  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A553   (frame_off=-6, body @ $A558)
; ============================================================
  $A558  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A559  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A55B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A55C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A55D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A55E  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A560  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A561  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A562  BC                          sub                                                ; regA = regA - regB
  $A563  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $A564  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A565  AA 11 6E 0C                 op_AA_wb                 $6E11, $0C
  $A569  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A56A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A56B  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A56F  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A570  D8 DD A5                    branch_z_abs             $A5DD                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A573  8E A6 F9                    push_imm_word            $F9A6                     ; "How many"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A576  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A57A  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A57B  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A57C  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A580  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A581  D8 F2 A5                    branch_z_abs             $A5F2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A584  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A585  AA 11 6E E9                 op_AA_wb                 $6E11, $E9
  $A589  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $A58A  83 04                       op_83_byte               $04
  $A58C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A58D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A58E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A58F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A590  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A591  BC                          sub                                                ; regA = regA - regB
  $A592  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A593  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A594  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A595  E9 F4 8B 04                 host_call                $8BF4 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A599  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A59A  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $A59E  FE                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $A59F  DF 04                       loadB_frameaddr          $04                       ; regB = ptr2 + byte  (address of a frame local; handler $EAA0)
  $A5A1  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $A5A2  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5A6  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A5A7  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A5A9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A5AA  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5AE  8D 21                       op_8D_sbyte              +33
  $A5B0  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5B4  8E D0 BD                    push_imm_word            $BDD0                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A5B7  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5BB  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A5BE  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A5C0  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A5C1  8C 11 70                    loadB_imm_word           $7011                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A5C4  BB                          add                                                ; regA = regA + regB
  $A5C5  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A5C6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A5C7  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A5CA  59                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 9)
  $A5CB  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A5CC  8C A8 77                    loadB_imm_word           $77A8 (daimyo_name_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A5CF  BB                          add                                                ; regA = regA + regB
  $A5D0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A5D1  8E 90 FB                    push_imm_word            $FB90                     ; "Lord %s, we\nnow have %d men"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A5D4  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5D8  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A5DB  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A5DC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A5DD  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $A5DE  D8 E7 A5                    branch_z_abs             $A5E7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A5E1  8A D9 FD                    loadA_imm_word           $FDD9                     ; "You have no gold!"  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $A5E4  D6 EA A5                    jump_abs                 $A5EA                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A5E7  8A C6 FD                    loadA_imm_word           $FDC6                     ; "We`re at our\nlimit"  | regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
 >$A5EA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A5EB  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A5EF  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A5F2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A5F3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A5F4 (driver_hire)   (frame_off=-4, body @ $A5F9)
; ============================================================
  $A5F9  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $A5FD  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $A5FE  93                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $A5FF  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $A600  D8 35 A6                    branch_z_abs             $A635                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A603  8E 81 FB                    push_imm_word            $FB81                     ; "Recruit which"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A606  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A60A  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A60D  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A60F  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A610  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A613  BB                          add                                                ; regA = regA + regB
  $A614  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A615  8E D1 BD                    push_imm_word            $BDD1                     ; "\n(Men/Ninja)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A618  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A61C  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $A61D  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $A61E  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $A61F  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $A620  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $A621  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $A622  A6 01 00                    loadB_mem_word           $0001 (ptr0_hi)           ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A625  2F                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $A626  A6 35 A6                    loadB_mem_word           $A635                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A629  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A62A  E9 D2 A2 02                 host_call                $A2D2 (effect_hire) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A62E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
  ; ---- gap $A62F-$A634 (6 bytes, not on any traced path) ----
 >$A635  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A636  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A637 (driver_train)   (frame_off=-2, body @ $A63C)
; ============================================================
  $A63C  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A63F  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A641  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A642  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A645  BB                          add                                                ; regA = regA + regB
  $A646  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A647  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A648  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A64A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A64B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A64C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A64D  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A64F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A650  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A651  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $A652  D8 A7 A6                    branch_z_abs             $A6A7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A655  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A656  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A658  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A659  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $A65A  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $A65B  D8 71 A6                    branch_z_abs             $A671                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A65E  8E FD FD                    push_imm_word            $FDFD                     ; "You have no\nsoldiers!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A661  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A665  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $A669  15                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 5)
  $A66A  D8 02 AC                    branch_z_abs             $AC02                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A66D  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $A66E  D7 40 CF                    branch_nz_abs            $CF40                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$A671  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $A672  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A676  AC 86 95                    host_call_simple         $9586 (effect_train) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A679  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A67A  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A67C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A67D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A67E  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A680  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A681  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A682  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A683  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A685  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A686  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A687  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $A688  D8 91 A6                    branch_z_abs             $A691                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A68B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A68C  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A68E  D6 94 A6                    jump_abs                 $A694                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A691  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A692  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
 >$A694  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A695  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A696  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A697  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A699  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A69A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A69B  8E DE FB                    push_imm_word            $FBDE                     ; "Skill is now %d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A69E  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A6A2  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A6A5  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A6A6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A6A7  8E E0 BD                    push_imm_word            $BDE0                     ; "The men can`t\ntrain any further"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A6AA  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A6AE  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A6B1  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A6B2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A6B3 (effect_view_d)   (frame_off=+0, body @ $A6B8)
; ============================================================
  $A6B8  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A6B9  75                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 5)
  $A6BA  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A6BB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A6BC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A6BD  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $A6BE  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A6BF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A6C0  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A6C4  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A6C5  BB                          add                                                ; regA = regA + regB
  $A6C6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A6C7 (driver_view)   (frame_off=-10, body @ $A6CC)
; ============================================================
  $A6CC  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A6CF  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
 >$A6D0  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A6D1  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $A6D2  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A6D3  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A6D7  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $A6D8  D8 DD A6                    branch_z_abs             $A6DD                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A6DB  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A6DC  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
 >$A6DD  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $A6DE  D7 13 A7                    branch_nz_abs            $A713                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A6E1  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A6E4  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A6E6  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A6E7  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A6EA  BB                          add                                                ; regA = regA + regB
  $A6EB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A6EC  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $A6ED  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $A6EE  D8 13 A7                    branch_z_abs             $A713                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A6F1  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A6F4  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A6F8  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A6FB  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A6FC  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A6FD  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $A6FE  8C D8 FE                    loadB_imm_word           $FED8                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A701  D6 53 A7                    jump_abs                 $A753                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A704  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A705  8B 63                       loadB_imm_byte           +99                       ; regB = byte literal (handler $EABE)
  $A707  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A708  D8 6C A7                    branch_z_abs             $A76C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A70B  AC EA 85                    host_call_simple         $85EA (effect_view_b) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A70E  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $A70F  E9 FA 83 02                 host_call                $83FA (effect_view_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$A713  8E EF FB                    push_imm_word            $FBEF                     ; "View which fief?\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A716  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A71A  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A71B  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A71F  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $A720  D8 2C A7                    branch_z_abs             $A72C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A723  8E 00 BE                    push_imm_word            $BE00                     ; "(99-view vassals)\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A726  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A72A  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A72B  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
 >$A72C  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A72D  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $A730  8D 63                       op_8D_sbyte              +99
  $A732  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A733  E9 9D D5 04                 host_call                $D59D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A737  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A738  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A739  A8 E0 7B                    loadA_mem_word           $7BE0                     ; regA = word read using <word> as address (handler $EA75)
  $A73C  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A73D  8B FF                       loadB_imm_byte           -1                        ; regB = byte literal (handler $EABE)
  $A73F  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A740  D8 68 A7                    branch_z_abs             $A768                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A743  AC 20 CD                    host_call_simple         $CD20 (ui_helper_cd20) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A746  A4 9D 6D                    loadA_mem_word           $6D9D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A749  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $A74B  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A74C  D8 FD A6                    branch_z_abs             $A6FD                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A74F  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $A750  8C A6 FE                    loadB_imm_word           $FEA6                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
 >$A753  BB                          add                                                ; regA = regA + regB
  $A754  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $A755  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A756  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A757  E9 F2 E5 02                 host_call                $E5F2 (map_helper_e5f2) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A75B  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A75C  E9 10 AF 02                 host_call                $AF10 (map_helper_af10) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A760  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $A761  E9 FA 83 02                 host_call                $83FA (effect_view_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A765  D6 D0 A6                    jump_abs                 $A6D0                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A768  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $A769  D7 04 A7                    branch_nz_abs            $A704                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$A76C  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A76D  D8 7A A7                    branch_z_abs             $A77A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A770  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A771  D1                          decA                                               ; regA -= 1
  $A772  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A773  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A776  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A777  D8 87 A7                    branch_z_abs             $A787                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$A77A  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $A77B  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A77E  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A77F  D8 C0 A7                    branch_z_abs             $A7C0                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A782  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A785  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A786  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A787  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A788  A6 9D 6D                    loadB_mem_word           $6D9D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A78B  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $A78C  D8 13 A7                    branch_z_abs             $A713                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A78F  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A790  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A794  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A795  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A798  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A799  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $A79A  D8 33 A8                    branch_z_abs             $A833                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A79D  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A7A0  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A7A2  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A7A3  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A7A6  BB                          add                                                ; regA = regA + regB
  $A7A7  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A7A8  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $A7A9  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $A7AA  D8 C6 A7                    branch_z_abs             $A7C6                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A7AD  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A7B0  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A7B3  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A7B7  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A7BA  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $A7BB  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $A7BC  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A7BD  D7 13 A7                    branch_nz_abs            $A713                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$A7C0  AC A7 85                    host_call_simple         $85A7 (effect_view_c) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A7C3  D6 85 A7                    jump_abs                 $A785                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A7C6  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $A7C7  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A7CA  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A7CC  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A7CD  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A7D0  BB                          add                                                ; regA = regA + regB
  $A7D1  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A7D2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A7D3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A7D4  BC                          sub                                                ; regA = regA - regB
  $A7D5  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A7D6  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $A7D7  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $A7DA  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A7DB  D8 F5 A7                    branch_z_abs             $A7F5                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A7DE  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $A7DF  8D 10                       op_8D_sbyte              +16
  $A7E1  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A7E5  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A7E8  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A7EA  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A7EB  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A7EE  BB                          add                                                ; regA = regA + regB
  $A7EF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A7F0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A7F1  E9 C7 83 02                 host_call                $83C7 (helper_83C7) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$A7F5  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A7F6  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A7FA  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A7FB  E9 B3 A6 02                 host_call                $A6B3 (effect_view_d) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A7FF  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A800  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A803  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A804  E9 B3 A6 02                 host_call                $A6B3 (effect_view_d) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A808  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A809  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $A80A  D8 17 A8                    branch_z_abs             $A817                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A80D  AA 63 6D E9                 op_AA_wb                 $6D63 (const_two), $E9
  $A811  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $A812  CA                          is_zero                                            ; regA = (regA == 0) ? 1 : 0   (logical NOT)
  $A813  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $A814  D8 2B A8                    branch_z_abs             $A82B                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$A817  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $A818  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A81C  D7 2B A8                    branch_nz_abs            $A82B                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A81F  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A820  E9 8D D9 02                 host_call                $D98D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A824  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A827  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A828  D8 43 A8                    branch_z_abs             $A843                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$A82B  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A82E  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $A82F  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$A833  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A834  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $A835  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $A836  D8 13 A7                    branch_z_abs             $A713                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A839  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A83A  E9 FA 83 02                 host_call                $83FA (effect_view_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A83E  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A83F  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $A840  D6 13 A7                    jump_abs                 $A713                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A843  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A846  8E 01 FC                    push_imm_word            $FC01                     ; "Our spy was\ncaught"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A849  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A84D  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A850  D6 13 A7                    jump_abs                 $A713                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)

; ============================================================
; sub $A853 (driver_build)   (frame_off=-4, body @ $A858)
; ============================================================
  $A858  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $A85B  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $A85D  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $A85E  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $A861  BB                          add                                                ; regA = regA + regB
  $A862  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A863  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A864  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $A865  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A866  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A867  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A868  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A86A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A86B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A86C  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $A86D  D8 7C A8                    branch_z_abs             $A87C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A870  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A873  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A877  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A87A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A87B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A87C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A87D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A87E  D8 C6 A8                    branch_z_abs             $A8C6                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A881  8E 13 BE                    push_imm_word            $BE13                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A884  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A888  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A889  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $A88A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A88B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A88C  8E 14 BE                    push_imm_word            $BE14                     ; "Town value"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A88F  8E 25 FB                    push_imm_word            $FB25                     ; "%s is %d\nSpend how much\non it"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A892  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A896  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A897  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A898  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A899  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A89A  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A89E  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A89F  D8 D2 A8                    branch_z_abs             $A8D2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A8A2  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A8A3  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A8A4  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A8A5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A8A6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A8A7  BC                          sub                                                ; regA = regA - regB
  $A8A8  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A8A9  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A8AA  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A8AB  E9 A6 88 04                 host_call                $88A6 (effect_build) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8AF  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A8B0  8D 18                       op_8D_sbyte              +24
  $A8B2  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8B6  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A8B7  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $A8B8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A8B9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A8BA  8E 14 FC                    push_imm_word            $FC14                     ; "Town value is now\n%d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A8BD  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8C1  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A8C4  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A8C5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A8C6  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A8C9  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8CD  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A8D0  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A8D1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A8D2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A8D3   (frame_off=-4, body @ $A8D8)
; ============================================================
  $A8D8  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A8D9  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $A8DA  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A8DB  D8 E2 A8                    branch_z_abs             $A8E2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A8DE  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A8DF  D6 E4 A8                    jump_abs                 $A8E4                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A8E2  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A8E3  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
 >$A8E4  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A8E5  8E A8 FD                    push_imm_word            $FDA8                     ; "Give how much"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A8E8  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8EC  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A8ED  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A8EE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A8EF  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $A8F0  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A8F4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A8F5  D8 1C A9                    branch_z_abs             $A91C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A8F8  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A8F9  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A8FA  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $A8FB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A8FC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A8FD  BC                          sub                                                ; regA = regA - regB
  $A8FE  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $A8FF  0E                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A900  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $A901  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A902  D8 14 A9                    branch_z_abs             $A914                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A905  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A906  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A907  E9 1D 89 04                 host_call                $891D {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A90B  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A90C  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A90D  E9 6F 89 04                 host_call                $896F {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A911  D6 1A A9                    jump_abs                 $A91A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A914  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $A915  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A916  E9 C1 89 04                 host_call                $89C1 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$A91A  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A91B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A91C  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A91D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A91E   (frame_off=-2, body @ $A923)
; ============================================================
  $A923  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A924  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A925  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A926  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $A927  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $A928  D8 2D A9                    branch_z_abs             $A92D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A92B  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A92C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$A92D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A92E  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $A92F  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $A930  D8 38 A9                    branch_z_abs             $A938                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A933  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A934  CD                          swap_AB                                            ; regA <-> regB
  $A935  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A936  DB                          bitor                                              ; regA = regA | regB
  $A937  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$A938  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A939  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A93A (effect_give_a)   (frame_off=+0, body @ $A93F)
; ============================================================
  $A93F  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A940  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $A942  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A943  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A944  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A945  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $A946  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A947  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A948  E9 1E A9 04                 host_call                $A91E {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A94C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A94D   (frame_off=+0, body @ $A952)
; ============================================================
  $A952  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A953  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $A954  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A955  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A956  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $A957  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $A958  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $A959  E9 1E A9 04                 host_call                $A91E {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A95D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A95E (effect_give_b)   (frame_off=-4, body @ $A963)
; ============================================================
  $A963  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A964  E9 4D A9 02                 host_call                $A94D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A968  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A969  D8 BB A9                    branch_z_abs             $A9BB                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A96C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A96D  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A96E  8E 1F BE                    push_imm_word            $BE1F                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A971  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A975  8E 20 BE                    push_imm_word            $BE20                     ; "(Gold/Rice)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A978  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A97C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A97D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A97E  D7 85 A9                    branch_nz_abs            $A985                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A981  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $A982  D6 8C A9                    jump_abs                 $A98C                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A985  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A986  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $A987  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A988  D8 8D A9                    branch_z_abs             $A98D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A98B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
 >$A98C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$A98D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A98E  D8 AF A9                    branch_z_abs             $A9AF                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A991  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A992  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $A993  DA                          bitand                                             ; regA = regA & regB
  $A994  D7 AF A9                    branch_nz_abs            $A9AF                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A997  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $A998  D1                          decA                                               ; regA -= 1
  $A999  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $A99A  D8 A3 A9                    branch_z_abs             $A9A3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A99D  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9A0  D6 A6 A9                    jump_abs                 $A9A6                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A9A3  8E EB FD                    push_imm_word            $FDEB                     ; "You have no rice!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$A9A6  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9AA  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $A9AD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A9AE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A9AF  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A9B0  D8 D3 A9                    branch_z_abs             $A9D3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A9B3  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A9B4  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $A9B5  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A9B6  E9 D3 A8 06                 host_call                $A8D3 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9BA  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$A9BB  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9BE  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9C2  8E 2E BE                    push_imm_word            $BE2E                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9C5  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9C9  8E EB FD                    push_imm_word            $FDEB                     ; "You have no rice!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9CC  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9D0  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$A9D3  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $A9D4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $A9D5 (effect_give_c)   (frame_off=-2, body @ $A9DA)
; ============================================================
  $A9DA  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $A9DB  E9 3A A9 02                 host_call                $A93A (effect_give_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9DF  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A9E0  D8 05 AA                    branch_z_abs             $AA05                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $A9E3  8E 30 BE                    push_imm_word            $BE30                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9E6  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9EA  8E 31 BE                    push_imm_word            $BE31                     ; "(Peasnts/Men)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $A9ED  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $A9F1  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A9F2  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A9F3  D7 FB A9                    branch_nz_abs            $A9FB                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $A9F6  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
 >$A9F7  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $A9F8  D6 1D AA                    jump_abs                 $AA1D                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$A9FB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $A9FC  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $A9FD  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $A9FE  D8 1D AA                    branch_z_abs             $AA1D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA01  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
 >$AA02  D6 F7 A9                    jump_abs                 $A9F7                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AA05  8E FD FD                    push_imm_word            $FDFD                     ; "You have no\nsoldiers!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AA08  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA0C  8E 41 BE                    push_imm_word            $BE41                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AA0F  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA13  8E 13 FE                    push_imm_word            $FE13                     ; "You have no\npeasants!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AA16  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA1A  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$AA1D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AA1E  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AA1F (driver_give)   (frame_off=-6, body @ $AA24)
; ============================================================
  $AA24  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AA27  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $AA29  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AA2A  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AA2D  BB                          add                                                ; regA = regA + regB
  $AA2E  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $AA2F  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $AA30  E9 3A A9 02                 host_call                $A93A (effect_give_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA34  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AA35  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $AA36  E9 D5 A9 02                 host_call                $A9D5 (effect_give_c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA3A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AA3B  D8 AC AA                    branch_z_abs             $AAAC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA3E  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AA3F  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $AA40  DA                          bitand                                             ; regA = regA & regB
  $AA41  D7 5C AA                    branch_nz_abs            $AA5C                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AA44  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AA45  D1                          decA                                               ; regA -= 1
  $AA46  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AA47  D8 50 AA                    branch_z_abs             $AA50                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA4A  8E 13 FE                    push_imm_word            $FE13                     ; "You have no\npeasants!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AA4D  D6 53 AA                    jump_abs                 $AA53                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AA50  8E FD FD                    push_imm_word            $FDFD                     ; "You have no\nsoldiers!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
 >$AA53  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA57  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AA5A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AA5B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AA5C  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AA5D  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AA5F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AA60  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AA61  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AA62  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $AA63  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AA64  D8 75 AA                    branch_z_abs             $AA75                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA67  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AA68  7E                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 14)
  $AA69  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AA6A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AA6B  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AA6C  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $AA6D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AA6E  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $AA6F  BB                          add                                                ; regA = regA + regB
  $AA70  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $AA71  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $AA72  D6 79 AA                    jump_abs                 $AA79                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AA75  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AA76  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AA78  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
 >$AA79  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $AA7A  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $AA7B  D8 8A AA                    branch_z_abs             $AA8A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA7E  8E C6 FD                    push_imm_word            $FDC6                     ; "We`re at our\nlimit"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AA81  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA85  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AA88  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AA89  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AA8A  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $AA8B  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $AA8C  E9 5E A9 04                 host_call                $A95E (effect_give_b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA90  D8 AC AA                    branch_z_abs             $AAAC                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AA93  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $AA94  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AA98  AC EA D7                    host_call_simple         $D7EA (ui_helper_d7ea) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AA9B  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $AA9C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AA9D  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AA9E  D0                          incA                                               ; regA += 1
  $AA9F  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $AAA0  8E B6 FD                    push_imm_word            $FDB6                     ; "Thank you Lord!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AAA3  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AAA7  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AAAA  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AAAB  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AAAC  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AAAD  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AAAE (driver_bribe)   (frame_off=-4, body @ $AAB3)
; ============================================================
  $AAB3  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AAB6  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $AAB8  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AAB9  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AABC  BB                          add                                                ; regA = regA + regB
  $AABD  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AABE  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AABF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AAC0  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $AAC1  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $AAC2  D8 16 AB                    branch_z_abs             $AB16                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AAC5  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $AAC6  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AACA  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AACD  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $AACE  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AAD2  D8 20 AB                    branch_z_abs             $AB20                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AAD5  8E 97 FE                    push_imm_word            $FE97                     ; "\nBribe which? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AAD8  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AADC  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AADF  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AAE3  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $AAE6  D8 20 AB                    branch_z_abs             $AB20                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AAE9  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AAEC  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AAEF  D1                          decA                                               ; regA -= 1
  $AAF0  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $AAF3  8E 29 FC                    push_imm_word            $FC29                     ; "Gold for spy"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AAF6  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AAFA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AAFB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AAFC  8F F6                       addA_imm_sbyte           -10                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AAFE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AAFF  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $AB00  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB04  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AB05  D8 20 AB                    branch_z_abs             $AB20                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AB08  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $AB09  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB0A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $AB0B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB0C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AB0D  BC                          sub                                                ; regA = regA - regB
  $AB0E  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $AB0F  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $AB10  E9 4D 8D 02                 host_call                $8D4D (effect_bribe) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB14  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AB15  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AB16  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AB19  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB1D  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$AB20  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AB21  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AB22   (frame_off=-4, body @ $AB27)
; ============================================================
  $AB27  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AB28  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$AB29  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB2A  D2                          aslA                                               ; regA <<= 1
  $AB2B  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $AB2C  BB                          add                                                ; regA = regA + regB
  $AB2D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB2E  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AB31  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $AB32  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AB33  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $AB34  BB                          add                                                ; regA = regA + regB
  $AB35  8C A9 76                    loadB_imm_word           $76A9                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AB38  BB                          add                                                ; regA = regA + regB
  $AB39  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AB3A  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $AB3B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB3C  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AB3E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB3F  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $AB40  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB44  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB45  D2                          aslA                                               ; regA <<= 1
  $AB46  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $AB47  BB                          add                                                ; regA = regA + regB
  $AB48  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AB49  8C 0F 27                    loadB_imm_word           $270F                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AB4C  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $AB4D  D8 59 AB                    branch_z_abs             $AB59                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AB50  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB51  D2                          aslA                                               ; regA <<= 1
  $AB52  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $AB53  BB                          add                                                ; regA = regA + regB
  $AB54  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB55  8A 0F 27                    loadA_imm_word           $270F                     ; regA = 16-bit word literal (handler $EAB9). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB55).
  $AB58  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$AB59  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB5A  D2                          aslA                                               ; regA <<= 1
  $AB5B  1D                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $AB5C  BB                          add                                                ; regA = regA + regB
  $AB5D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AB5E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB5F  8E 43 BE                    push_imm_word            $BE43                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AB62  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB66  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB67  8F 14                       addA_imm_sbyte           +20                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AB69  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB6A  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $AB6B  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB6F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB70  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $AB71  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $AB72  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AB73  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $AB74  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AB75  7D                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 13)
  $AB76  AB 02 00                    op_AB_word               $0002 (ptr1_lo)
  $AB79  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AB7A  AB B9 AB                    op_AB_word               $ABB9
  $AB7D  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AB7E  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AB7F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AB80  D2                          aslA                                               ; regA <<= 1
  $AB81  8C AF F9                    loadB_imm_word           $F9AF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AB84  BB                          add                                                ; regA = regA + regB
  $AB85  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AB86  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB87  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AB88  D0                          incA                                               ; regA += 1
  $AB89  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AB8A  8E 47 BE                    push_imm_word            $BE47                     ; "%d%s"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AB8D  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB91  8D 19                       op_8D_sbyte              +25
  $AB93  65                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 5)
  $AB94  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB98  8E 4C BE                    push_imm_word            $BE4C                     ; "available  0"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AB9B  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AB9F  8D 14                       op_8D_sbyte              +20
  $ABA1  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $ABA2  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ABA6  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $ABA7  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ABAB  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABAC  D0                          incA                                               ; regA += 1
  $ABAD  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $ABAE  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABAF  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $ABB0  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $ABB1  D7 29 AB                    branch_nz_abs            $AB29                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $ABB4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

  ; ---- gap $ABB5-$ABBC (8 bytes, not on any traced path) ----

; ============================================================
; sub $ABBD   (frame_off=-6, body @ $ABC2)
; ============================================================
  $ABC2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ABC3  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $ABC4  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $ABC7  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $ABC8  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $ABC9  8C A9 76                    loadB_imm_word           $76A9                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $ABCC  BB                          add                                                ; regA = regA + regB
  $ABCD  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $ABCE  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ABCF  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$ABD0  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABD1  D2                          aslA                                               ; regA <<= 1
  $ABD2  1C                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $ABD3  BB                          add                                                ; regA = regA + regB
  $ABD4  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $ABD5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $ABD6  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $ABD7  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $ABD8  BB                          add                                                ; regA = regA + regB
  $ABD9  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $ABDA  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $ABDB  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $ABDC  D8 E1 AB                    branch_z_abs             $ABE1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $ABDF  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $ABE0  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
 >$ABE1  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABE2  D0                          incA                                               ; regA += 1
  $ABE3  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $ABE4  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABE5  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $ABE6  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $ABE7  D7 D0 AB                    branch_nz_abs            $ABD0                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $ABEA  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $ABEB  D7 F2 AB                    branch_nz_abs            $ABF2                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $ABEE  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $ABEF  D7 F4 AB                    branch_nz_abs            $ABF4                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$ABF2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ABF3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$ABF4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ABF5  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$ABF6  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $ABF7  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $ABF8  BB                          add                                                ; regA = regA + regB
  $ABF9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $ABFA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ABFB  D2                          aslA                                               ; regA <<= 1
  $ABFC  1C                          loadB_local_pos                                    ; regB = word at frame[+0x0B/0x0D/0x0F/0x11]
  $ABFD  BB                          add                                                ; regA = regA + regB
  $ABFE  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $ABFF  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $AC00  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AC01  D0                          incA                                               ; regA += 1
 >$AC02  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AC03  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AC04  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $AC05  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $AC06  D7 F6 AB                    branch_nz_abs            $ABF6                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AC09  8D 16                       op_8D_sbyte              +22
  $AC0B  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC0F  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AC10  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AC11 (effect_assign)   (frame_off=-18, body @ $AC16)
; ============================================================
  $AC16  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AC19  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $AC1B  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AC1C  8C 17 70                    loadB_imm_word           $7017                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AC1F  BB                          add                                                ; regA = regA + regB
  $AC20  23                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 3)
  $AC21  DE F0                       loadA_frameaddr          $F0                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $AC23  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $AC24  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AC25  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $AC26  E9 22 AB 04                 host_call                $AB22 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC2A  AC 87 D2                    host_call_simple         $D287 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC2D  89 14                       loadA_imm_sbyte          +20                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $AC2F  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $AC30  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AC31  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AC32  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$AC33  AC 4E D1                    host_call_simple         $D14E {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC36  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $AC37  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $AC38  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AC39  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $AC3A  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AC3B  4B                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AC3C  AD 02                       op_AD_byte               $02
  $AC3E  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AC3F  5D                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 13)
  $AC40  AD 10                       op_AD_byte               $10
  $AC42  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AC43  92                          trigger_syscall_9X                                 ; BRK to syscall_dispatch (handler $EF92)
  $AC44  AC 20 00                    host_call_simple         $0020                     ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC47  FA                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $AC48  AC 40 00                    host_call_simple         $0040                     ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC4B  53                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 3)
  $AC4C  AC 80 00                    host_call_simple         $0080 (skip_vblank_wait)  ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC4F  7B                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 11)
  $AC50  AC 33 AC                    host_call_simple         $AC33 {native}            ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AC53  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $AC54  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC58  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AC59  D1                          decA                                               ; regA -= 1
  $AC5A  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $AC5B  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AC5C  D1                          decA                                               ; regA -= 1
  $AC5D  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AC5E  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $AC5F  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $AC60  D8 68 AC                    branch_z_abs             $AC68                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AC63  44                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AC64  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AC65  89 18                       loadA_imm_sbyte          +24                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $AC67  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
 >$AC68  39                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 9)
  $AC69  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $AC6A  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC6E  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $AC6F  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC73  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $AC74  E9 3E D7 02                 host_call                $D73E {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AC78  D6 33 AC                    jump_abs                 $AC33                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)

  ; ---- gap $AC7B-$AD66 (236 bytes, not on any traced path) ----

; ============================================================
; sub $AD67 (driver_assign)   (frame_off=-2, body @ $AD6C)
; ============================================================
  $AD6C  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AD6F  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $AD71  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AD72  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AD75  BB                          add                                                ; regA = regA + regB
  $AD76  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AD77  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AD78  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $AD7A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AD7B  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $AD7C  C3                          cmp_sge                                            ; regA = (regA >= regB) ? 1 : 0  (signed)
  $AD7D  D8 8C AD                    branch_z_abs             $AD8C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AD80  8E FD FD                    push_imm_word            $FDFD                     ; "You have no\nsoldiers!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AD83  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AD87  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AD8A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AD8B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AD8C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AD8D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AD8E  8B 1E                       loadB_imm_byte           +30                       ; regB = byte literal (handler $EABE)
  $AD90  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $AD91  D8 A7 AD                    branch_z_abs             $ADA7                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AD94  8D 1E                       op_8D_sbyte              +30
  $AD96  E9 11 AC 02                 host_call                $AC11 (effect_assign) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AD9A  D8 B1 AD                    branch_z_abs             $ADB1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AD9D  8D 1E                       op_8D_sbyte              +30
  $AD9F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $ADA0  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $ADA1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $ADA2  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $ADA3  BC                          sub                                                ; regA = regA - regB
  $ADA4  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $ADA5  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $ADA6  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$ADA7  8E D9 FD                    push_imm_word            $FDD9                     ; "You have no gold!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $ADAA  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ADAE  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$ADB1  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ADB2  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $ADB3 (driver_rest)   (frame_off=+0, body @ $ADB8)
; ============================================================
  $ADB8  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $ADBB  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $ADBE  BB                          add                                                ; regA = regA + regB
  $ADBF  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $ADC0  D8 EE AD                    branch_z_abs             $ADEE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $ADC3  8E 66 FC                    push_imm_word            $FC66                     ; "Seasons"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $ADC6  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ADCA  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $ADCD  8C 67 6D                    loadB_imm_word           $6D67                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $ADD0  BB                          add                                                ; regA = regA + regB
  $ADD1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $ADD2  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $ADD3  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $ADD4  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ADD8  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $ADD9  D8 F4 AD                    branch_z_abs             $ADF4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $ADDC  8E 6E FC                    push_imm_word            $FC6E                     ; "It will do you\ngood"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $ADDF  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ADE3  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $ADE6  8D 20                       op_8D_sbyte              +32
  $ADE8  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $ADEC  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $ADED  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$ADEE  AC CB 87                    host_call_simple         $87CB (helper_87CB) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $ADF1  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$ADF4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $ADF5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $ADF6   (frame_off=-45, body @ $ADFB)
; ============================================================
  $ADFB  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $ADFC  A2 D7                       op_A0_A3_byte            $D7                       ; 1-byte operand; indexed load/store via ptr0 (handlers $EAED-$EB1E)
  $ADFE  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $ADFF  8D 22                       op_8D_sbyte              +34
  $AE01  DE DA                       loadA_frameaddr          $DA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $AE03  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $AE04  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE05  A4 5D 6F                    loadA_mem_word           $6F5D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AE08  8B 22                       loadB_imm_byte           +34                       ; regB = byte literal (handler $EABE)
  $AE0A  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AE0B  8C 3C 9E                    loadB_imm_word           $9E3C                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AE0E  BB                          add                                                ; regA = regA + regB
  $AE0F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE10  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $AE11  E9 BD CB 08                 host_call                $CBBD {bytecode}, $08     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AE15  DE DA                       loadA_frameaddr          $DA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $AE17  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $AE18  85 D8                       op_85_byte               $D8
  $AE1A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AE1B  85 D3                       op_85_byte               $D3
 >$AE1D  81 D8                       op_81_byte               $D8
  $AE1F  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AE20  8C FF 00                    loadB_imm_word           $00FF                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AE23  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $AE24  D7 56 AE                    branch_nz_abs            $AE56                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AE27  D6 77 AE                    jump_abs                 $AE77                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AE2A  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $AE2B  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AE2C  8F FC                       addA_imm_sbyte           -4                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $AE2E  8B 1C                       loadB_imm_byte           +28                       ; regB = byte literal (handler $EABE)
  $AE30  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AE31  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE32  A4 5D 6F                    loadA_mem_word           $6F5D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AE35  8C C0 01                    loadB_imm_word           $01C0                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AE38  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $AE39  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $AE3A  BB                          add                                                ; regA = regA + regB
  $AE3B  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $AE3C  BB                          add                                                ; regA = regA + regB
  $AE3D  8C 5A 8D                    loadB_imm_word           $8D5A                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AE40  BB                          add                                                ; regA = regA + regB
  $AE41  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE42  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $AE43  8D 1D                       op_8D_sbyte              +29
  $AE45  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $AE46  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $AE47  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE48  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AE4C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AE4D  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $AE4E  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $AE4F  E9 54 CC 0C                 host_call                $CC54 {bytecode}, $0C     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AE53  D6 A2 AE                    jump_abs                 $AEA2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AE56  81 D8                       op_81_byte               $D8
  $AE58  D0                          incA                                               ; regA += 1
  $AE59  85 D8                       op_85_byte               $D8
  $AE5B  D1                          decA                                               ; regA -= 1
  $AE5C  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AE5D  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AE5E  81 D8                       op_81_byte               $D8
  $AE60  D0                          incA                                               ; regA += 1
  $AE61  85 D8                       op_85_byte               $D8
  $AE63  D1                          decA                                               ; regA -= 1
  $AE64  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AE65  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AE66  81 D8                       op_81_byte               $D8
  $AE68  D0                          incA                                               ; regA += 1
  $AE69  85 D8                       op_85_byte               $D8
  $AE6B  D1                          decA                                               ; regA -= 1
  $AE6C  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AE6D  A6 5F 6F                    loadB_mem_word           $6F5F (selected_province_idx) ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $AE70  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AE71  D8 1D AE                    branch_z_abs             $AE1D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AE74  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AE75  85 D3                       op_85_byte               $D3
 >$AE77  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AE78  85 D5                       op_85_byte               $D5
  $AE7A  81 D3                       op_81_byte               $D3
  $AE7C  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $AE7D  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AE7E  D8 A2 AE                    branch_z_abs             $AEA2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AE81  81 D5                       op_81_byte               $D5
  $AE83  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $AE85  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AE86  D7 2A AE                    branch_nz_abs            $AE2A                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AE89  81 D5                       op_81_byte               $D5
  $AE8B  8C C8 00                    loadB_imm_word           $00C8                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AE8E  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AE8F  D8 A2 AE                    branch_z_abs             $AEA2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AE92  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $AE93  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $AE94  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AE98  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $AE9C  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $AE9D  DB                          bitor                                              ; regA = regA | regB
  $AE9E  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $AE9F  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AEA0  85 D5                       op_85_byte               $D5
 >$AEA2  AC 4E D1                    host_call_simple         $D14E {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AEA5  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $AEA6  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $AEA7  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEA8  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $AEA9  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEAA  BE                          lshr_by_regB                                       ; regA >>= regB  (logical shift right by regB count)
  $AEAB  AE 10                       adjust_stack_sbyte       +16                       ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEAD  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEAE  DD                          host_call_indirect_simple                          ; JSR to native code at address in regA; no ptr1 adjust (handler $EB43)
  $AEAF  AE 20                       adjust_stack_sbyte       +32                       ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEB1  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEB2  EE                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $AEB3  AE 40                       adjust_stack_sbyte       +64                       ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEB5  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEB6  EE                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $AEB7  AE 80                       adjust_stack_sbyte       -128                      ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEB9  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $AEBA  DD                          host_call_indirect_simple                          ; JSR to native code at address in regA; no ptr1 adjust (handler $EB43)
  $AEBB  AE CF                       adjust_stack_sbyte       -49                       ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEBD  AE 4A                       adjust_stack_sbyte       +74                       ; ptr1 += signed byte (VM data-stack pointer adjust; handler $EB9E)
  $AEBF  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $AEC0  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $AEC1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

  ; ---- gap $AEC2-$AF0F (78 bytes, not on any traced path) ----

; ============================================================
; sub $AF10 (map_helper_af10)   (frame_off=-4, body @ $AF15)
; ============================================================
  $AF15  8E 69 BE                    push_imm_word            $BE69                     ; "(arrows  )...other sections\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AF18  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF1C  8E 86 BE                    push_imm_word            $BE86                     ; "(A Button)...menu"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AF1F  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF23  D6 2B AF                    jump_abs                 $AF2B                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AF26  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $AF27  E9 F2 E5 02                 host_call                $E5F2 (map_helper_e5f2) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$AF2B  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $AF2C  E9 F6 AD 02                 host_call                $ADF6 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF30  2C                          storeA_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regA
  $AF31  5A                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 10)
  $AF32  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $AF33  D7 26 AF                    branch_nz_abs            $AF26                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AF36  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AF37  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AF38 (driver_map)   (frame_off=-2, body @ $AF3D)
; ============================================================
  $AF3D  AC 20 CD                    host_call_simple         $CD20 (ui_helper_cd20) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AF40  A4 9D 6D                    loadA_mem_word           $6D9D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AF43  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $AF45  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $AF46  D8 52 AF                    branch_z_abs             $AF52                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AF49  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AF4C  8C A6 FE                    loadB_imm_word           $FEA6                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AF4F  D6 58 AF                    jump_abs                 $AF58                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$AF52  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AF55  8C D8 FE                    loadB_imm_word           $FED8                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
 >$AF58  BB                          add                                                ; regA = regA + regB
  $AF59  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AF5A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AF5B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AF5C  E9 F2 E5 02                 host_call                $E5F2 (map_helper_e5f2) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF60  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $AF61  E9 10 AF 02                 host_call                $AF10 (map_helper_af10) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF65  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $AF66 (driver_grant)   (frame_off=-6, body @ $AF6B)
; ============================================================
  $AF6B  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AF6E  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AF71  BB                          add                                                ; regA = regA + regB
  $AF72  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $AF73  D7 7E AF                    branch_nz_abs            $AF7E                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AF76  AC CB 87                    host_call_simple         $87CB (helper_87CB) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AF79  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AF7C  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AF7D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AF7E  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $AF7F  E9 10 E5 02                 host_call                $E510 (ui_helper_e510) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF83  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AF84  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$AF85  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AF88  8D 13                       op_8D_sbyte              +19
  $AF8A  E9 9F 87 04                 host_call                $879F (province_select_helper) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AF8E  D7 9C AF                    branch_nz_abs            $AF9C                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$AF91  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AF92  D8 9A AF                    branch_z_abs             $AF9A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AF95  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $AF96  E9 1E 87 02                 host_call                $871E (fief_info_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$AF9A  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AF9B  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$AF9C  8E 40 FE                    push_imm_word            $FE40                     ; "\nWhich fief "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AF9F  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFA3  8E 89 6F                    push_imm_word            $6F89                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AFA6  E9 4C 80 02                 host_call                $804C (province_select_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFAA  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $AFAD  D8 60 B0                    branch_z_abs             $B060                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $AFB0  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $AFB3  D1                          decA                                               ; regA -= 1
  $AFB4  A8 63 6F                    loadA_mem_word           $6F63                     ; regA = word read using <word> as address (handler $EA75)
  $AFB7  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AFBA  8E 4D FE                    push_imm_word            $FE4D                     ; "\nWhat are your\norders?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AFBD  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFC1  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $AFC2  D7 F5 AF                    branch_nz_abs            $AFF5                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $AFC5  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $AFC6  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $AFC7  AC 69 CC                    host_call_simple         $CC69 (trade_helper_cc69) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $AFCA  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $AFCB  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
 >$AFCC  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AFCD  77                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 7)
  $AFCE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AFCF  8D 17                       op_8D_sbyte              +23
  $AFD1  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFD5  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AFD6  D0                          incA                                               ; regA += 1
  $AFD7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AFD8  8E 98 BE                    push_imm_word            $BE98                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $AFDB  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFDF  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AFE0  D0                          incA                                               ; regA += 1
  $AFE1  D2                          aslA                                               ; regA <<= 1
  $AFE2  8C E9 FE                    loadB_imm_word           $FEE9                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $AFE5  BB                          add                                                ; regA = regA + regB
  $AFE6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $AFE7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $AFE8  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFEC  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AFED  D0                          incA                                               ; regA += 1
  $AFEE  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $AFEF  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $AFF0  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $AFF1  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $AFF2  D7 CC AF                    branch_nz_abs            $AFCC                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$AFF5  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $AFF6  E9 A6 B1 02                 host_call                $B1A6 (submenu_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $AFFA  D0                          incA                                               ; regA += 1
  $AFFB  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $AFFC  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $AFFD  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $AFFE  D8 85 AF                    branch_z_abs             $AF85                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B001  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B004  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B007  BB                          add                                                ; regA = regA + regB
  $B008  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B009  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $B00A  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B00B  D8 23 B0                    branch_z_abs             $B023                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B00E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B00F  D2                          aslA                                               ; regA <<= 1
  $B010  8C D4 F7                    loadB_imm_word           $F7D4                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B013  BB                          add                                                ; regA = regA + regB
  $B014  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B015  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B016  8E 9B BE                    push_imm_word            $BE9B                     ; "It`s already a\n%s\nstate"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B019  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B01D  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B020  D6 85 AF                    jump_abs                 $AF85                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B023  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B024  D2                          aslA                                               ; regA <<= 1
  $B025  8C D4 F7                    loadB_imm_word           $F7D4                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B028  BB                          add                                                ; regA = regA + regB
  $B029  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B02A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B02B  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B02E  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B031  BB                          add                                                ; regA = regA + regB
  $B032  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B033  D2                          aslA                                               ; regA <<= 1
  $B034  8C D4 F7                    loadB_imm_word           $F7D4                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B037  BB                          add                                                ; regA = regA + regB
  $B038  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B039  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B03A  8E 64 FE                    push_imm_word            $FE64                     ; "It`s currently a\n%s state\nOK to make it a\n%s sta"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B03D  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B041  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B044  D8 85 AF                    branch_z_abs             $AF85                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B047  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B04A  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B04D  BB                          add                                                ; regA = regA + regB
  $B04E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B04F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B050  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $B051  6B                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 11)
  $B052  E9 0C E8 02                 host_call                $E80C (ui_helper_e80c) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B056  8E 82 FC                    push_imm_word            $FC82                     ; "Lord, you are\ntruly wise!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B059  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B05D  D6 1D B0                    jump_abs                 $B01D                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B060  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B063  D6 91 AF                    jump_abs                 $AF91                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)

; ============================================================
; sub $B066 (subhandler_B066)   (frame_off=-2, body @ $B06B)
; ============================================================
  $B06B  8E B3 BE                    push_imm_word            $BEB3                     ; "Sound "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B06E  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B072  8E BA BE                    push_imm_word            $BEBA                     ; "(on/off)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B075  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B079  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B07A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B07B  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B07C  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B07D  D8 82 B0                    branch_z_abs             $B082                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B080  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B081  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B082  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B083  A8 4D 6F                    loadA_mem_word           $6F4D                     ; regA = word read using <word> as address (handler $EA75)
  $B086  A4 4D 6F                    loadA_mem_word           $6F4D                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B089  D7 96 B0                    branch_nz_abs            $B096                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B08C  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $B08D  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $B08E  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B08F  E9 26 F2 06                 host_call                $F226 (syscall_dispatch) {native}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B093  D6 9B B0                    jump_abs                 $B09B                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B096  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $B097  E9 03 CA 02                 host_call                $CA03 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B09B  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B09C  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B09D (subhandler_B09D)   (frame_off=+0, body @ $B0A2)
; ============================================================
  $B0A2  8E C5 BE                    push_imm_word            $BEC5                     ; "Animation\n"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B0A5  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0A9  8E D0 BE                    push_imm_word            $BED0                     ; "(on/off)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B0AC  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0B0  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $B0B1  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B0B2  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B0B3  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $B0B4  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B0B5  BD                          shl_by_regB                                        ; regA <<= regB  (shift left by regB count)
  $B0B6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B0B7  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B0B8  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B0B9  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $B0BA  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B0BB  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B0BC  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B0BD  44                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B0BE  CD                          swap_AB                                            ; regA <-> regB
  $B0BF  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B0C2  DB                          bitor                                              ; regA = regA | regB
  $B0C3  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $B0C6  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B0C7  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

  ; ---- gap $B0C8-$B0D1 (10 bytes, not on any traced path) ----

; ============================================================
; sub $B0D2 (subhandler_B0D2)   (frame_off=-2, body @ $B0D7)
; ============================================================
  $B0D7  8E DB BE                    push_imm_word            $BEDB                     ; "Wait is now "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B0DA  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0DE  A4 65 6D                    loadA_mem_word           $6D65                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B0E1  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B0E2  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B0E3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B0E4  E9 CD CB 02                 host_call                $CBCD (sqrt_int) {native}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0E8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B0E9  8E E8 BE                    push_imm_word            $BEE8                     ; "%d\nEnter new wait"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B0EC  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0F0  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B0F1  61                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 1)
  $B0F2  E9 E9 D5 04                 host_call                $D5E9 (number_input) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B0F6  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B0F7  D8 04 B1                    branch_z_abs             $B104                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B0FA  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B0FB  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $B0FC  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B0FD  D2                          aslA                                               ; regA <<= 1
  $B0FE  A8 65 6D                    loadA_mem_word           $6D65                     ; regA = word read using <word> as address (handler $EA75)
  $B101  D6 07 B1                    jump_abs                 $B107                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B104  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B107  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B108  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B109 (subhandler_B109)   (frame_off=+0, body @ $B10E)
; ============================================================
  $B10E  8E FA BE                    push_imm_word            $BEFA                     ; "Are you sure"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B111  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B115  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B118  D8 29 B1                    branch_z_abs             $B129                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B11B  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B11C  A8 61 6F                    loadA_mem_word           $6F61                     ; regA = word read using <word> as address (handler $EA75)
  $B11F  8E 07 BF                    push_imm_word            $BF07                     ; "Game will save at\nend of season"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B122  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B126  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B129  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B12A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B12B (subhandler_B12B)   (frame_off=-2, body @ $B130)
; ============================================================
  $B130  8E 27 BF                    push_imm_word            $BF27                     ; "Watch others\nbattle "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B133  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B137  8E 3C BF                    push_imm_word            $BF3C                     ; "(Y/N)? "  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B13A  E9 51 D3 02                 host_call                $D351 (ui_helper_d351) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B13E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B13F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B140  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B141  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $B142  D8 49 B1                    branch_z_abs             $B149                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B145  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B146  A8 7D 6E                    loadA_mem_word           $6E7D                     ; regA = word read using <word> as address (handler $EA75)
 >$B149  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B14A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B14B (subhandler_B14B)   (frame_off=-4, body @ $B150)
; ============================================================
  $B150  8E 44 BF                    push_imm_word            $BF44                     ; "Do you really\nwant to end the\ngame"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B153  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B157  AC A7 D3                    host_call_simple         $D3A7 (ui_helper_d3a7) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B15A  D8 A4 B1                    branch_z_abs             $B1A4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B15D  AC 35 DB                    host_call_simple         $DB35 (ui_helper_db35) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B160  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B163  8C 80 00                    loadB_imm_word           $0080 (skip_vblank_wait)  ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B166  DA                          bitand                                             ; regA = regA & regB
  $B167  D8 7D B1                    branch_z_abs             $B17D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B16A  AC 20 CD                    host_call_simple         $CD20 (ui_helper_cd20) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B16D  6E                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 14)
  $B16E  6C                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 12)
  $B16F  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B173  8E 67 BF                    push_imm_word            $BF67                     ; "Game over"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B176  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B17A  D6 7A B1                    jump_abs                 $B17A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B17D  AC 7E D7                    host_call_simple         $D77E (ui_helper_d77e) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B180  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B181  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B182  D6 99 B1                    jump_abs                 $B199                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B185  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B186  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B18A  1B                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 11)
  $B18B  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B18C  D8 97 B1                    branch_z_abs             $B197                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B18F  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B190  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B193  BB                          add                                                ; regA = regA + regB
  $B194  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B195  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B196  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
 >$B197  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B198  D0                          incA                                               ; regA += 1
 >$B199  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B19A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B19B  A6 9D 6D                    loadB_mem_word           $6D9D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $B19E  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B19F  D7 85 B1                    branch_nz_abs            $B185                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B1A2  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B1A3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B1A4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B1A5  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B1A6 (submenu_prompt)   (frame_off=-4, body @ $B1AB)
; ============================================================
  $B1AB  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $B1AC  8D 15                       op_8D_sbyte              +21
  $B1AE  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B1B2  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $B1B3  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B1B7  AC 87 D2                    host_call_simple         $D287 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B1BA  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B1BB  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B1BC  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B1BD  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$B1BE  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B1BF  D8 C4 B1                    branch_z_abs             $B1C4                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B1C2  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B1C3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B1C4  AC 4E D1                    host_call_simple         $D14E {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B1C7  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $B1C8  04                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 4)
  $B1C9  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B1CA  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $B1CB  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B1CC  DF B1                       loadB_frameaddr          $B1                       ; regB = ptr2 + byte  (address of a frame local; handler $EAA0)
  $B1CE  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B1CF  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B1D0  DC                          bitxor                                             ; regA = regA ^ regB
  $B1D1  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B1D2  10                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 0)
  $B1D3  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B1D4  EC                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $B1D5  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B1D6  20                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 0)
  $B1D7  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B1D8  1A                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 10)
  $B1D9  B2                          pushA_B2                                           ; Push regA to VM data stack (handler $F020)
  $B1DA  BE                          lshr_by_regB                                       ; regA >>= regB  (logical shift right by regB count)
  $B1DB  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B1DC  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B1DD  D1                          decA                                               ; regA -= 1
  $B1DE  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B1DF  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $B1E0  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B1E4  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B1E5  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B1E6  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B1E9  D6 BE B1                    jump_abs                 $B1BE                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)

  ; ---- gap $B1EC-$B23D (82 bytes, not on any traced path) ----

; ============================================================
; sub $B23E (driver_other)   (frame_off=-7, body @ $B243)
; ============================================================
  $B243  AC 69 CC                    host_call_simple         $CC69 (trade_helper_cc69) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B246  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B247  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
 >$B248  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B249  77                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 7)
  $B24A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B24B  8D 16                       op_8D_sbyte              +22
  $B24D  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B251  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B252  D0                          incA                                               ; regA += 1
  $B253  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B254  8E 71 BF                    push_imm_word            $BF71                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B257  E9 34 D1 04                 host_call                $D134 (ui_helper_d134) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B25B  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B25C  D2                          aslA                                               ; regA <<= 1
  $B25D  8C 1A FF                    loadB_imm_word           $FF1A                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B260  BB                          add                                                ; regA = regA + regB
  $B261  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B262  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B263  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B267  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B268  D0                          incA                                               ; regA += 1
  $B269  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B26A  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B26B  57                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 7)
  $B26C  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $B26D  D7 48 B2                    branch_nz_abs            $B248                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$B270  8E 75 BF                    push_imm_word            $BF75                     ; "Change which?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B273  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B277  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $B278  E9 A6 B1 02                 host_call                $B1A6 (submenu_prompt) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B27C  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B27D  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B27E  D2                          aslA                                               ; regA <<= 1
  $B27F  8C E8 B9                    loadB_imm_word           $B9E8                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B282  BB                          add                                                ; regA = regA + regB
  $B283  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B284  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B285  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B286  DD                          host_call_indirect_simple                          ; JSR to native code at address in regA; no ptr1 adjust (handler $EB43)
  $B287  A2 F9                       op_A0_A3_byte            $F9                       ; 1-byte operand; indexed load/store via ptr0 (handlers $EAED-$EB1E)
  $B289  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B28A  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B28B  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $B28C  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $B28D  D8 98 B2                    branch_z_abs             $B298                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B290  A0 F9                       op_A0_A3_byte            $F9                       ; 1-byte operand; indexed load/store via ptr0 (handlers $EAED-$EB1E)
  $B292  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B293  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $B294  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $B295  D7 70 B2                    branch_nz_abs            $B270                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$B298  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $B299  E9 1E 87 02                 host_call                $871E (fief_info_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B29D  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B29E  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $B29F  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B2A0  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B2A1 (driver_pass)   (frame_off=+0, body @ $B2A6)
; ============================================================
  $B2A6  8E 83 BF                    push_imm_word            $BF83                     ; "What a waste!"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B2A9  E9 26 D3 02                 host_call                $D326 (message_display) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B2AD  AC 66 D7                    host_call_simple         $D766 (confirm_prompt) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B2B0  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B2B1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B2B2   (frame_off=+0, body @ $B2B7)
; ============================================================
  $B2B7  8E 63 6F                    push_imm_word            $6F63                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B2BA  DE 0B                       loadA_frameaddr          $0B                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B2BC  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B2BD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B2BE  E9 94 CB 04                 host_call                $CB94 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B2C2  A4 63 6F                    loadA_mem_word           $6F63                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B2C5  8C 2D 6D                    loadB_imm_word           $6D2D                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B2C8  BB                          add                                                ; regA = regA + regB
  $B2C9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B2CA  AC 12 DB                    host_call_simple         $DB12 (tax_helper_db12) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B2CD  D8 DA B2                    branch_z_abs             $B2DA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B2D0  6F                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 15)
  $B2D1  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B2D5  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B2D7  D6 E2 B2                    jump_abs                 $B2E2                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B2DA  8D 1E                       op_8D_sbyte              +30
  $B2DC  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B2E0  8F 23                       addA_imm_sbyte           +35                       ; regA += sign-extended signed byte literal (handler $EAD9)
 >$B2E2  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $B2E3  8E 63 6F                    push_imm_word            $6F63                     ; Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B2E6  DE 0B                       loadA_frameaddr          $0B                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B2E8  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B2E9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B2EA  E9 94 CB 04                 host_call                $CB94 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B2EE  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B2EF   (frame_off=-2, body @ $B2F4)
; ============================================================
  $B2F4  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B2F7  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B2F9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B2FA  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B2FD  BB                          add                                                ; regA = regA + regB
  $B2FE  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B2FF  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B300  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B301  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B303  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B304  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B305  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B306  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B307  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B308  BC                          sub                                                ; regA = regA - regB
  $B309  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B30A  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B30B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B30C  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B30E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B30F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B310  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B311  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B312  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B313  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B314  BC                          sub                                                ; regA = regA - regB
  $B315  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B316  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B317  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B318  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $B319  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $B31A  D8 20 B3                    branch_z_abs             $B320                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B31D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B31E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B31F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B320  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B321  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B322  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $B323  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $B324  D8 2A B3                    branch_z_abs             $B32A                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B327  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B328  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B329  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B32A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B32B   (frame_off=+0, body @ $B330)
; ============================================================
  $B330  8D 14                       op_8D_sbyte              +20
  $B332  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B336  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $B337  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B338   (frame_off=-4, body @ $B33D)
; ============================================================
  $B33D  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $B33E  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B342  D7 46 B3                    branch_nz_abs            $B346                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B345  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B346  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B349  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B34B  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B34C  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B34F  BB                          add                                                ; regA = regA + regB
  $B350  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B351  AC 2B B3                    host_call_simple         $B32B {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B354  A6 0D 6E                    loadB_mem_word           $6E0D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $B357  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B358  D8 67 B3                    branch_z_abs             $B367                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B35B  0D                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B35C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B35D  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B35E  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B35F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B360  E9 0A 8B 02                 host_call                $8B0A (effect_subhandler_A003) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B364  D6 A3 B3                    jump_abs                 $B3A3                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B367  AC 2B B3                    host_call_simple         $B32B {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B36A  A6 0D 6E                    loadB_mem_word           $6E0D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $B36D  C8                          cmp_ugt                                            ; regA = (regA > regB) ? 1 : 0   (unsigned)
  $B36E  D8 A3 B3                    branch_z_abs             $B3A3                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B371  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B372  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B373  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B374  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B375  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B376  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B378  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B379  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B37A  BC                          sub                                                ; regA = regA - regB
  $B37B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B37C  AA 0D 6E 0C                 op_AA_wb                 $6E0D, $0C
  $B380  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B381  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B382  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B386  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B387  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B388  AA 0D 6E E9                 op_AA_wb                 $6E0D, $E9
  $B38C  03                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 3)
  $B38D  83 04                       op_83_byte               $04
  $B38F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B390  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B391  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B392  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B393  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B394  BC                          sub                                                ; regA = regA - regB
  $B395  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B396  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B397  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B398  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B399  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B39A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B39B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B39C  BB                          add                                                ; regA = regA + regB
  $B39D  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B39E  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $B39F  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B3A3  3D                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B3A4  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B3A5  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B3A9  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B3AA   (frame_off=-8, body @ $B3AF)
; ============================================================
  $B3AF  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B3B0  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B3B1  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B3B2  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B3B5  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B3B7  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B3B8  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B3BB  BB                          add                                                ; regA = regA + regB
  $B3BC  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B3BD  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B3BF  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B3C0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3C1  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B3C3  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B3C4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3C5  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B3C9  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B3CD  B2                          pushA_B2                                           ; Push regA to VM data stack (handler $F020)
  $B3CE  B2                          pushA_B2                                           ; Push regA to VM data stack (handler $F020)
  $B3CF  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B3D0  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B3D2  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B3D3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3D4  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B3D6  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B3D7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3D8  E9 38 B3 04                 host_call                $B338 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B3DC  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B3DD  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B3DE  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $B3DF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B3E0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3E1  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B3E2  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B3E4  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B3E5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3E6  A4 9F 6D                    loadA_mem_word           $6D9F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B3E9  90 E9 F9                    op_90_bb                 $E9, $F9
  $B3EC  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $B3EE  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B3EF  8F 64                       addA_imm_sbyte           +100                      ; regA += sign-extended signed byte literal (handler $EAD9)
  $B3F1  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3F2  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B3F6  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B3F7  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B3FB  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B3FC  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B3FD  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B3FF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B400  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B401  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B402  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $B403  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B404  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B405  C2                          cmp_slt                                            ; regA = (regA < regB) ? 1 : 0   (signed)
  $B406  D8 26 B4                    branch_z_abs             $B426                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B409  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $B40A  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B40E  D7 26 B4                    branch_nz_abs            $B426                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B411  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B412  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $B413  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $B414  D8 26 B4                    branch_z_abs             $B426                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B417  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $B418  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B419  74                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 4)
  $B41A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B41B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B41C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B41D  BB                          add                                                ; regA = regA + regB
  $B41E  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B41F  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $B420  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B421  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B422  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B423  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B424  BC                          sub                                                ; regA = regA - regB
  $B425  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B426  AC C9 95                    host_call_simple         $95C9 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B429  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B42A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B42B   (frame_off=-12, body @ $B430)
; ============================================================
  $B430  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B431  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B432  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B433  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B435  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B436  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B437  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B439  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B43A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B43B  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B43F  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B443  B2                          pushA_B2                                           ; Push regA to VM data stack (handler $F020)
  $B444  B2                          pushA_B2                                           ; Push regA to VM data stack (handler $F020)
  $B445  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B446  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B447  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $B448  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B449  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B44A  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B44D  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B44F  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B450  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B453  BB                          add                                                ; regA = regA + regB
  $B454  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B455  7C                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 12)
  $B456  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B457  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B458  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $B459  D8 C9 B4                    branch_z_abs             $B4C9                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B45C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B45D  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $B45E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B45F  D8 97 B4                    branch_z_abs             $B497                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B462  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B463  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $B464  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B465  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B466  89 64                       loadA_imm_sbyte          +100                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $B468  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B469  BC                          sub                                                ; regA = regA - regB
  $B46A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B46B  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $B46C  E9 D8 87 02                 host_call                $87D8 (effect_dam) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B470  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B471  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B472  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B473  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B474  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B478  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $B479  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $B47A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B47B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B47C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B47D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B47E  BC                          sub                                                ; regA = regA - regB
  $B47F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B480  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $B481  E9 D8 87 02                 host_call                $87D8 (effect_dam) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B485  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B486  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $B487  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B488  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B489  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $B48A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B48B  7A                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 10)
  $B48C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B48D  37                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 7)
  $B48E  E9 7D 88 04                 host_call                $887D (helper_dam_rounding) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B492  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $B493  CD                          swap_AB                                            ; regA <-> regB
  $B494  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B495  BC                          sub                                                ; regA = regA - regB
  $B496  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
 >$B497  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B498  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B499  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $B49A  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B49B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B49C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B49D  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B49F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B4A0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4A1  A4 9F 6D                    loadA_mem_word           $6D9F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B4A4  90 E9 F9                    op_90_bb                 $E9, $F9
  $B4A7  8B 32                       loadB_imm_byte           +50                       ; regB = byte literal (handler $EABE)
  $B4A9  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B4AA  90 FA 00                    op_90_bb                 $FA, $00
  $B4AD  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4AE  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B4B2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4B3  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B4B7  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $B4B8  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $B4B9  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B4BA  78                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 8)
  $B4BB  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B4BC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4BD  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B4BE  BB                          add                                                ; regA = regA + regB
  $B4BF  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B4C0  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $B4C1  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $B4C2  E9 F0 87 04                 host_call                $87F0 (effect_grow) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B4C6  D6 CC B4                    jump_abs                 $B4CC                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B4C9  AC C9 95                    host_call_simple         $95C9 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B4CC  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B4D0  36                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 6)
  $B4D1  D8 02 41                    branch_z_abs             $4102                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B4D4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B4D5   (frame_off=-8, body @ $B4DA)
; ============================================================
  $B4DA  AC 9A 94                    host_call_simple         $949A {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B4DD  D8 E2 B4                    branch_z_abs             $B4E2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B4E0  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B4E1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B4E2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B4E3  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B4E4  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B4E5  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B4E7  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B4E8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4E9  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B4EB  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B4EC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B4ED  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B4F1  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B4F4  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B4F6  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B4F7  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B4FA  BB                          add                                                ; regA = regA + regB
  $B4FB  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B4FC  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B4FD  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B4FF  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B500  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B501  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B502  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B504  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B505  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B506  A4 9F 6D                    loadA_mem_word           $6D9F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B509  90 E9 F9                    op_90_bb                 $E9, $F9
  $B50C  8B 28                       loadB_imm_byte           +40                       ; regB = byte literal (handler $EABE)
  $B50E  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B50F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B510  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B514  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B515  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $B516  D8 71 B5                    branch_z_abs             $B571                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B519  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B51A  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B51C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B51D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B51E  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B51F  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B521  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B522  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B523  A4 9F 6D                    loadA_mem_word           $6D9F                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B526  90 E9 F9                    op_90_bb                 $E9, $F9
  $B529  8B 64                       loadB_imm_byte           +100                      ; regB = byte literal (handler $EABE)
  $B52B  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B52C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B52D  E9 5E CB 04                 host_call                $CB5E (ui_helper_cb5e) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B531  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B532  BC                          sub                                                ; regA = regA - regB
  $B533  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B534  AA 11 6E 0A                 op_AA_wb                 $6E11, $0A
  $B538  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B539  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B53A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B53B  E9 57 83 06                 host_call                $8357 (helper_8357) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B53F  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B540  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B541  50                          setB_imm4_50                                       ; regB = 0 (variant handler $EA11)
  $B542  C4                          cmp_sgt                                            ; regA = (regA > regB) ? 1 : 0   (signed)
  $B543  D8 71 B5                    branch_z_abs             $B571                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B546  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B549  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $B54A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B54B  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B54C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B550  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B551  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B552  D8 71 B5                    branch_z_abs             $B571                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B555  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B556  52                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 2)
  $B557  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B558  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B559  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B55A  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B55B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B55C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B55D  BC                          sub                                                ; regA = regA - regB
  $B55E  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B55F  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $B560  3B                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 11)
  $B561  E9 F4 8B 04                 host_call                $8BF4 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B565  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B567  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B568  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B569  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B56B  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B56C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B56D  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B571  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B572  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B574  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B575  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B576  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B577  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B578  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B579  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B57A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B57B  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B57D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B57E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B57F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B580  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B582  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B583  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B584  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B588  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B589  38                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 8)
  $B58A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B58B  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B58D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B58E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B58F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B590  BB                          add                                                ; regA = regA + regB
  $B591  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B592  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B593  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B595  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B596  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $B597  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B598  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B599  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B59A  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B59B  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B59C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B59D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B59E  BC                          sub                                                ; regA = regA - regB
  $B59F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B5A0  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5A1  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B5A3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5A4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5A5  AA 0F 6E 0B                 op_AA_wb                 $6E0F, $0B
  $B5A9  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B5AB  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5AC  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5AD  E9 27 83 06                 host_call                $8327 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B5B1  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B5B2  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5B3  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5B4  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $B5B5  C5                          cmp_sle                                            ; regA = (regA <= regB) ? 1 : 0  (signed)
  $B5B6  D8 F1 B5                    branch_z_abs             $B5F1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B5B9  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B5BA  8B FF                       loadB_imm_byte           -1                        ; regB = byte literal (handler $EABE)
  $B5BC  C1                          cmp_ne                                             ; regA = (regA != regB) ? 1 : 0
  $B5BD  D8 F1 B5                    branch_z_abs             $B5F1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B5C0  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B5C1  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $B5C2  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B5C3  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B5C4  D8 F1 B5                    branch_z_abs             $B5F1                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B5C7  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5C8  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5C9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5CA  AA 0F 6E 38                 op_AA_wb                 $6E0F, $38
  $B5CE  E9 27 83 06                 host_call                $8327 {bytecode}, $06     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B5D2  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5D3  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5D4  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B5D5  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5D6  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5D7  BC                          sub                                                ; regA = regA - regB
  $B5D8  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B5D9  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B5DA  D2                          aslA                                               ; regA <<= 1
  $B5DB  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5DC  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5DD  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B5DF  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B5E0  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5E1  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B5E2  BB                          add                                                ; regA = regA + regB
  $B5E3  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B5E4  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5E5  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B5E7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5E8  E9 AC 82 02                 host_call                $82AC (helper_82AC) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B5EC  63                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 3)
  $B5ED  E9 4E 8A 02                 host_call                $8A4E (helper_8A4E) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B5F1  DE FA                       loadA_frameaddr          $FA                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B5F3  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B5F4  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5F5  DE FC                       loadA_frameaddr          $FC                       ; regA = ptr2 + byte  (address of a frame local; handler $EA91)
  $B5F7  FF                          op_FF                                              ; No operand; handler $EF93 (BRK signature byte region)
  $B5F8  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B5F9  E9 EF B2 04                 host_call                $B2EF {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B5FD  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B5FE  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B600  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B601  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B602  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B603  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B604  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B605  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B606  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B607  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B609  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B60A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B60B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B60C  8F 18                       addA_imm_sbyte           +24                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B60E  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B60F  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B610  E9 E5 8B 06                 host_call                $8BE5 (effect_send) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B614  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B615  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B616  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B61A  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $B61B  BB                          add                                                ; regA = regA + regB
  $B61C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B61D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B61E  8F 12                       addA_imm_sbyte           +18                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B620  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B621  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B622  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B623  BB                          add                                                ; regA = regA + regB
  $B624  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B625  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B626  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B628  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B629  18                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 8)
  $B62A  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B62B  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B62C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B62D  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B62E  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B62F  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B630  BC                          sub                                                ; regA = regA - regB
  $B631  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B632  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $B633  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B637  D8 3D B6                    branch_z_abs             $B63D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B63A  AC 86 95                    host_call_simple         $9586 (effect_train) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B63D  8D 64                       op_8D_sbyte              +100
  $B63F  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B643  D7 49 B6                    branch_nz_abs            $B649                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B646  AC 0C 96                    host_call_simple         $960C {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B649  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B64A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B64B   (frame_off=-2, body @ $B650)
; ============================================================
  $B650  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B653  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B655  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B656  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B659  BB                          add                                                ; regA = regA + regB
  $B65A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B65B  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B65C  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B65D  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B65E  D7 6E B6                    branch_nz_abs            $B66E                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B661  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B662  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B666  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B667  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B668  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B669  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B66A  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B66B  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B66C  BB                          add                                                ; regA = regA + regB
  $B66D  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B66E  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B671  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B674  BB                          add                                                ; regA = regA + regB
  $B675  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B676  D8 96 B6                    branch_z_abs             $B696                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B679  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B67D  8D D9                       op_8D_sbyte              -39
  $B67F  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B680  D7 96 B6                    branch_nz_abs            $B696                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B683  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B684  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B686  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B687  D7 96 B6                    branch_nz_abs            $B696                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B68A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B68B  8F 10                       addA_imm_sbyte           +16                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B68D  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B68E  42                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B68F  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
  $B690  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B691  76                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 6)
  $B692  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B693  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B694  D0                          incA                                               ; regA += 1
  $B695  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B696  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B697  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B69B  D8 A5 B6                    branch_z_abs             $B6A5                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B69E  AC D5 B4                    host_call_simple         $B4D5 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B6A1  D8 A5 B6                    branch_z_abs             $B6A5                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B6A4  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B6A5  6A                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 10)
  $B6A6  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B6AA  D8 B0 B6                    branch_z_abs             $B6B0                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B6AD  AC AA B3                    host_call_simple         $B3AA {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B6B0  AC 2B B4                    host_call_simple         $B42B {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B6B3  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B6B4   (frame_off=-4, body @ $B6B9)
; ============================================================
  $B6B9  67                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 7)
  $B6BA  8D 15                       op_8D_sbyte              +21
  $B6BC  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B6C0  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $B6C1  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B6C5  AC 87 D2                    host_call_simple         $D287 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B6C8  A4 E2 7B                    loadA_mem_word           $7BE2                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B6CB  D8 D2 B6                    branch_z_abs             $B6D2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B6CE  4C                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B6CF  D6 D3 B6                    jump_abs                 $B6D3                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B6D2  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
 >$B6D3  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B6D4  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B6D5  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$B6D6  AC 4E D1                    host_call_simple         $D14E {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B6D9  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $B6DA  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $B6DB  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6DC  01                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 1)
  $B6DD  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6DE  F9                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $B6DF  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B6E0  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B6E1  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6E2  F6                          trigger_syscall_EB_FE                              ; BRK to syscall_dispatch (handler $EF92)
  $B6E3  B6                          div_signed                                         ; regA = regA / regB  (signed quotient)
  $B6E4  10                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 0)
  $B6E5  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6E6  5D                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 13)
  $B6E7  B7 20                       ext_op                   $20                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $B6E9  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6EA  73                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 3)
  $B6EB  B7 40                       ext_op                   $40                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $B6ED  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6EE  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B6EF  B7 80                       ext_op                   $80                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $B6F1  00                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 0)
  $B6F2  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B6F3  B7 03                       ext_op                   $03                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $B6F5  B7 89                       ext_op                   $89                       ; EXTENDED-OPCODE PREFIX. Handler $F246 reads the next byte as a sub-opcode index and dispatches through a ~47-entry secondary handler table at $F263. So `B7 xx` is a 2-byte instruction = extended opcode xx. The extended set appears to hold the VM's wide/32-bit arithmetic — the first handler ($F2C1) zeroes $10-$13 and runs a 32-iteration loop. Decoding the extended table is a future session; for now the disassembler stays byte-aligned by consuming the 1 sub-opcode byte.
  $B6F7  11                          loadB_local_neg                                    ; regB = word at frame[-24 + 2*(n-0x10)]  (inline operand = 1)
  $B6F8  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B6F9  60                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 0)
  $B6FA  E9 9D D2 02                 host_call                $D29D {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B6FE  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B6FF  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B700  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B703  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B704  D8 D6 B6                    branch_z_abs             $B6D6                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B707  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B708  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

  ; ---- gap $B709-$B79A (146 bytes, not on any traced path) ----

; ============================================================
; sub $B79B   (frame_off=-16, body @ $B7A0)
; ============================================================
  $B7A0  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B7A3  A8 DD 7F                    loadA_mem_word           $7FDD                     ; regA = word read using <word> as address (handler $EA75)
  $B7A6  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B7A7  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B7AB  8C 67 6D                    loadB_imm_word           $6D67                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B7AE  BB                          add                                                ; regA = regA + regB
  $B7AF  28                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 8)
  $B7B0  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B7B1  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B7B2  D8 DA B7                    branch_z_abs             $B7DA                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B7B5  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B7B6  8C A2 6D                    loadB_imm_word           $6DA2                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B7B9  BB                          add                                                ; regA = regA + regB
  $B7BA  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B7BB  D8 74 B8                    branch_z_abs             $B874                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B7BE  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B7BF  E9 FA 83 02                 host_call                $83FA (effect_view_a) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B7C3  AC 59 D7                    host_call_simple         $D759 (ui_helper_d759) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B7C6  08                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 8)
  $B7C7  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B7C8  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B7C9  D1                          decA                                               ; regA -= 1
  $B7CA  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $B7CB  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $B7CC  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B7CD  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B7D1  D0                          incA                                               ; regA += 1
  $B7D2  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B7D3  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B7D4  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B7D5  BB                          add                                                ; regA = regA + regB
  $B7D6  D4                          storeA_ind_byte                                    ; byte at [regB] = regA low byte; regB popped from VM data stack
  $B7D7  D6 74 B8                    jump_abs                 $B874                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B7DA  AC A7 85                    host_call_simple         $85A7 (effect_view_c) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B7DD  8D 1B                       op_8D_sbyte              +27
  $B7DF  E9 03 CA 02                 host_call                $CA03 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B7E3  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B7E4  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $B7E5  D6 10 B8                    jump_abs                 $B810                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B7E8  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $B7E9  8B 11                       loadB_imm_byte           +17                       ; regB = byte literal (handler $EABE)
  $B7EB  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B7EC  D8 F2 B7                    branch_z_abs             $B7F2                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B7EF  AC A7 85                    host_call_simple         $85A7 (effect_view_c) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B7F2  AC 89 CC                    host_call_simple         $CC89 (ui_helper_cc89) {bytecode} ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B7F5  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B7F9  8B D7                       loadB_imm_byte           -41                       ; regB = byte literal (handler $EABE)
  $B7FB  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B7FC  8E 91 BF                    push_imm_word            $BF91                     ; "\nyour orders?"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B7FF  E9 C4 CE 02                 host_call                $CEC4 (redraw_window) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B803  AC B4 B6                    host_call_simple         $B6B4 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B806  27                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 7)
  $B807  D2                          aslA                                               ; regA <<= 1
  $B808  8C B2 B9                    loadB_imm_word           $B9B2 (command_driver_table) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B80B  BB                          add                                                ; regA = regA + regB
  $B80C  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B80D  25                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 5)
  $B80E  05                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 5)
  $B80F  DD                          host_call_indirect_simple                          ; JSR to native code at address in regA; no ptr1 adjust (handler $EB43)
 >$B810  26                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 6)
  $B811  06                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 6)
  $B812  D8 E8 B7                    branch_z_abs             $B7E8                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B815  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $B816  51                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 1)
  $B817  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B818  D7 74 B8                    branch_nz_abs            $B874                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B81B  07                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 7)
  $B81C  8B 14                       loadB_imm_byte           +20                       ; regB = byte literal (handler $EABE)
  $B81E  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B81F  D7 74 B8                    branch_nz_abs            $B874                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B822  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B823  E9 36 D8 02                 host_call                $D836 {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B827  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B828  E9 DA D7 02                 host_call                $D7DA {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B82C  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B82D  0C                          loadA_local_pos                                    ; regA = word at frame[+0x0B/0x0D/0x0F/0x11]  (handlers $E8C4-$E8D0)
  $B82E  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B830  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B831  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B834  BB                          add                                                ; regA = regA + regB
  $B835  24                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 4)
  $B836  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B837  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B838  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
 >$B839  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B83A  56                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 6)
  $B83B  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B83C  D8 55 B8                    branch_z_abs             $B855                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B83F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B840  7D                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 13)
  $B841  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B842  66                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 6)
  $B843  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B847  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B848  D0                          incA                                               ; regA += 1
  $B849  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B84A  D1                          decA                                               ; regA -= 1
  $B84B  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B84C  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B84D  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B84E  E9 D5 83 04                 host_call                $83D5 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B852  D6 6A B8                    jump_abs                 $B86A                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B855  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B856  D0                          incA                                               ; regA += 1
  $B857  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B858  8D 10                       op_8D_sbyte              +16
  $B85A  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B85E  09                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 9)
  $B85F  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $B860  29                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 9)
  $B861  8F FE                       addA_imm_sbyte           -2                        ; regA += sign-extended signed byte literal (handler $EAD9)
  $B863  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B864  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B865  3C                          storeB_local_pos                                   ; word at frame[+0x0B/0x0D/0x0F/0x11] = regB
  $B866  E9 A2 83 04                 host_call                $83A2 {bytecode}, $04     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B86A  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B86B  D0                          incA                                               ; regA += 1
  $B86C  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B86D  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B86E  8B 12                       loadB_imm_byte           +18                       ; regB = byte literal (handler $EABE)
  $B870  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B871  D7 39 B8                    branch_nz_abs            $B839                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$B874  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B875   (frame_off=-2, body @ $B87A)
; ============================================================
  $B87A  AC 4B B6                    host_call_simple         $B64B {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B87D  AC 68 8C                    host_call_simple         $8C68 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B880  A4 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B883  8B 1A                       loadB_imm_byte           +26                       ; regB = byte literal (handler $EABE)
  $B885  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B886  8C 01 70                    loadB_imm_word           $7001 (province_table_live) ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B889  BB                          add                                                ; regA = regA + regB
  $B88A  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B88B  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $B88C  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B890  D7 9A B8                    branch_nz_abs            $B89A                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B893  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B894  8F 16                       addA_imm_sbyte           +22                       ; regA += sign-extended signed byte literal (handler $EAD9)
  $B896  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B897  B0                          loadA_ind_word                                     ; regA = word at [regA]  (dereference regA as pointer)
  $B898  D0                          incA                                               ; regA += 1
  $B899  B1                          storeA_ind_word                                    ; word at [regB] = regA; regB is popped from the VM data stack
 >$B89A  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ============================================================
; sub $B89B   (frame_off=-4, body @ $B8A0)
; ============================================================
  $B8A0  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B8A1  A8 D3 7F                    loadA_mem_word           $7FD3                     ; regA = word read using <word> as address (handler $EA75)
  $B8A4  A4 01 60                    loadA_mem_word           $6001                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B8A7  D6 9E B9                    jump_abs                 $B99E                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B8AA  89 FE                       loadA_imm_sbyte          -2                        ; regA = sign-extended signed byte literal (handler $EAAF)
  $B8AC  CD                          swap_AB                                            ; regA <-> regB
  $B8AD  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B8B0  DA                          bitand                                             ; regA = regA & regB
  $B8B1  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $B8B4  A4 63 6D                    loadA_mem_word           $6D63 (const_two)         ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B8B7  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $B8B8  B5                          mul                                                ; regA = regA * regB  (16-bit shift-add multiply)
  $B8B9  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B8BA  89 37                       loadA_imm_sbyte          +55                       ; regA = sign-extended signed byte literal (handler $EAAF)
  $B8BC  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B8BD  BC                          sub                                                ; regA = regA - regB
  $B8BE  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B8BF  8D 64                       op_8D_sbyte              +100
  $B8C1  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B8C5  B4                          popB                                               ; regB = pop from VM data stack (handler $ECD5 -> $ECDB)
  $B8C6  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B8C7  CD                          swap_AB                                            ; regA <-> regB
  $B8C8  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B8CB  DB                          bitor                                              ; regA = regA | regB
  $B8CC  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $B8CF  AA 5F 6F E9                 op_AA_wb                 $6F5F (selected_province_idx), $E9
  $B8D3  72                          addA_imm4                                          ; regA += (opcode & 0x0F)  (16-bit add with carry)  (inline operand = 2)
  $B8D4  D9                          op_D9                                              ; handler $EC1D (TBD — reads via ptr3)
  $B8D5  02                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 2)
  $B8D6  D8 EE B8                    branch_z_abs             $B8EE                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B8D9  64                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 4)
  $B8DA  E9 52 CA 02                 host_call                $CA52 (ui_helper_ca52) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B8DE  D7 E5 B8                    branch_nz_abs            $B8E5                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B8E1  41                          clearA                                             ; regA = 0 (shared handler $E9F6)
  $B8E2  D6 E6 B8                    jump_abs                 $B8E6                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B8E5  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
 >$B8E6  CD                          swap_AB                                            ; regA <-> regB
  $B8E7  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B8EA  DB                          bitor                                              ; regA = regA | regB
  $B8EB  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
 >$B8EE  AC 28 D6                    host_call_simple         $D628 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B8F1  D7 FA B8                    branch_nz_abs            $B8FA                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B8F4  AC 82 D9                    host_call_simple         $D982 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B8F7  D8 0E B9                    branch_z_abs             $B90E                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
 >$B8FA  89 80                       loadA_imm_sbyte          -128                      ; regA = sign-extended signed byte literal (handler $EAAF)
  $B8FC  CD                          swap_AB                                            ; regA <-> regB
  $B8FD  A5 A1 6D                    loadA_mem_byte           $6DA1                     ; regA = byte at absolute address <word>, zero-extended (handler $EAF9, normal path $EAF0). Word operand (address), NOT word+byte.
  $B900  DB                          bitor                                              ; regA = regA | regB
  $B901  A9 A1 6D                    storeA_mem_byte          $6DA1                     ; byte at absolute address <word> = regA low byte (handler $EB19, normal path $EB12). Word operand, NOT word+byte.
  $B904  AC 28 D6                    host_call_simple         $D628 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
  $B907  D8 0D B9                    branch_z_abs             $B90D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B90A  AC 09 D6                    host_call_simple         $D609 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B90D  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return
 >$B90E  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B90F  A8 79 6F                    loadA_mem_word           $6F79                     ; regA = word read using <word> as address (handler $EA75)
  $B912  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B913  8C 1B 6F                    loadB_imm_word           $6F1B                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B916  BB                          add                                                ; regA = regA + regB
  $B917  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B918  A8 5F 6F                    loadA_mem_word           $6F5F (selected_province_idx) ; regA = word read using <word> as address (handler $EA75)
  $B91B  2A                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 10)
  $B91C  8D 1A                       op_8D_sbyte              +26
  $B91E  62                          push_imm4                                          ; Push 16-bit (high=0, low=opcode&0x0F) to VM data stack at ptr1  (inline operand = 2)
  $B91F  E9 7B CC 04                 host_call                $CC7B (ui_helper_cc7b) {bytecode}, $04 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B923  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B924  D0                          incA                                               ; regA += 1
  $B925  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B926  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B927  D0                          incA                                               ; regA += 1
  $B928  B3                          pushA                                              ; Push regA to VM data stack (handler $ECCE)
  $B929  8E 9F BF                    push_imm_word            $BF9F                     ; "Turn %2d  Fief %2d"  | Push a 16-bit word literal from the bytecode stream onto the VM data stack (handler $EAD3: jsr $EFBF 16-bit fetch, jmp $F008 push). Classifier mis-sized this as a 1-byte operand — $EFBF fetches a word, same mis-sizing as $D6/$D7/$D8/$8A/$8C.
  $B92C  E9 34 D1 06                 host_call                $D134 (ui_helper_d134) {bytecode}, $06 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B930  A4 F9 7B                    loadA_mem_word           $7BF9                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B933  D7 39 B9                    branch_nz_abs            $B939                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B936  AC 94 80                    host_call_simple         $8094 {bytecode}          ; JSR to native function / bytecode subroutine at <word>; no ptr1 adjust
 >$B939  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B93A  E9 72 D7 02                 host_call                $D772 (ui_helper_d772) {bytecode}, $02 ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
  $B93E  8C 67 6D                    loadB_imm_word           $6D67                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B941  BB                          add                                                ; regA = regA + regB
  $B942  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B943  D7 7D B9                    branch_nz_abs            $B97D                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B946  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B947  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B94A  BB                          add                                                ; regA = regA + regB
  $B94B  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B94C  D5                          switch                                             ; Jump-table dispatch on regA (handler $EC65) — reads inline (value,target) pairs from the bytecode stream, jumps to the matching target. Variable-length; the disassembler stops the fall-through run here.
  ; ---- gap $B94D-$B97C (48 bytes, not on any traced path) ----
 >$B97D  0A                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 10)
  $B97E  8C F7 6C                    loadB_imm_word           $6CF7                     ; regB = 16-bit word literal (handler $EAC8). Classifier mis-tagged as byte+sbyte; confirmed word by the 9999-clamp sites (e.g. bank 1 $AB49 'loadB_imm_word 9999').
  $B981  BB                          add                                                ; regA = regA + regB
  $B982  D3                          loadA_ind_byte                                     ; regA = byte at [regA], zero-extended
  $B983  55                          setB_imm4                                          ; regB = (opcode & 0x0F), high byte 0  (inline operand = 5)
  $B984  C0                          cmp_eq                                             ; regA = (regA == regB) ? 1 : 0
  $B985  D8 8D B9                    branch_z_abs             $B98D                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B988  3A                          storeB_local_neg                                   ; word at frame[-24 + 2*(n-0x30)] = regB  (inline operand = 10)
  $B989  E9 9B B7 02                 host_call                $B79B {bytecode}, $02     ; JSR to native function / bytecode subroutine at <word>; ptr1 += <byte> after return
 >$B98D  A4 79 6F                    loadA_mem_word           $6F79                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B990  D8 9C B9                    branch_z_abs             $B99C                     ; if regA == 0: ptr3 = word operand (handler $EBD1). The dominant conditional branch — pairs with a cmp_* opcode that leaves 0/1 in regA. Word operand, NOT sbyte.
  $B993  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B994  D0                          incA                                               ; regA += 1
  $B995  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B996  A8 01 60                    loadA_mem_word           $6001                     ; regA = word read using <word> as address (handler $EA75)
  $B999  D6 A7 B9                    jump_abs                 $B9A7                     ; ptr3 = word operand — unconditional absolute jump (handler $EBBB; fetches a word via $EFBF)
 >$B99C  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B99D  D0                          incA                                               ; regA += 1
 >$B99E  2B                          storeA_local_neg                                   ; word at frame[-24 + 2*(n-0x20)] = regA  (inline operand = 11)
  $B99F  0B                          loadA_local_neg                                    ; regA = word at frame[-24 + 2*n]  (n = opcode & 0x0F)  (inline operand = 11)
  $B9A0  A6 9D 6D                    loadB_mem_word           $6D9D                     ; regB = word at absolute address <word> (handler $EA5C, normal path $EA4B). Word operand, NOT word+byte.
  $B9A3  C6                          cmp_uge                                            ; regA = (regA >= regB) ? 1 : 0  (unsigned)
  $B9A4  D7 AA B8                    branch_nz_abs            $B8AA                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
 >$B9A7  A4 79 6F                    loadA_mem_word           $6F79                     ; regA = word at absolute address <word> (handler $EA43, normal path $EA37). Classifier mis-sized as word+byte — the $EFD5 fetch is word-only; the extra byte is a not-taken conditional path. Same fix as $A7/$A8.
  $B9AA  D7 B1 B9                    branch_nz_abs            $B9B1                     ; if regA != 0: ptr3 = word operand (handler $EBC5). Word operand, NOT sbyte — the classifier mis-sized this; $EFBF fetches 16 bits.
  $B9AD  40                          clearA_40                                          ; regA = 0 (own handler $EA00, same effect as $41-$4F)
  $B9AE  A8 01 60                    loadA_mem_word           $6001                     ; regA = word read using <word> as address (handler $EA75)
 >$B9B1  CF                          vm_return                                          ; Restore caller frame from frame[0..7], JMP (ptr0) — VM subroutine return

; ==== summary ====
; 131 subroutines scanned; 7296 instructions decoded (7296 known + 0 unknown opcodes)
; 426 branch targets, 159 unique host_call targets
